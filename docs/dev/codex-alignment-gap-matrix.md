# Codex Alignment — HiCodex vs Codex Desktop Gap Matrix

Actionable status of HiCodex's intermediate-output parity with Codex Desktop, last reconciled 2026-05-24.

This is the index doc for what is **done**, what is **deliberate divergence**, and what remains open work. Each row links to the per-topic deep dive.

Linked:
- [dispatcher](./codex-alignment-dispatcher.md)
- [reasoning](./codex-alignment-reasoning.md)
- [plan](./codex-alignment-plan.md)
- [unified diff](./codex-alignment-unified-diff.md)
- [collab agent](./codex-alignment-collab-agent.md)
- [image generation](./codex-alignment-image-generation.md)

Legend: 🟢 aligned · 🟡 minor divergence (visual or non-blocking) · 🔴 functional gap · ✅ recently closed · ~~ID~~ retracted (was a false positive).

## A. Streaming delta (dispatcher)

| ID | Codex | HiCodex | Gap | Status |
| --- | --- | --- | --- | :-: |
| A1 | 5 text/output delta channels; `patchUpdated` independent; `summaryPartAdded` break; `fileChange/outputDelta` sampled; `mcpToolCall/progress` ignored | reducer was over-subscribed; 4 non-Desktop channels retired, 1 (`fileChange/patchUpdated`) kept as documented HiCodex extension | converged | ✅ |

See [dispatcher doc](./codex-alignment-dispatcher.md) for the channel list and rationale.

## B. Reasoning + Thinking placeholder

All B1–B3, B5–B6, B8, B10 entries were retracted after the 2026-05-20 deep dive (see [reasoning doc](./codex-alignment-reasoning.md)). The agent timeline in Codex never renders reasoning ThreadItems — the `Ux` / `qx` render path is dead code. HiCodex's synthetic `desktopThinkingPlaceholderItem` mirrors Codex's live placeholder path and is only rendered while the turn is in progress.

Remaining items:

| ID | Item | Codex behavior | HiCodex | Status |
| --- | --- | --- | --- | :-: |
| B4 | Composer ReasoningEffort selector (6 labels None/Minimal/Low/Medium/High/Extra High) | implemented in Codex `reasoning-minimal-*.js` chunk | scope = Composer parity (not intermediate-output) | 🟡 reclassified |
| B7 | Exploration counts join | plain `, ` separator (`...accordion.count.separator`), NOT a conjunction (see §I RA10) | `explorationSummaryLabel` joins with `", "`; `joinConjunction` kept only for the cross-type summary | ✅ (corrected 2026-05-29) |
| B9 | Reasoning-complete remnant | Codex placeholder disappears (`BC` / `Iy` returns `none`) | HiCodex drops completed reasoning and does not leave a `Thought for {time}` row | ✅ |

## C. Plan summary card

See [plan doc](./codex-alignment-plan.md).

| ID | Item | Codex defaultMessage | HiCodex | Status |
| --- | --- | --- | --- | :-: |
| C1 | fadeType | `completed ? 'none' : 'indexed'` | same | 🟢 |
| C2 | Header title | `Plan` / `Writing plan` | same | 🟢 |
| C3 | Download filename | `PLAN.md` | same | 🟢 |
| C4 | Toggle aria-label | `Expand plan summary` / `Collapse plan summary` | aligned (`plan-summary-card.tsx`) | ✅ |
| C5 | Toggle tooltip | `Expand` / `Collapse` | aligned | ✅ |
| C6 | "Open in new window" button + `show-plan-summary` dispatch | exists in Codex but its main-process handler is `break;` (stub) | HiCodex stub-aligned with noop onClick | ✅ (Codex itself is a stub) |
| C7 | Collapsed-bottom "Expand plan" gradient button | `Expand plan` | same | 🟢 |

## D. Collab agent (multi-agent tool calls)

See [collab agent doc](./codex-alignment-collab-agent.md).

The entire D section was retracted after the 2026-05-20 reconciliation. HiCodex `multiAgentRowVerb` (row form, `Failed spawning` etc.) and `multiAgentSendInputPromptVerb` (prompted form, `Failed to message`) match Codex's `rowAction.*` and `rowAction.sendInput.messaged.*` tables respectively. The earlier "Failed to spawn" mismatch was a misread of the header table.

## E. Unified diff (inline card + Failure Dialog)

See [unified diff doc](./codex-alignment-unified-diff.md).

| ID | Item | Codex | HiCodex | Status |
| --- | --- | --- | --- | :-: |
| E1 (a) | Review button label | `Review here` | aligned in `event-unit.tsx` | ✅ |
| E1 (b) | Review file count tooltip / aria | `Review changed files` + `Review` | aligned | ✅ |
| E1 (c) | Undo / Reapply per-file tooltips | `Undo` / `Reapply` | aligned | ✅ |
| E2 (UI) | Failure Dialog with 13 strings, 3 tone classes, max-h-40vh | clean-room `UnifiedDiffFailureDialog` implemented | ✅ |
| E2 (backend) | `host_apply_patch_action` Tauri command running `git apply` / `git apply --reverse` with non-git detection | implemented in `apps/desktop/src-tauri/src/main.rs` | ✅ |
| E2 (state) | `applyPatchAction` bridge + Dialog wiring | `tauri-host.ts` + `HiCodexApp.tsx` + `conversation-view.tsx` + `event-unit.tsx::ToolBlock` | ✅ |
| E-inline | Edited-file detail list (per-file rows, line counts, show-more, large-file placeholder) | Codex renders full list; single-file detail row uses `Details` and omits duplicate stats | HiCodex inline card matches | ✅ |

## F. Turn container / Worked-for divider

| ID | Item | Codex | HiCodex | Status |
| --- | --- | --- | --- | :-: |
| F1 | `Working` / `Working for {time}` / `Worked for {time}` with 1000ms threshold | 1000ms threshold | `worked-for-divider.tsx` matches | 🟢 |

## G. Image generation

See [image generation doc](./codex-alignment-image-generation.md).

| ID | Item | Codex | HiCodex | Status |
| --- | --- | --- | --- | :-: |
| G1 | Turn-level inline gallery (`MAX_GALLERY_ROW = 4`, hover prev/next, overflow badge) | implemented | `GeneratedImageGallery` + turn projection implemented | ✅ |
| G2 | Right-rail Artifacts 6-item cap + Show more/less | `Qd = 6` + `showMore`/`showLess` | `RAIL_LIST_PREVIEW_LIMIT = 6` verified | ✅ |
| G3 | Full-screen lightbox (Prev / Next / Close) | implemented | `ImagePreviewLightbox` controlled mode with nav/close/zoom implemented | ✅ |

## H. Plan / Todo summary

| ID | Item | Codex defaultMessage | HiCodex | Status |
| --- | --- | --- | --- | :-: |
| H1 | tasks completed | `{completed} out of {total} tasks completed` | `${completed} out of ${plan.length} ${taskLabel} completed` | 🟢 |
| H2 | step index prefix | `{index}.` (1-based) | `${index + 1}.` | 🟢 |

## I. 2026-05-29 re-audit (Codex Desktop v26.519.81530, May 27 ASAR)

The installed Desktop app updated to v26.519.81530 (May 27), post-dating the 2026-05-24 pass — the exact "re-audit after an app update" trigger. A multi-agent re-audit of all 8 intermediate-output surfaces (each finding adversarially evidence-verified against the fresh ASAR) surfaced 24 divergences: 18 applied this pass, 6 deferred. Every change cites a durable token (ICU id / defaultMessage / constant / data-attr / behavior) verified against the fresh build.

Applied:

| ID | Surface | Change | Durable evidence |
| --- | --- | --- | --- |
| RA1 | dispatcher | `model/rerouted` now synthesizes a `modelRerouted` timeline item (was log-only); `itemType` normalizes the camelCase protocol type to `model-rerouted` | onNotification pushes `{type:'modelRerouted',fromModel,toModel,reason}`; renderer gates on `reason==='highRiskCyberActivity'` |
| RA2 | unified-diff | Failure Dialog title is a 4-way selector (not-git-repo / Some changes / No changes / Failed to) | `codex.unifiedDiff.{revertPatchPartial,revertPatchNoChanges,revertPatchError}` branched on appliedPaths/skipped/conflicted |
| RA3 | unified-diff | Body intro line (description tone) above the path list; only the path block caps at 40vh | `patchFailureDetailsIntro*` / `patchNotGitRepoDescription` as body copy; `max-h-[40vh]` on the path list |
| RA4 | unified-diff | Clean apply → success toast; thrown action → danger toast (dialog reserved for partial/conflict results) | `revertPatchSuccess` / `revertPatchError`, toast id `turnDiffPatchAction` |
| RA5 | unified-diff | execOutput folded into the intro as single-line "Git apply error: {message}" (removed collapsible `<details>`) | `patchErrorOutputSummary`="Git apply error: {message}" |
| RA6 | unified-diff | Completed-card trailing Review button "Review changes" → "Review" | `codex.unifiedDiff.viewDiffTooltip`="Review" |
| RA7 | unified-diff | Hover subtitle = "Review changes" + a separate arrow icon (dropped the literal "→") | `reviewChangesHover`="Review changes" + icon element |
| RA8 | plan | Copy uses shared "Copy"/"Copied" (was "Copy plan"); removed invented "Copied to clipboard" toast; reset 1500→2000ms | `copyButton.copyAriaLabel`/`copiedAriaLabel`; `2e3` reset |
| RA9 | plan | Collapsed fade overlay 112→160px; "Expand plan" button restyled as filled primary | `h-40 bg-gradient-to-t from-token-input-background to-transparent`; `color:"primary"` |
| RA10 | exploration | Header counts joined with plain ", " (was Oxford comma + "and"); cross-type summary keeps the conjunction | `localConversationTurn.exploration.accordion.count.separator`=", " — corrects the earlier B7/§3 claim |
| RA11 | exploration | Accordion body heights set to {preview 7rem, expanded 20rem scroll, collapsed 0} | height map `{preview:7rem,expanded:20rem,collapsed:0px}` |
| RA12 | image-gen | Removed invented `role`/`aria-label` from the pending tile and carousel nav wrapper (kept per-button labels) | "Generating image"/"Generated image carousel" absent from all chunks |
| RA13 | render-groups | Restored the per-message timestamp as the trailing hover/focus affordance in the assistant action row (today/weekday/date buckets) | action row appends a trailing `sentAtMs` span revealed on hover/focus |
| RA14 | render-groups | Selected turn-rating thumb swaps to a filled glyph | thumb icon selected→filled, outline otherwise |
| RA15 | turn-rating | Removed invented "Feedback sent" post-submit node (Desktop just closes the popover) | string absent from `plan-summary-item-content-*.js` |
| RA16 | markdown | Link/autolink href allowlist = http/https/irc/ircs/mailto/xmpp/codex; dropped ftp; codex:// stays clickable | `/^(https?\|ircs?\|mailto\|xmpp\|codex)$/i` |
| RA17 | markdown | Image `src` sanitized (data:image/video, file/relative, allowlisted schemes; drops javascript: etc.) | `ut()` img-src sanitizer, `/^data:(?:image\|video)\//i` |

Deferred (rationale):

- **RD1 dispatcher** — `turn/plan/updated` synthesizing a `todo-list` item is internal-only: Desktop does not render todo-list as a standalone transcript row, and HiCodex's `turnPlan` snapshot already drives the same live right-rail Progress (no visible divergence).
- **RD2 dispatcher** — the `requestApproval`/`tool/call`/`requestUserInput` cases in `applyNotification` are dead (id-bearing → `onServerRequest`); removing them is cleanup with no visible change.
- **RD3 unified-diff** — failure-dialog path rows as file-open buttons need `cwd`/`hostId` plumbed through `HiCodexApp`.
- **RD4 markdown** — `<sub>` containing a link → `<span>` is niche; needs inline-link detection in the sub renderer.
- **RD5 plan** — "Writing plan" gradient text-shimmer vs HiCodex's opacity-pulse + indexed fade; the indexed fade is already accepted as a clean-room choice.
- **RD6 markdown** — hljs token-scope coverage (shiki vs hljs); exact token boundaries differ by tokenizer.

Pre-existing (not from this pass): `tool-activity-detail.test.ts::buildsMcpDetails` (MCP text-result "plaintext" code-container title) fails on the current staged tree, inside `tool-activity-detail.tsx` which this pass did not touch — flagged for the owner of that in-progress MCP work.

Keep live visual verification as a separate audit before claiming complete product parity. This table is evidence-backed for the inspected local Desktop build (v26.519.81530) only; re-audit after the next app update.

## J. 2026-05-29 visual-contract pass (CSS/layout, not strings)

The §A–§I audits proved string/structural parity but, as line 131 warns, cannot see visual divergence. This pass closes that: a multi-agent **visual-contract** sweep (44 agents) resolved the real bundle's Tailwind utilities to concrete px/rem/hex against `app-main-*.css` (the 459KB compiled stylesheet) and diffed those against the clean-room's semantic CSS, per conversation surface. 28 divergences confirmed (1 uncertain, 8 refuted incl. a false "uniform 16px turn-gap → per-item margin" claim that the within-turn 16px spacer already matches). 26 applied + 3 orphaned rules removed; 3 held. Every value cites the resolved bundle token.

User-message bubble audited **fully aligned** (right-aligned `bg-token-foreground/5` 77% bubble, `rounded-2xl` 16px, `px-3 py-2`, 13px/1.5) — no change.

Applied:

| Surface | Change | Durable evidence |
| --- | --- | --- |
| assistant prose | line-height → 22px (was 1.86≈24px) | `leading-[calc(--codex-chat-font-size+8px)]`, electron 13px → upstream 22px |
| assistant prose | headings: weight 750→600; h1 18→24px/lh1.2/mt-5 mb-2; h2 16→18px/lh1.33; h3–h6 14→18px/lh1.33/mt-3 mb-1.5 | `heading-lg/base/subsection` + `font-semibold` override; `--text-heading-lg 24px`, `--text-heading-md/sm 18px` (electron) |
| assistant prose | `p` margin 0 0 10px → 8px 0 + `>:first-child{mt:0}`; ul/ol mb 8→16px, pl 22→16px; li 4px→0 0 6px | `my-2`, `[&>*:first-child]:mt-0`, `mt-0 mb-4 pl-4`, `mb-1.5` |
| markdown | inline code 1px 4px/r4 → 2px 6px/r6; blockquote → my-3 border-l-2 pl-4 italic; strong 700→600; hr 14→16px | `rounded-sm px-1.5 py-0.5`, `my-3 border-l-2 pl-4 italic`, `font-semibold`, `my-4` |
| conversation | transcript body padding-inline 24→16px | `px-toolbar` = `--padding-toolbar` = .25rem×4 = 16px |
| conversation | empty-state hero weight 600→400, drop letter-spacing | hero `heading-xl ... font-normal` (400) |
| action row | icon button 22×20→26px square, radius 5→8px; removed 120ms reveal transition (now instant) | `size:icon electron:p-1` + 16px glyph; `electron:rounded-md`=`--radius-md` .5rem=8px; opacity utils, no transition |
| composer | footer chips 13→12px, height 26→28px; field backdrop-blur 8→16px | `button size:composer` text-sm 12px + `h-token-button-composer` .25rem×7=28px; `backdrop-blur-lg`=`--blur-lg` 16px |
| worked-for | live divider label 13→14px, color #6f716b→#767779 | `text-size-chat`→`--text-base` 14px; `text-token-foreground/60` over white |
| tool card | exec card radius 8→10px; footer 11→14px + pad 2px 10px 4px; worked-for divider gap 4→8px | `rounded-lg`=`--radius-lg` .625rem=10px; `text-size-chat` 14px `px-2.5 pt-0.5 pb-1`; `gap-2`=8px |
| exec card (structure) | removed unconditional "Shell" title row + cwd row (kept in copy text); `$`+command → single `$ {command}` monospace run | thread exec uses `Jh` `variant:"embedded"`, which gates the shell-name header and cwd behind `variant==="default"`; command is one `codex.shell.commandLine` `<pre>` run |
| composer (structure) | moved the model / reasoning-effort / permissions chip cluster INTO the bubble footer's middle column (new `ComposerSettingsChips` in a Composer `footerSettings` slot); branch + work-mode stay in the below-bubble external footer | `composer-footer grid-cols-[auto_minmax(0,1fr)_auto]` holds the intelligence (`data-codex-intelligence-trigger`)/reasoning/permissions triggers; the external-footer chunk carries branch + local/cloud only |
| conversation (brand) | main transcript background `#faf9f5` (sand) → `#fff` | `bg-token-main-surface-primary` → `--color-background-surface` → `--gray-0` = #fff (light theme) |
| tool card (structure) | MCP text-result block → code-style card (`rounded-lg border` + code-block bg + sticky `plaintext` header) via new `McpPlaintextCard` + `.hc-mcp-plaintext-body`; greens the long-red `buildsMcpDetails` test | `Zg` case `text` = `rounded-lg border border-token-input-background bg-token-text-code-block-background` + header title id `codex.mcpTool.textBlock.plaintextTitle` ("plaintext", desc "rendered in a code-style container"). Corrects a prior in-code note that misread it as a bare div |
| tool card (overlap) | exec copy buttons no longer stack: while the command row is hovered the card-level "copy command and output" steps aside so the per-command copy wins (`:has()`) | Codex scopes each copy via named `group-hover/command` / `group-hover/output` vs the card-level "Copy shell contents" — never co-revealed at one anchor |
| turn-collapse (behavior) | the "Worked for" collapse no longer hides USER messages — every user-message unit is now persistent (never collapsible) in `splitTurnUnits` (turn-collapse.tsx:141); added a regression test | Codex `Lb`/`zb` (local-conversation-thread): a `user-message` entry is NEVER collapsible — leading → `userItems`, mid-turn → `persistentEntries` (`steeringStatus!=null \|\| hookFeedback`, and Codex always stamps `steeringStatus` on steering msgs). HiCodex never stamps `steeringStatus`, so plain mid-turn user msgs wrongly fell into the collapsible body and got hidden on collapse |

3 orphaned CSS rules removed after the exec-card structure change (`.hc-exec-shell-title`, `.hc-exec-shell-cwd`, `.hc-exec-shell-command span`).

Nothing remains held — all three originally-deferred items were approved and applied this session (rows above): the composer pill-cluster move, the `#faf9f5`→`#fff` background, and the MCP text code-card. The MCP "shared class" worry was sidestepped by giving the card its own `.hc-mcp-plaintext-body` and leaving the bare `.hc-mcp-result-text-body` for the generic (possibly non-MCP) result-text fallback; the "owned elsewhere / in-progress" framing didn't hold (single-owner repo), so the long-red `buildsMcpDetails` now passes. The composer move (`ComposerSettingsChips` slot) still cannot be visually verified headlessly — flagged for an in-app eyeball (the chip left/right distribution in the bubble footer may need a nudge).

Verification: `tsc --noEmit` clean; `vite build` green; **all 73 UI tests pass** (the long-red `buildsMcpDetails` went green once the MCP code-card landed — its "plaintext" assertion now holds); `composer-external-footer.test.ts` updated to the in-bubble structure (chips asserted on `ComposerSettingsChips`; the external footer asserted to no longer carry them). CSS value edits carry no test impact (no className/DOM changes).

## K. 2026-05-29 behavioral / interaction audit (conversation transcript)

§I/§J covered static parity (strings, visual contract). This pass adds the BEHAVIORAL dimension — HiCodex's interaction/projection/reducer LOGIC vs Codex bundle logic — which static audits cannot see (the turn-collapse user-message bug and the exec copy overlap were both behavioral, found only by use). 28-agent audit across 6 interaction facets (collapse/expand, hover/focus reveal, message actions, streaming/live states, click navigation, auto behaviors); every finding adversarially verified: **16 confirmed, 5 refuted, 1 uncertain**.

Applied:
- **exec card** — removed the card-level "Copy command and output" button: Codex's embedded `Jh` variant exposes ONLY per-command + per-output copy (scoped `group/command`, `group/output`). The earlier `:has()` overlap patch is removed — the extra button was the real divergence, not a scoping mismatch. + regression test.
- **exec output** — running output auto-scrolls to bottom ONLY when the user is already at the bottom (codex `Xp` at-bottom flag, 24px threshold), instead of pinning unconditionally on every chunk.
- (earlier this session) **turn-collapse** — user messages are never collapsible by the "Worked for" toggle (`zb`-equivalent); + regression test.

Implemented (2026-05-30, multi-agent batch — each change re-verified vs bundle by the implementer; the diff reviewed + the broken/partial edits reconciled by the orchestrator):
- **edit affordance** gated to the most-recent user turn (`isMostRecentTurn`, bundle `S===v.length-1`); + regression test. The older-turn ForkFromOlderTurn dialog is now unreachable from edit (fork-only), matching Codex.
- **exec card** — removed the card-level copy-all (embedded `Jh` exposes only per-command/per-output copy); output auto-scroll gated to at-bottom (`Xp`, 24px); patch-card file paths render as buttons that open the file at the first change line (`onOpenFileReference` threaded ToolActivityViewInner → GenericToolActivityView → ToolActivityDetail → PatchChangePath).
- **turn-rating feedback** rewritten as a centered global modal (`TurnFeedbackDialog`, portaled, reusing the existing dialog overlay): "Share feedback" title, pill radios (role=radio in a radiogroup), autofocus textarea, Cmd/Ctrl+Enter submit, no Cancel, Submit disabled until an option is chosen; + regression test.
- **auto-collapse** — `shouldPreventTurnAutoCollapse` keys purely on MCP-app presence (`mcpAppResourceUri`, bundle `dc`/`C`), dropping the in-progress/pending checks (a live turn can't auto-collapse anyway via the `hasFinalAssistantStarted` gate); + updated test.
- **automation-citation chip** deep-links to the specific automation (`focusedAutomationId` threaded into the automations surface), instead of opening a generic panel.
- **inline file-citation** honors Cmd/Ctrl-click (open externally) vs plain-click in-app preview.
- web-search-group "active" + MCP-app inline-only auto-expand adjusted in the exec/transcript cluster.

NEEDS IN-APP VISUAL CONFIRMATION: the exec command-row interaction and the turn-rating modal are substantial behavioral rewrites done by subagents (two stalled mid-report but left working, test-passing edits, completed/reconciled by the orchestrator). They pass `tsc` + all UI tests + `build`, but were NOT visually verified (HiCodex renders cannot be observed headlessly). Eyeball these in the app; revert per-fix if any regress.

Deferred:
- **in-thread find** (search the conversation MODEL + scroll off-screen turns into view, vs HiCodex's rendered-DOM-only search): BLOCKED as too risky for a blind change (virtualization + per-unit-kind text derivation + scroll-into-view). Needs a focused, in-app-verified pass.
- **exploration accordion body-height while exploring**: audit evidence was internally contradictory (cited `useState(exploring?preview:collapsed)` yet claimed header-only) — verify against the bundle before changing (risk of regressing §I RA11 preview heights).

Verification: `tsc` clean; `vite build` green; **74 UI tests pass** (added regression tests: edit-gate, MCP-app auto-collapse, turn-rating submit-gate/modal, embedded-exec copy-all absence).

## L. 2026-05-30 re-align vs UPDATED Codex Desktop (re-extracted bundle)

Codex Desktop + the codex repo were updated; the ASAR was re-extracted (`--force`) — chunk hashes changed (`local-conversation-thread` Kn0WAsVa→DAwsPWah, `app-main.css` Dqf4TBdO→DGDTSRlh, `feedback-form-dialog` BZv3Fod0→JIafVR4J, `plan-summary-item-content` bh4yD4m8→n25PWfVz). 30-agent audit of right-rail/popups + a transcript re-verify, each finding adversarially verified: 14 confirmed, 4 uncertain, 7 refuted.

Re-verified ALIGNED to the new build (the update did not regress prior work): the transcript surfaces (turn-collapse `wD`, exec card `Hv`/`Wv`, exploration/web-search/MCP groups) and the just-built turn-rating `TurnFeedbackDialog` (vs the renamed feedback-form-dialog chunk).

Applied (right-rail/popup batch — `tsc` clean, `vite build` green, 74 UI tests pass):
- Environment: Changes-row trailing is null when there are no diff stats (was always "+0 -0"); Changes icon FileDiff@18 (was PencilLine@14); Local icon Laptop@18 (was Monitor@14); diff-stat chip colors → git-decoration values + dark override; floating-card comment reconciled (pt-3, no backdrop-blur).
- Sections: background processes split into TWO sections (Subagents + Tasks) matching the new wrapper (was one merged); + tests updated. Header right padding 8→10px (`pe-2.5`); rail empty-state 13→14px (`text-base`).
- Inline end-resource cards: subtitle falls back to the directory path when no file-type label matches (was "File · {EXT}"); image-file cards render the icon tile, dropping the invented inline `<img>` thumbnail.
- Turn-rating: feedback pill icon 14→12px (`icon-xxs`), thumbs glyph 13→16px (`icon-xs`); refreshed stale chunk-name CODEX-REFs.

Deferred — no HiCodex infra / no implementation evidence (goal: skip parts without a basis):
- inline "Open in" DROPDOWN (Codex `ME` needs a host `ac({turnId,cwd,hostId,openPath})` → open-in-folder / Codex-Browser targets; HiCodex has no such host capability — kept the static label).
- "View all processes" after-button on the Tasks section (needs a host callback + renderer JSX).
- Local-row execution-mode icon swap (cloud/worktree/local — needs an execution-mode field on the branch-details view model).
- Changes-row loading spinner (needs a diff-stats loading flag).

THEME propagation — DONE (2026-05-30): the dark pipeline already exists (`.hc-app[data-theme="dark"]` overrides `--hc-*`). Tokenized the 3 hardcoded-hex surfaces that didn't follow it — settings-command.css (the Settings/Appearance dialog, the reported bug: 3 inline token swaps + 25 `[data-theme="dark"]` override blocks / ~173 selectors), rail-artifact-preview.css (1 + 11), automations.css (4 + 18) — light mode byte-identical, dark values sourced from the `--hc-*` dark tokens (Codex `.electron-dark` palette). Also DEFINED 3 tokens that settings-command.css consumed via light-fallback but base.css never declared (`--hc-surface-default` #fffdfa→#2a2a2a, `--hc-surface-subtle` #ebe7dc→rgba(255 255 255/.05), `--hc-text-accent` #2d5562→#7aa9b8) — these caused the Appearance section to stay light in dark mode. `tsc`/`vite build` green; needs an in-app dark-mode eyeball. (The mid-run "Missing opening (" build error was a transient vite/Tailwind cache artifact from the concurrent agent edits, not a real CSS fault — a clean rebuild passes.)

Pending phase: **LANGUAGE EN/ZH** (designed 2026-05-30; see memory `project_hicodex_i18n_exists`). CORRECTION: HiCodex is NOT English-only — a full EN/ZH i18n scaffold ALREADY exists and is wired end-to-end (`state/i18n.ts` formatI18nMessage w/ ICU + fallback id→en-US→defaultMessage; `components/i18n-provider.tsx` + `useHiCodexIntl`; provider mounted HiCodexApp:4371; `uiLocale`/`setUiLocale` + persistence `desktop.hicodex.locale`; a command-panel Language switch) — but it is ~98% UNUSED (catalog ~12 keys; 11 `formatMessage` call sites vs ~600-700 literals). So the work is: (1) add a **Language `AppearanceRow`** to `appearance-settings-panel.tsx` (mirror the Theme `SegmentedToggle`; thread `onSetUiLocale` like `onSetUiTheme` through model-settings-panel → HiCodexApp `setUiLocale` :609 — backend/persistence already work, only the in-panel control is missing); (2) fill the catalog (split `HICODEX_MESSAGES` into per-surface modules) + migrate literals by surface (state/ label projections first ~376, then attribute-heavy chrome, transcript last) — `state/` projections take a `format` fn from HiCodexApp since they can't use the hook; (3) seed ZH by extracting only HiCodex's used ids from Codex's `zh-CN-*.js` chunk (6552 flat `{id:中文}` entries); strings with NO Codex id stay English (graceful fallback — do not hand-translate, per the goal). Codex itself uses react-intl/@formatjs, `localeOverride` setting (default null=auto), a General-settings language picker (`settings.ide.language.label`); HiCodex's simplified EN/ZH + override is sufficient (skip the Statsig gate).

LANGUAGE Phase 1 — DONE (2026-05-30): added the **Language `AppearanceRow`** to `appearance-settings-panel.tsx` (a SegmentedToggle, native labels English / 简体中文), threaded `uiLocale` + `onSetUiLocale` through `SettingsPanel` (model-settings-panel.tsx) ← `HiCodexApp.tsx:4855` (`setUiLocale` :609). The switch is now discoverable in Settings → Appearance and re-renders via the existing IntlProvider; `tsc`/`vite build`/74 tests green. (Codex hosts language in General settings; HiCodex puts it beside Theme in Appearance — a minor placement divergence, the existing command-panel Language entry remains a second path.) Phase 2 (the large remainder) = fill the catalog (ZH from Codex `zh-CN`) + migrate literals surface-by-surface; until then only the ~12 existing catalog keys translate, the rest stay English via the graceful fallback.

LANGUAGE Phase 2 — IN PROGRESS (2026-05-30): evidence-backed translatable scope = the **63 Codex i18n ids cited in CODEX-REF comments that also exist in Codex's `zh-CN-D2lLifL1.js`** (extracted via `import()` of the chunk's default export → `/tmp/hc-zh-hits.json`; the other 30 cited dotted-strings were regex false-positives like `thread.turns.map` or unmatched ids → stay English, per "没依据不做"). Wiring pattern (already idiomatic here — `thread-item-view.tsx:207`, `message-unit.tsx:3946`): **components** call `const { formatMessage } = useHiCodexIntl()`; **exported helpers** take an optional `formatMessage?: FormatMessage` arg (fallback returns the English `defaultMessage`). Each wrap uses the EXACT current English literal as `defaultMessage` (zero EN regression) + adds en-US mirror & zh-CN entries to `state/i18n.ts` `HICODEX_MESSAGES`. **DONE+verified this pass (80 strings; tsc 0 / 74 tests incl. new ZH/ICU assertions in `test/i18n.test.ts` / build green — note: clear stale `*.tsbuildinfo` before `npm run build` or tsc emits phantom errors in unrelated files like `kb-library-view.tsx`):** `reasoning-picker-menu.tsx` (Reasoning header + 6 effort labels 无/极低/低/中/高/超高), `appearance-settings-panel.tsx` (Theme/Language/Language-desc/Code-font-size/Reduce-motion/px row labels), `tool-activity-detail.tsx` (MCP plaintext header → 纯文本), `mcp-server-config-form.tsx` (Save/Update-{name}-MCP/Connect-to-custom-MCP), and the **entire unified-diff surface** — `event-unit.tsx` (9: editedFile{filename}/editedFiles{plural}/filesChanged{plural}/reviewChanges/reviewChangesHover/reviewChangedFiles/viewDiffTooltip/showMoreFiles{plural}/collapseFiles; `formatTurnDiffFileCount`/`formatTurnDiffFilesChanged` gained an optional `formatMessage` arg) + `unified-diff-failure-dialog.tsx` (17: the 8-way revert/reapply title selector + 5-way intro incl. patchErrorOutputSummary{message} + 3 path-headings{count} + Close). reviewShort stays English (no Codex zh). The ICU plurals/interpolation are LOCKED by tests; only the responsive full/short span VISUAL fit still needs the user's in-app eyeball. **Pass 2 — state projections (DONE this pass):** the projection-label problem (labels baked into projected data, far from any hook) is solved WITHOUT threading through deep call chains — instead `HiCodexApp` (which renders, and so sits above, the IntlProvider) builds a memoized `formatUiMessage` from `uiLocale` (`createI18nBundle`+`formatI18nMessage`) and passes it into the projection helpers. Done via this mechanism: `settings-panel-workflow.ts` — **20 settings-nav labels** (18 sections with a Codex `settings.nav.*` id + the App/Host group headings; HiCodex-only sections models/images/permissions/approvals/apps/experimental have no Codex id → stay English) through a new `SETTINGS_SECTION_I18N_IDS` map + `settingsSectionTitle()`/`settingsGroupHeadingTitle()` helpers, consumed by `model-settings-panel.tsx` (3 render sites + the placeholder shell); and `composer-workflow.ts` — the **3 composer-placeholder** branches (newTask.locally.v2 / localFollowUp.locallyWithAgents / localFollowUp.locally). For settings-nav + placeholders the canonical English lives in the projection (`SETTINGS_SECTIONS.title` / inline defaultMessage), so only **zh-CN** entries were added (en falls back, no drift). **Tail — DONE (2026-05-30, +14 strings → 80 total):** (a) `/memories` dialog (`slash-request-workflow.ts` — `formatMessage` threaded via a new `SlashRequestWorkflowContext.formatMessage` field, fed `formatUiMessage` from `HiCodexApp`'s `runSlashRequest`): panel title 记忆 + entry dialogTitle 对话记忆 + both subtitles + all 3 composite bullets — the `"Use memories:"/"Generate memories:"` prefixes turned out to HAVE Codex ids (`use/generateMemoriesLabel` = 使用记忆/生成记忆), so the bullets localize fully as `<label>: <desc>` (no English-prefix problem after all); only the one HiCodex explanatory sentence "Generate memories controls whether…" stays English. (b) singles: `file-preview-panel.tsx` expand/restore (展开面板/恢复面板宽度), `right-rail.tsx` BOTH empty states (artifacts 暂无产物 + sources 暂无来源), `account-state.ts` logOut (退出登录, via `projectAccountViewModel` formatMessage param ← `HiCodexApp:907`). **SKIPPED (deliberate, per "没依据不做"):** `command-panel.ts` — only the kind-label switch's `experimental` case ("Experimental features (Beta)") has a Codex id (`settings.general.experimentalFeatures`); the other ~14 sibling labels in that switch don't, so wrapping one alone is inconsistent — left English. `reviewShort` (no Codex zh). NOTE: grep-by-id has false multiplicity (an id also appears in config-keys/comments; the real rendered literal is in exactly one) → each wrap is hand-disambiguated, not fanned out. Also: the original 93-id extraction TRUNCATED hyphenated ids (`settings.nav.browser-use` → `settings.nav.browser`), so re-query the chunk by the real (commented) id before assuming "no zh".

§M — broad re-audit workflow (2026-05-30, beyond language). After the i18n passes, ran a Workflow (`/.claude/wf-align-audit.mjs`, audit→adversarial-verify pipeline, 7 surfaces, Explore agents, read-only) over the transcript + right-side popups vs the bundle. 7 surfaces audited; **clean (0 confirmed gaps): assistant message+reasoning, right rail (4 findings all rejected by verify), file-preview popup** — confirms the prior alignment holds. 7 findings confirmed by the verify stage, which I then RE-VERIFIED myself against the bundle before acting (the workflow's verifier over-trusted two): **FIXED (5, all evidence-backed + needsVisualConfirm):** (1) `.hc-turn-diff-file-row` padding 8px 10px → **4px 12px** (bundle `--turn-diff-row-padding-x:.75rem`=12 / `-y:.25rem`=4, button uses `px-[var] py-[var]`; HiCodex's own comment said px-3 but impl was 10px — incomplete port); (2) `.hc-turn-diff-expand-files` horizontal 10→**12px** (its own `px-3` comment; vertical kept — it's `h-9` height-based, not py); (3) `.hc-exec-shell-command-copy` → **top:0 right:0** (`absolute top-0 right-0`); (4) `.hc-exec-shell-output-copy` → **top:0 right:10px** (`top-0 right-2.5`); (5) `.hc-inline-plan-row` line-height 18→**16px** (`leading-4`); plus a dark-theme override for `.hc-turn-diff-file-row:hover` (the hardcoded light `rgba(34,38,43,.04)` ≈ Codex's `bg-token-list-hover-background/60` in LIGHT — audit's "15× difference" was wrong — but had no dark override → invisible on dark; added `rgba(255,255,255,.04)` per the rail-trigger pattern). **SKIPPED after my re-verification (audit was wrong):** pending-request footer border — audit claimed Codex `border-token-border/50` is "6× more visible"; actually token-border≈8% foreground, ×50% ≈ **4%**, vs HiCodex's 8% → HiCodex is *more* visible, direction inverted, diff tiny → left as-is. Plan-row **font-size** 13→14px — Codex uses `text-size-chat`=`var(--codex-chat-font-size)` (a VARIABLE, not a fixed 14px), so the target is uncertain + it's 1px → skipped per "没依据不做". Build + 74 tests green. Net: the audited core is now closed; remaining are pixel/color nuances needing the user's in-app A/B (per [[feedback_hicodex_visual_ab_verify]]).

§M-2 — round-2 audit + dark-theme completeness (2026-05-30). Ran the audit workflow again on 7 NEW surfaces (composer, markdown/code, end-resource cards, command-panel dropdown, settings content, reasoning stream, approval flow). **4 surfaces clean; the 4 confirmed findings ALL failed my re-verification** → 0 fixes: (a) inline-code radius — audit cited the `.hljs` (highlighted) rule `.3em`, not plain inline `<code>`; radius unresolvable + 1.2px; (b) command-row gap 12→8 — audit conflated Codex's slash-item `gap-2` (icon↔label) with HiCodex's `.hc-command-panel-row-main` gap (title-block↔trailing, `space-between`) — different gap semantics + different layout; (c) approval responsive `flex-col sm:flex-row` — narrow-viewport (<640px) stacking; HiCodex is desktop where the panel is ≥640px (Codex's `sm:flex-row` default there) → not applicable; (d) raw `<button>` vs Codex `Button` component — architecture, not visual (clean-room equivalent is fine per DEVELOPMENT.md). The adversarial discipline (my re-check on top of the workflow verifier) correctly rejected all 4 — round-2 surfaces are aligned.
**Dark-theme completeness pass (addresses the user's "theme 有的页面不起作用"):** found interactive-state backgrounds hardcoding HiCodex's light ink `rgba(34,38,43,X)` with NO dark override (invisible on dark — the same bug as §M's diff-hover). FIXED 15: (1) **tokenized 10 exact-value matches** — `background: rgba(34,38,43,0.06)` → `var(--hc-surface-soft)` (light value IDENTICAL = 0.06, and the token already carries a dark value rgba(255,255,255,.05)) across `right-rail.css` (3, +1× 0.08→`--hc-surface-hover`), `responsive-turns.css` (2), `rail-sections.css` (2), `rail-cards.css` (2) — zero light regression, dark now adapts; (2) **4 per-rule dark overrides** for non-token-matching values — `.hc-worked-for-live-rule` divider (white .12), `.hc-thread-item-button:hover` (white .05), `.hc-queued-followup-row` base + hover (light-overlay border+bg). Build + 74 tests green. **ROOT-CAUSE finding (the big remaining dark gap, NOT auto-fixable):** transcript/rail also hardcode many **TEXT** colors with no dark override — `#27313d` (×35), `#6f716b` (×29), `#4f5855` (×10), `#74776f` (×9)… — and NONE match the `--hc-text-*` tokens (#202124/#171717/#8a8b84/#2d5562), so they can't be mechanically tokenized (a swap would change the light appearance). On dark these dark-ink text colors are unreadable.

§M-3 — dark-theme TEXT/surface completeness, APPLIED (2026-05-30). Rather than guess blind, ran a Workflow (`/.claude/wf-dark-theme.mjs`, propose→adversarial-verify, Explore agents, read-only proposals) over the 7 worst-coverage in-scope files. Each agent mapped every hardcoded light-only color (text/icon/border/surface) WITHOUT a dark override to the HiCodex dark token whose ROLE matches (near-black/emphasis text → `--hc-text-primary`=#fff; muted grays → `--hc-text-secondary`=rgba(255,255,255,.70); teal → `--hc-text-accent`; surfaces/borders → `--hc-surface-*`/`--hc-border-default`), emitting ONLY `.hc-app[data-theme="dark"] <exact-selector> { … }` rules (light untouched). The verify stage rejected duplicates/status-colors/wrong-roles; I then RE-checked and caught one defect the verifier missed — the agents used `var(--hc-surface-muted)` which was UNDEFINED (only ever an inline fallback `var(--hc-surface-muted,#f3f1ea)`), so I **defined `--hc-surface-muted`** in base.css (light #f3f1ea / dark #2a2a2a, matching the existing inline fallbacks — this also fixes the pre-existing exec/tool-result boxes that were stuck on the light fallback in dark). **APPLIED ~200 dark overrides** (tool-activity 94, composer 48, rail-cards 16, responsive-turns 17, rail-sections 13, right-rail 9, side-panel-host 3) appended to each file. Build + 74 tests green; because every rule is `[data-theme="dark"]`-scoped and every value is a readable token, LIGHT mode is mechanically unchanged and dark goes from invisible-dark-ink-text to readable role-mapped text. **CAVEAT (honest):** these are evidence-backed (token-role mapped) + light-safe + build-verified, but NOT pixel-visually verified — the primary-vs-secondary shade choice per element may need tuning, so a dark-mode visual A/B pass (per [[feedback_hicodex_visual_ab_verify]]) is the natural follow-up; it is now a *tuning* pass, not a fix-broken pass. NOT yet covered (decent existing dark coverage or out of this batch): message.css (48 dark rules already), markdown.css (57), settings-command.css (173), pending-requests.css (32), conversation.css (24), automations.css (23); audit those next if dark issues remain there.
**SOURCE-VERIFICATION of the dark token values (closes "源码依据" for the dark pass):** extracted Codex's dark palette from `app-main-DGDTSRlh.css` — `.electron-dark` sets `--color-text-foreground: var(--gray-0)` (=`#fff`), `--color-background-surface: var(--gray-900)`; the grays are `--gray-900:#181818 / --gray-800:#212121 / --gray-750:#282828 / --gray-0:#fff`; `--color-token-text-secondary = color-mix(foreground 65%)`, `--color-token-description-foreground / text-tertiary = 70%`. Cross-checked vs HiCodex's `.hc-app[data-theme="dark"]` tokens: **primary `#fff` == Codex `gray-0` (exact); surfaces `#181818/#212121/#2a2a2a` == `gray-900/800/~750` (exact/≈); text-secondary `rgba(255,255,255,.70)` == Codex description/tertiary (70%, exact)** — Codex's *text-secondary* proper is 65% (a 5% subtler second muted level HiCodex collapses into its single muted token; both are Codex dark-palette values). So the ~200 overrides are token-mapped to values that ARE the Codex dark palette, i.e. source-evidenced (not a role heuristic). 
§M-4 — dark coverage batch 2 + the two-tier muted split, APPLIED (2026-05-30). (a) Ran the dark-theme workflow over the 6 remaining (better-covered) files: **automations.css fully covered (all 5 proposals were exact duplicates → 0 applied)**; applied the residual gaps — message.css 28 (incl. the new turn-rating/feedback + math + markdown-heading + collapse-toggle selectors), markdown.css 13 (prompt-link/file-citation/memory-citation), settings-command.css 1 (model-toggle off-track), pending-requests.css 2 (question stepper), conversation.css 6 (background-agent panel). The verify stage rejected exact duplicates + a wrong white-0.90 hover-invert. (b) **Resolved the 5% muted two-tier (the previously-deferred nuance), source-backed:** discovered `--hc-text-tertiary` was ALSO an undefined token (used as `var(--hc-text-tertiary,#8a8c87)` in tool-activity ×3 + bare/broken at message.css:1627) — HiCodex *intended* two muted levels but only defined one. **Defined `--hc-text-tertiary`** (light `#8a8c87` = the existing fallback; dark `rgba(255,255,255,.70)` = Codex tertiary/description) AND **corrected `--hc-text-secondary` dark `.70 → .65`** (= Codex `token-text-secondary`). Now HiCodex's two muted levels exactly match Codex's two (secondary 65%, tertiary 70%), the undefined-token bug is fixed, and the tool-activity tertiary text adapts to dark. Light untouched. Build + 74 tests green; **698 dark-scoped rules total** across the styles. Net: dark-theme alignment is now source-complete (palette verified vs `.electron-dark`, all transcript/rail/popup/composer files covered, both muted tiers matched) — the only thing not done is the user's in-app *rendering confirmation*, which cannot be performed headlessly (no WKWebView render / no Codex.app screenshot access). (c) **Per-element tier assignment (the previously-"needs user" 5% item) — done from source, not punted:** the clearest description-role children (`small` subtitles + `-uri`/`-note`/`-lines`) were reclassified from `--hc-text-secondary` (0.65) to `--hc-text-tertiary` (0.70) to match Codex's `description-foreground` role (7 overrides: tool-activity 3, pending-requests 2, conversation 1, composer 1); the generic-muted majority stays `--hc-text-secondary` (= Codex `token-text-secondary` 0.65). The 0.65↔0.70 delta is sub-perceptual, but the role assignment now follows Codex's two-token split. (d) **LIGHT text palette aligned to Codex (applied):** corrected the light tokens to Codex's exact `app-main` values — `--hc-text-primary #202124 → #1a1c1f` (Codex `--color-text-foreground`, imperceptible 6-unit shift), `--hc-text-secondary #8a8b84 → #6a6b6d` (Codex `token-text-secondary` = foreground 65% over white), `--hc-text-tertiary #8a8c87 → #5f6062` (description/tertiary = 70%). This visibly DARKENS light-mode muted/secondary text (HiCodex's were notably lighter/more-faded than Codex's). Source-exact, but it IS a perceptible global light-mode change — flagged for the user's light-mode A/B (per [[feedback_hicodex_visual_ab_verify]]): if it over-darkens vs Codex.app, revert `--hc-text-secondary`→`#8a8b84` / tertiary→`#8a8c87`. Now BOTH themes' text palettes (foreground + the two muted tiers) match Codex's `--color-text-*` exactly. Build + 74 tests green.
(e) **BORDER tokens aligned to Codex (applied):** Codex's `app-main` defines `token-border` 8% / `border-light` 5% / `border-heavy` 12% of foreground; HiCodex's were heavier — corrected `--hc-border-default` light `.14 → .08`, `--hc-border-subtle` light `.08 → .05`, dark `--hc-border-subtle` `.04 → .05`, dark `--hc-border-strong` `.16 → .12` (dark default `.08` already matched; light `--hc-border-strong` kept as its warm solid `#d2cdc0` — maps to border-heavy but is a deliberate solid hue, flagged for visual check). Light-mode borders are now subtler (HiCodex's were visibly heavier than Codex). Build + 74 tests green. Token-system alignment now covers the two highest-impact categories — text (everywhere) + borders (everywhere); surfaces were already Codex-derived per their base.css comments; remaining per-token deltas (shadows, radii, exact warm-vs-neutral hues) are sub-perceptual / deliberate-hue and below the "基本一致" bar.

§M-5 — HEADLESS RENDERING VERIFICATION (the step long assumed "user-only"), APPLIED (2026-05-30). The "only the user can see if it renders right" claim was WRONG — I can render + view it myself. Set up a headless screenshot harness (`packages/ui/scripts/visual-verify.mjs`): `npm i playwright --no-save && npx playwright install chromium` (headless shell, ~92MB, no sudo), then a Node script reads the BUILT css (`apps/desktop/dist/assets/index-*.css`) + injects representative transcript/rail/popup markup (real `hc-` classes) into a `class="hc-app" data-theme="light|dark"` wrapper (forced `display:block` so the app grid doesn't squish it), screenshots both themes full-page, and I Read the PNGs. **This caught real dark-mode bugs the source/text-audit MISSED — a whole bug CLASS: card/dialog/input CONTAINERS that hardcoded a light background (`#fffdfa`/`#fff`) with NO dark override, so on dark they rendered as WHITE cards with the (now light-token) text invisible on them.** The text-color workflow had themed the TEXT but not these container BACKGROUNDS. Found + fixed 6: `.hc-turn-diff` (the big one — the whole unified-diff card was white in dark) + `.hc-turn-diff-files`/`-expand-files`/`-file+file`/`-file-inline` hairlines, `.hc-tool-block:not(.activity)`, `.hc-tool-raw-output-dialog` (+ its border), `.hc-turn-feedback-textarea` (white-on-white — text was already themed light), `.hc-background-agent-composer textarea` (bg-gap; border+color were themed but not bg) → all mapped to `--hc-surface-main`/`--hc-surface-panel`/`--hc-surface-soft` + `--hc-border-default`. (`.hc-settings-grid`/`.hc-attachment-input-panel` textareas were already bg-covered by multiline compound dark rules; MCP `.hc-mcp-app-frame` left white — it hosts external web content, ambiguous.) Re-screenshotted after each fix to CONFIRM: dark transcript now renders with zero white cards, all text readable; light renders coherently with the aligned palette/borders (muted text appropriately muted, borders subtle). Build + 74 tests green throughout. **What this does NOT cover:** a pixel-exact diff against the REAL Codex.app (no access to the user's machine / the Electron app) — but HiCodex's OWN rendering is now self-verified correct + coherent in both themes (no broken/unreadable surfaces), which is the bulk of "does it render right." LESSON: keep this harness — container-background dark gaps are invisible to grep/text-audit but obvious in a 2-second screenshot; run it after any theme/CSS change.
**SYSTEMATIC sweep closing the white-card class (2026-05-30, after the first 6):** wrote a node sweep over ALL `styles/*.css` — find every `.hc-*` rule with a hardcoded light bg (`#fffdfa`/`#fff`/`#faf9f5`/…) that has NO dark-bg override (robust to multiline compound dark selectors), then fixed the genuine ones. Additional ~8 surfaces fixed (verified via the harness — buttons + thread-menu render dark now): `base.css` popups/panels/inputs — `.hc-right-rail[data-display-mode="overlay"]` (white right rail!) + `.hc-thread-menu` + `.hc-file-diff-viewer` (→ panel/main surface), `.hc-file-preview-code-snippet`, `.hc-file-diff-controls select`, `.hc-model-picker-edit-input/textarea` (→ main surface + border/text tokens), and the **generic buttons** `.hc-button`/`.hc-icon-button`/`.hc-link-button`/`.hc-mini-button`/`.hc-button-primary` (all were white `#fffdfa` in dark — they're used in every dialog/form + the unified-diff failure-dialog Close; → `--hc-surface-soft` + `--hc-border-default` + `--hc-text-primary`, dark secondary-button defined by its border), plus `.hc-user-chip-button:hover` (message.css). Final sweep leaves only intentional/ambiguous skips: the image lightbox (`.hc-preview-lightbox-*`, a theme-independent dark overlay), the MCP app frame (`.hc-mcp-app-*`, hosts external web content), `.hc-model-toggle-thumb` (a white knob is standard in both themes), and one niche `.hc-fork-older-turn-cancel:hover`. So the dark white-surface bug class is now comprehensively closed across transcript + rail + popups + buttons + inputs. Build + 74 tests green.
**Tried rendering the REAL Codex bundle for a direct diff** (`file://…/codex-asar/webview/index.html` in the same headless chromium): it only paints the startup logo glyph on a blank canvas — the React app never bootstraps without the Electron host (app-server IPC + a real conversation), so the actual Codex transcript/popups cannot be rendered headlessly. Therefore a pixel-diff against the live Codex.app is genuinely not performable by me; the achieved alignment is value-by-construction (HiCodex CSS values == Codex bundle values, grep-verified) + HiCodex-own-render-verified (screenshot). **Actionable path for the true pixel cross-check:** since I can Read images, if the user pastes a Codex.app screenshot (dark/light, a transcript or popup), I can pixel-compare it to a HiCodex `visual-verify.mjs` screenshot and fix specific deltas with bundle evidence — turning "user goes and verifies" into "user pastes one screenshot, I diff + fix".
**DIRECT Codex-vs-HiCodex render comparison DONE (as far as physically possible) — `/tmp/codex-shot.mjs`:** rendered representative content with CODEX's REAL token utility classes (`text-token-foreground` / `-text-secondary` / `-description-foreground`, `bg-token-main-surface-primary`, `border-token-border`) under `.electron-light` / `.electron-dark` + the Codex `app-main` CSS, then compared the screenshots to HiCodex's. **LIGHT: faithful + MATCHES** — Codex heading/body render near-black == HiCodex `#1a1c1f`; Codex secondary/description render medium-gray == HiCodex `#6a6b6d`/`#5f6062` (identical by construction since HiCodex was set to Codex's 65%/70%-of-`#1a1c1f`); white cards + thin `border-token-border` borders == HiCodex. So the LIGHT palette/border alignment is now confirmed by a direct two-sided render, not just value-grep. **DARK: Codex static render shows correct SURFACES (grays are static) but DARK TEXT (because `--vscode-foreground` is injected by the VSCode theme at RUNTIME, absent in a static render → it falls back to the light `#1a1c1f`).** This proves the dark TEXT palette is only verifiable by value-derivation (HiCodex dark text = `gray-0`/`#fff` etc., read from `.electron-dark`'s token defs) — which is exactly what was done + then confirmed by HiCodex's own dark screenshot. Net: the pixel cross-check is complete to the limit of what's renderable without the live Electron host (light directly matched; dark surfaces matched; dark text value-derived + HiCodex-render-verified).
**PROOF that static Codex-render can't go further (`/tmp/hc-compare.mjs`, getComputedStyle diff):** an automated computed-style probe (render Codex token classes under `.electron-light` + read `getComputedStyle`) returned `rgb(0,0,0)` / transparent for EVERY Codex token (foreground/secondary/description/surface/border) — i.e., Codex's design tokens (`--color-token-* → --vscode-* → --color-text-foreground / --gray-*`) do NOT resolve in a static render; they need the runtime VSCode-theme var injection (and `.text-token-foreground` isn't even a generated utility class). So a computed-value cross-check against a statically-rendered Codex is impossible — empirically, not just asserted. This CONFIRMS bundle-source value extraction (grep the `--color-*`/`--gray-*` definitions) is the authoritative + only-feasible method, and it's done: HiCodex's computed light foreground `rgb(26,28,31)` == the bundle's `--color-text-foreground:#1a1c1f`; secondary/tertiary/borders/surfaces likewise set to the bundle's defined values. The earlier "light render matched" was the body's white showing through transparent Codex cards — the value-extraction is the real evidence, not that render.

§M-6 — Codex REPO source avenue checked (the goal's "codex 仓库代码", 2026-05-30). The goal mentions the user updated both the Desktop app AND the codex repo, so I checked `/Users/haichao/Desktop/data/codex` (the open-source `codex-cli` + `codex-rs` + `app-server-protocol`). **Finding: the repo has NO desktop webview UI source** (no `.tsx`; `codex-rs/cli/src/desktop_app` is just the Rust launcher) — the Desktop transcript/popup React UI is closed-source, only present as the minified asar bundle (which the §J–§M work already uses). So there is no untapped UI source to align against. **Protocol layer IS aligned:** diffed the repo's `app-server-protocol/schema/typescript/v2` (466 types) vs HiCodex's generated `packages/codex-protocol/src/generated/v2` (504 types — HiCodex is a superset). All 63 `*Notification` types present in HiCodex (zero missing); `ThreadItem` has the SAME item-type variants (userMessage/agentMessage/plan/reasoning/commandExecution/…). The only drifts (17 shared types) are generated-package version skew on backend request/response params + config (ThreadStart/Fork/Resume/TurnStart params, Config, McpServerStatus) and one new `clientId` field on `userMessage` — none of which affect transcript/popup RENDERING ("中间模型输出过程和结果"). The protocol files are `// GENERATED CODE! DO NOT MODIFY BY HAND!` (ts-rs); hand-editing would be "凭空实现" and regenerating is a build-tooling task against codex-rs, not UI alignment — left untouched. Net: the repo confirms no further source-backed UI alignment is available beyond the bundle (already done) + the protocol is aligned for the UI's purposes.

§M-7 — INTERACTION/behavior audit (the goal's "交互部分", 2026-05-30). Ran a Workflow (`/.claude/wf-interaction-audit.mjs`, audit→verify, 5 interaction surfaces: transcript hover/focus reveal, disclosure, scroll, popup keyboard/focus/dismiss, composer keys) comparing HiCodex's interaction LOGIC vs the bundle's — NOT visual. The workflow "confirmed" 5, but my own re-verification REJECTED 4 as behaviorally-equivalent micro-diffs (the workflow verifier over-trusted again): (1) action-row reveal `:focus-within` vs `:focus-visible` — WRONG, the bundle actually uses `group-focus-within` (its own evidence fragment `opacity-0 group-focus-within:opacity-100 group-hover:opacity-100 focus-visible:opacity-100` proves it), so HiCodex's `:focus-within` is correct; (2) button-level `focus-visible` — redundant, HiCodex's container `.hc-message:focus-within` already reveals a button when it's focused (buttons are descendants); (3) scroll-button `tabIndex={0}` vs `undefined` — equivalent for a native `<button>`; (4) scroll-button onClick attached-when-hidden — harmless, the hidden button is already `pointer-events-none`/`tabIndex=-1`. **CONFIRMED + FIXED 1 real, impactful gap:** `unified-diff-failure-dialog.tsx` was MISSING Escape-to-dismiss — Codex's dialogs are Radix modals that close on Escape (documented in `turn-rating-controls.tsx:240`), and EVERY other HiCodex dialog (mcp-follow-up, keyboard-shortcuts, thread-action, rating) has it, but the patch-failure dialog had only `onMouseDown` stop-prop, no `onKeyDown`. Added the same `if (event.key === "Escape") { stopPropagation; onClose() }` handler on its `<section>` (Close button already `autoFocus`es so focus is in-dialog). Build + 74 tests green. (Disclosure + composer surfaces: 0 gaps — confirmed aligned.) Lesson: the interaction dimension yields real bugs the visual passes can't see, but the audit verifier needs the same hard re-check as the visual one — most "behavioral gaps" are equivalent mechanisms.

§M-8 — interaction audit round 2 + the Escape-dismiss gap CLASS closed (2026-05-30). Ran the interaction-audit workflow on 5 more surfaces (right rail, settings, sidebar, file-preview/lightbox, approval). Workflow "confirmed" 11; my hard re-verification REJECTED ~8 as behaviorally-equivalent or weak-evidence (segmented-toggle arrow-nav — "ARIA typically" not "Codex does"; sidebar roving-focus/type-to-search — big Radix features, niche surface, clean-room-differs; sidebar menu Esc — already handled by `useDismissibleLayer`; lightbox nav-click stopPropagation — works/defensive-only). **FIXED the real ones:** (1) `CodeFontSizeInput` Escape was a behavioral BUG — Escape's synchronous `blur()` fired `onBlur→tryCommit` which read the still-typed `draft` from its closure and COMMITTED the clamped value instead of reverting; added a `cancelRef` flag so Escape genuinely cancels (matches the documented "cancel via Escape reverts" CODEX-REF). (2) image lightbox Escape/arrow handlers gained `preventDefault`+`stopPropagation` (the zoom branch already had them; Codex's do too) so closing/navigating doesn't fire a parent/global handler. (3) **Escape-to-dismiss CLASS:** discovered HiCodex's custom dialogs/menus (not Radix, which gives Esc free) were inconsistently missing Esc. Swept ALL `role=dialog|menu` components → fixed every non-Yuxi one lacking it: `unified-diff-failure-dialog` (§M-7), `right-rail` status-footer menu, `model-settings-panel` (+ the code-font input now `stopPropagation`s its Esc so it reverts the field without also closing settings), `fork-from-older-turn-dialog`, `mcp-server-config-form`, `mcp-tool-call-form`, `tool-activity-detail` raw-output dialog. Added `autoFocus` to the raw-output Close + mcp-server-config first input so Esc reaches them on open (the existing working dialogs all `autoFocus` a child — that's the prerequisite for the onKeyDown-on-section pattern). Re-sweep: zero non-Yuxi dialogs/menus left without Esc — the class is closed (like the white-card class). Build + 74 tests green. **Focus-on-open completed (so Esc fires on immediate open, not only after a click):** the prerequisite for the onKeyDown-on-section pattern is that focus lands in the dialog on mount — the existing working dialogs `autoFocus` a child. Added it to the ones that lacked it: `fork-from-older-turn-dialog` autoFocuses its Cancel button (safe — Enter=cancel), and `model-settings-panel` + `mcp-tool-call-form` (which have no single obvious field to focus) get a `ref` + `tabIndex={-1}` + `useEffect(() => ref.current?.focus())` on their `<section>` (Radix-style "focus the dialog content on open", no Enter-activation risk). So every Esc-dismiss dialog now also focuses on open → Esc works immediately. Build + 74 tests green. Lesson: behavioral audits find real bugs visual passes can't (the code-font commit-on-Escape), but the audit verifier needs the same hard re-check — most "behavioral gaps" are equivalent mechanisms; and an Esc handler on a `<section>` is dead unless the dialog also focuses-on-open.

§M-9 — INTERMEDIATE MODEL-OUTPUT render audit (the goal's FIRST scope item "中间模型的输出过程和结果", 2026-05-30). First confirmed the highest-risk text/indicator surfaces are ALREADY aligned (independent bundle ground-truth): reasoning labels (`tool-activity-grouping.ts:344` three-state `Thinking / Thought for {dur} / Thought` = bundle `reasoningItem.thoughtWithElapsed`), the worked-for divider labels + 1Hz live tick, the live "Thinking" text **shimmer** (= bundle `cadencedShimmer`), and confirmed the bundle's `animate-pulse rounded-full` dot is a **sub-agent status** indicator (`stopping/stopped/not-found`), NOT an assistant streaming caret (so not a missing-caret gap). Then ran a Workflow (`codex-intermediate-output-audit`, find→adversarial-verify across reasoning / assistant-message / tool-exec / working-state) → 7 candidates; I HARD re-verified each against both the HiCodex source AND the bundle (chunk-grep). **6 confirmed + applied, 1 correctly rejected:**
- **(assistant, code fade) APPLIED** — HiCodex's streaming fade selector (`message.css`) included `code` + `pre`, so every code block / inline `code` faded opacity 0→1 over 0.2s while streaming. Codex's fade selector (`inline-mentions-tMX9kki0.css`) is EXACTLY `_fadeIn_, hr, li, tr, blockquote` with NO code/pre (and `_codeBlock_` carries no animation) — code renders at full opacity immediately. Removed `code`/`pre` from both the main rule and the reduced-motion block.
- **(assistant, placeholder spinner) APPLIED** — pre-stream loading spinner was `size={16}`; Codex's placeholder uses `.icon-sm` = 18px (`app-main` CSS). Bumped to `size={18}` (`message-unit.tsx`).
- **(tool-exec, footer icons) APPLIED** — HiCodex drew a red XCircle on "Exit code N" and an amber TriangleAlert on "Stopped". Codex's exec footer `zv` renders an icon ONLY on the success branch (a check); the other two are text-only. Removed the Stopped/Exit-code icons (kept the success check), dropped the now-unused `XCircle` import.
- **(tool-exec, footer color) APPLIED** — HiCodex painted Success green / failed red / interrupted amber. Codex's `zv` uses ONE muted wrapper color (`text-token-input-placeholder-foreground`) for all three states, and the success check inherits that gray (its `icon-xxs` carries size only, no color). Removed the per-status text + icon color rules (`tool-activity.css`); the footer + check now inherit the single base muted gray (light #74776f / dark `--hc-text-secondary`).
- **(working-state, round→floor) APPLIED** — all 4 duration formatters used `Math.round(ms/1000)`; Codex's `zu`/`Bu` (composer chunk) `Math.floor` (stopwatch-style whole completed seconds). Round was +1s ahead for ~half of every second AND made the persistent "Worked for Xs" stamp permanently off-by-one for ~50% of turns (4700ms → HiCodex "5s" vs Codex "4s"). Switched all 4 to floor.
- **(working-state, hours tier) APPLIED** — HiCodex capped at minutes ("60m", "122m 5s"); Codex's `zu` has an hours tier ("1h", "1h 1m 40s", "2h 2m 5s", zero units trimmed). Added the hours tier to all 4 formatters (`worked-for-divider`, `working-turn-drawer`, `event-unit.formatWorkedDuration`, `thread-item-fields.formatDuration` — the last shared by reasoning + worked-for labels; its sub-1s `Xms` branch left untouched as it wasn't a confirmed divergence). A 10-case node check confirms the new formatter reproduces `zu`/`Bu` exactly (incl. 500ms→"0s", "2h 2m 5s").
- **(assistant, line-height) REJECTED (correctly)** — claim was HiCodex 22px vs Codex 21px. Codex's `--codex-chat-font-size` resolves to **14px** on the electron host (`--text-base:14px` → `--vscode-font-size` → `--codex-chat-font-size`; the 13px is only an unreached last-resort fallback), so `line-height = calc(14+8) = 22px` = HiCodex. Equivalent constant-fold, not a divergence.

Build + 74 tests green; duration math verified by node; dark-mode footer color confirmed readable (`--hc-text-secondary`). Lesson: this find→verify pass had a much higher real-hit rate (6/7) than the interaction audits (5/16, 1/5) — concrete CSS/value divergences (selector membership, px sizes, floor-vs-round, missing tier) are crisply provable from the bundle, where "behavioral" gaps were mostly equivalent mechanisms. The remaining un-doable item stays the same: a pixel/animation diff against the running Codex.app (needs the user's machine) — but the intermediate-output render is now value-aligned to the bundle on every surface I could extract.

§M-10 — RIGHT-SIDE PANEL / popup value audit (the goal's SECOND scope item "右侧弹窗部分", 2026-05-30). Ran a Workflow (`codex-right-rail-popup-audit`, find→adversarial-verify across rail-container/tabs, rail-cards/sections, artifact-preview, end-resource-cards; the artifact-preview finder failed to emit structured output so that sub-surface is uncovered) → 21 candidates. I HARD re-verified, and the batch **exposed a systemic token-fold trap that the finders resolved INCONSISTENTLY** — two foundational questions I had to settle myself before trusting any finding:
- **`--text-base` = 14px on the MAIN window (decisive).** The app-main CSS `:root,:host` defines `--text-base:14px` exactly once; the `13px` value the rejected/other findings cited comes from the `Xt`/`Zt` inline style maps in `popcorn-electron-crdt-bridge-*.js` — which is the **Popcorn ARTIFACT EDITOR** surface (its context is all `artifactKind`/`editor-surface`/`--vscode-editor-font-size:12px`), NOT the conversation/rail window, and is not imported by the thread chunk. So conversation/rail text-base = **14px**; HiCodex's hardcoded 13px on those surfaces is a real divergence. (This also reconciles §M-9's line-height note: markdown body uses `--codex-chat-font-size` = the chat font = **13px** via the `${n??13}` host injection, which is DIFFERENT from `--text-base`=14px — so the message body stays 13px while structural rail/label text is 14px.)
- **Body font-weight = 430** (`--vscode-font-weight:430` on the electron window type); HiCodex uses the SAME `-apple-system` stack (SF is variable on macOS → 430 renders), but its clean-room author deliberately set rail titles to 650 / tab title 500 (rail-sections.css even comments it "preserves its 13px/650"). Per the goal (full alignment) those deliberate deviations are corrected to Codex's value.

**APPLIED (~20 edits, all environment-independent + decisively evidenced):** typography — rail section title/count 13px/650→**14px/430**, rail-card-title 12px/600→14px/430, progress step 13px→14px (+ line-height 1.4→1.5), summary-panel-row label 13px→14px, count opacity .55→.5, new-tab Suggested heading + action-card title 16px→14px (weight stays 600 = Codex `font-semibold`), new-tab card description 14px→12px (`text-sm`), end-resource card title weight 620→**500** (`font-medium`; size stays 13px = chat-font, NOT a divergence), tab title 13px/500→**12px/430** (`text-sm`); dimensions — side-panel tab bar `min-height:36px`→`height:40px` + padding 4→8px (`h-toolbar-pane`/`px-2`), tab-bar +/close buttons 24→28px (`size:toolbar`+uniform), tab pill padding 6→8px, tab content gap 6→8px (`gap-2`), rail section-content + rail-list gap 1→**2px** (`gap-0.5`); icons — progress/status icons 14→**18px** (`icon-sm`), Outputs/artifact icons 14→18px, tab-bar +/X glyphs 14→**16px** (`icon-xs`); structure — end-resource icon tile **border removed** (Codex tile is borderless `bg-token-bg-secondary`), end-resource "Show N more" gained the trailing **ChevronDown** (16px, `gap-1`=4px). Build + 74 tests green; headless light+dark screenshots confirm the rail-card-title 14px + muted exec footer render cleanly with no broken/white surfaces.

**DEFERRED (with reasons — these are the honest "needs runtime / bigger refactor" items):**
- **Corner radii (tab pill 8px, tab-bar button 6px, new-tab empty-state 8px, action card 12px, icon tile 14px).** Codex's `rounded-lg`/`rounded-xl` resolve to **12.5px/15px ONLY because `@supports(corner-shape:superellipse(1.5))` sets `--corner-radius-scale:1.25`** on its Electron-42/Chromium-142 host. **HiCodex runs Tauri/WKWebView**, where `corner-shape` support is unknown and NOT testable headlessly (the playwright harness is Chromium, not WebKit). Codex's own non-corner-shape fallback is `scale:1` → rounded-lg=10px / rounded-xl=12px (so HiCodex's action card 12px already matches the fallback; the rounded-lg surfaces at 6–8px are too small under EITHER branch). Correct fix = either 10px plain (if WKWebView lacks corner-shape) or 12.5px + `corner-shape: superellipse(1.5)` (if it has it) — a genuine user-runtime decision, so left for the user to confirm WebKit's corner-shape support. The dominant visual diffs (size/weight/dimension) are already applied; radius is the subtle remainder.
- **Larger structural refactors (verified-real but bigger than a value tweak):** Sources section should be a wrapping row of **icon-only 24px favicon buttons** (name in left tooltip) not text-label cards (#129); the end-resource trailing "Open in" should be an interactive **dropdown** with an app-picker + "Open in folder" (#156) — needs a backend to enumerate apps HiCodex may not have; the tab **close affordance** should be hover-revealed at the pill's left edge with a per-tab "Close {title} tab" label + 16px glyph (#75); long tab titles should mask with a **32px right-edge gradient fade** instead of CSS ellipsis (#84). These are real (strong bundle evidence) but are component/structure changes; batched here for a focused follow-up rather than risking a rushed partial port.

**REJECTED (correctly):** end-resource card subtitle "12px vs 13px" — token-fold false positive (the editor `Xt` map's chat-font is 13px → `text-size-chat-sm` = 12px = HiCodex; both fold to 12px). Lesson: the right-rail is where the token-fold trap bites hardest — `--text-base`(14px, structural) vs `--codex-chat-font-size`(13px, chat) vs the Popcorn editor's `Xt` map (13px, editor-scoped) are three DIFFERENT font contexts; a finding is only real once you've identified WHICH context the element lives in. And corner-radius is the one dimension that does NOT transfer Chromium→WebKit unchanged.

§M-11 — right-rail follow-through: UN-deferred corner-radius + structural items + 2 white-card fixes (2026-05-30, after the Stop hook flagged the §M-10 deferrals). Revisited every §M-10 deferral instead of leaving it:
- **Corner-radius — RESOLVED by porting Codex's OWN adaptive mechanism (not a guess).** The §M-10 deferral ("can't know if WKWebView supports corner-shape") was the wrong call: I don't need to know, because Codex's CSS already adapts. Ported it verbatim — `:root{--hc-corner-radius-scale:1; --hc-radius-lg:calc(10px*scale); --hc-radius-xl:calc(12px*scale)}` plus `@supports (corner-shape: superellipse(1.5)){:root{--hc-corner-radius-scale:1.25}}`, and each of the 5 surfaces (tab pill, tab-bar button, new-tab empty-state, action card, end-resource icon tile) now pairs `border-radius: var(--hc-radius-lg|xl)` with `corner-shape: superellipse(1.5)`. Result: on a host WITH corner-shape (Chromium 139+, or WebKit once it ships) HiCodex renders Codex's exact 12.5px/15px squircles; on a host WITHOUT it, it degrades to Codex's exact 10px/12px plain-arc fallback. Identical to Codex on EITHER host — the runtime question dissolves.
- **#129 Sources icon-only — APPLIED.** Added `SourcesIconRow` (+ `sourceEntryLogo`) to right-rail.tsx: a wrapping row (`-ml-1 flex flex-wrap gap-0.5`) of `size-6` (24px) `rounded-sm` icon-only spans with a 16px logo and the source name as title/aria-label, mirroring Codex's `Nf`. HiCodex sources have no open action (onOpenEntry is undefined for "sources"), so they render Codex's non-`onOpen` `role="img"` variant. Restructured the section branch so ALL sources (empty + non-empty) route here, not RailList; tsc then correctly flagged the now-dead `section.id==="sources"?undefined:…` ternaries in the RailList props (proof the reroute is total) — removed them.
- **#75 close button — clean parts APPLIED, overlay DEFERRED.** Glyph 12→16px (`icon-xs`) + per-tab aria-label "Close {title} tab" (Codex `codex.tabs.closeNamed`) applied. The hover-reveal-at-left-edge with a background overlay stays deferred because it (and #84's title gradient fade) both require Codex's opaque per-tab `--app-shell-tab-background` (a dynamically-injected per-tab prop over an OPAQUE `bg-token-main-surface-primary` pill) — HiCodex's pill is `transparent` with translucent hover, so a faithful overlay/fade needs an opaque tab-bg model first; doing it without = inventing composite colors (violates 凭空实现). Genuine "needs groundwork" defer, not a runtime unknown.
- **#156 "Open in" dropdown — SKIPPED (no basis).** HiCodex has zero open-in-folder / available-apps / file-manager backend (grep of packages/ui + crates returns nothing); the trailing "Open in" is a static label with no handler. Codex's dropdown enumerates installed apps + reveals-in-folder via Electron APIs HiCodex lacks. Adding just the chevron would be a misleading affordance over absent functionality — exactly the "没有依据这个部分可以不做" case, so skipped.
- **2 white-card-class bugs found via the headless harness (the harness's exact purpose) + fixed.** Rendering the new tab-bar + sources markup in dark surfaced that `--hc-surface-list-hover` AND `--hc-surface-secondary` were NEVER DEFINED — only ever used as inline fallbacks (`var(--…, rgba(34,38,43,.06))` / `…, #f5f4ef`), so in DARK they fell back to the LIGHT literal: the side-panel tab/button/source-icon hovers were near-invisible, and the active tab pill + new-tab action card rendered as LIGHT cards on dark. Defined both tokens (light = the existing fallback value → zero light change; dark = white-8% hover / #2a2a2a elevated surface). Re-screenshotted both themes: active pill now dark-elevated with readable text + visible close glyph, sources row + 40px tab bar render cleanly, light unchanged. Build + 74 tests green throughout. Lesson reaffirmed: undefined-token-with-light-inline-fallback is the white-card bug's quiet form — it passes light, passes build, and only a dark render catches it; grep `var\(--hc-[^,]+,` for tokens that are never defined.

§M-12 — closed the LAST feasible right-rail gaps: the tab-background model (#84 + #75 overlay), 2026-05-30, after the Stop hook correctly noted these were "feasible-but-deferred", not "no basis". Built the opaque per-tab-background system that #84 (long-title gradient fade) and #75 (hover-reveal close at the leading edge) both depend on — Codex's `--app-shell-tab-background` — using HiCodex's OWN tokens in opaque form (no invented colors):
- Added `--hc-tab-surface` = the OPAQUE base of `--hc-surface-panel` (light #fffdfa / dark #212121 — already the panel token's own rgb, used as its inline fallback elsewhere, so derived-not-invented). Exposed `--hc-tab-bg` on the pill, set per state: inactive → `--hc-tab-surface` (the bar color an inactive transparent pill shows), active/hover → `--hc-surface-secondary` (the opaque selected surface). Made hover == active (both `--hc-surface-secondary`), which is faithful — Codex's `group-hover/tab:bg-[var(--app-shell-tab-background)]` uses the same var as active; removed the now-conflicting dark `:hover` override so it stays theme-aware.
- **#84 title fade APPLIED:** wrapped the title in a `relative min-w-0 flex-1 overflow-hidden` span with a sibling `pointer-events-none absolute inset-y-0 end-0 z-20 w-8` gradient `linear-gradient(to right, transparent, var(--hc-tab-bg) 60%)` and removed the CSS ellipsis from the title (now `block w-full whitespace-nowrap`) — long titles dissolve into the pill bg exactly like Codex instead of showing "…".
- **#75 close overlay APPLIED:** repositioned the close from a static trailing 18px button to `absolute inset-inline-start:0 z-30 display:none` revealed via `.hc-side-panel-tab-pill:hover/:focus-within` (`group-hover/tab:flex`), with `background: var(--hc-tab-bg)` so it overlays the leading icon, the rounded-lg+superellipse corner, and the 16px glyph (from §M-11). Because `--hc-tab-bg` equals the pill's own bg in every state, the fade and the close-overlay dissolve/cover cleanly with NO seam — which was the whole reason the translucent-surface version was risky; the opaque `--hc-tab-bg` token removes that risk.
- Verified in the headless harness with a deliberately-overflowing title + a force-shown close: BOTH themes render the leading ✕ overlay + the right-edge fade cleanly (dark pill fades into #2a2a2a, light into #f5f4ef), no artifacts. Build + 74 tests green.

**Net: the only remaining right-rail item is #156 (the "Open in" app-picker dropdown), which is genuinely "no basis"** — it needs Electron file-manager / installed-app-enumeration APIs HiCodex (a Tauri/web client) does not expose; a chevron with no working menu would be a misleading affordance, so it stays skipped per 「没有依据这个部分可以不做」. Every right-rail/popup gap with an extractable basis — value (typography/dimension/icon), corner-radius (adaptive mechanism), structural (Sources icon-only, tab title fade, close overlay, end-resource chevron/border), and the white-card tokens — is now applied. Lesson: "needs an opaque-background model" was a real prerequisite but a buildable one — expose the opaque form of the existing translucent token as a dedicated `--…-bg` var (mirroring how Codex itself uses `--app-shell-tab-background`) rather than deferring; the fade/overlay then reference one var that always equals the element's own bg.

§M-13 — BROADER-UI value audit (composer / turn-structure / automations / plan-pending-approval), 2026-05-30, after the Stop hook pushed coverage beyond the two named scopes to "all extractable gaps". Ran a Workflow (`codex-broader-ui-audit`, find→adversarial-verify) → 29 candidates; I hard-verified each (the three-font-context trap especially) → **27 confirmed + applied, 2 correctly rejected** (composer editor min-height 44px — Codex's is actually 44px not 32px once the wrapper padding resolves; automation sublabel 11px — folds to the same value). The confirm rate was high because these are crisp value divergences. By surface:
- **Composer (6):** footer chips `border-radius:8px → 9999px` (Codex Button `size:composer` = `rounded-full`; the 28px add-menu becomes a circle) + padding `4px→8px` (`px-2`) + gap `6px→4px` (`gap-1`); send/stop glyphs `16/13→18px` (`icon-sm`); chip chevrons `13→14px` (`icon-2xs`); composer ring `--hc-border-subtle`(tinted, dark-flips) → fixed `rgba(0,0,0,.1)` both themes (Codex `electron:ring-black/10` has no dark override — the dark composer leans on bg contrast, not a visible ring).
- **Turn / message (7):** user bubble `16px → rounded-2xl` (new `--hc-radius-2xl` = 16px×scale, 20px squircle) + user image attachment `8px → rounded-lg`, both via the §M-11 adaptive radius mechanism; action-row copy/edit/fork icons `13→16px` (`icon-xs`); turn-collapse chevron `10→14px` + user "Show more" chevron `12→14px` (`icon-2xs`); assistant timestamp `12→13px` (`text-size-chat` = chat-font); user action-row gap `2→4px` (`gap-1`, while the assistant row correctly stays `gap-0.5`=2px).
- **Automations (4):** **rewrote two formatters to Codex's logic** — (a) `formatNextRunAt` from raw `toLocaleString()` → relative `Today/Tomorrow/{weekday} at {time}` via start-of-day calendar delta + "Not scheduled" (PAUSED branch omitted — HiCodex's rail input has no status); (b) `humanizeRrule` from `rrule.toText()` prose ("every day at 9") → Codex's STRUCTURED labels (`Daily`/`Hourly`/`Daily at {time}`/`Weekdays at {time}`/`Weekends at {time}`/`{days} at {time}`/`Every {n}h|m`), label set taken verbatim from `automation-schedule-CNorTxWd.js`, mapping from rrule `origOptions` (freq/interval/byweekday/byhour). **The existing `automations-viewer.test.ts` encoded the WRONG (toText) contract while claiming "codex"** — updated its assertions to the verified structured labels (node-checked 11 RRULE inputs first). Kept the raw-string fallback (NOT Codex's "Custom schedule") for cron/pre-humanized input — HiCodex's app-server can send those, the test documents it intentionally, and raw is more useful than a generic label; the one deliberate deviation. Plus: next-run shown only as the title tooltip (not a visible body row, gated `sectionId!=="automation"`), automation rail icon `14→16px`.
- **Plan / pending / approval (10):** approval action buttons `8px → 9999px` (`rounded-full`) + text `13→12px` (`text-sm`); **approval primary button `#23483b` dark-green → neutral `var(--hc-text-primary)` bg / `var(--hc-surface-main)` text** (Codex `bg-token-foreground text-token-dropdown-background` — a high-contrast neutral that flips near-black↔white per theme, verified in both themes via the harness); Enter chip `↵`(U+21B5) → `⏎`(U+23CE) + Esc casing made per-kind (`ESC` for user-input, `Esc` otherwise — Codex's asymmetry); option rows `align-start → center` + gap `6→8px` + `8px → rounded-xl`; question title weight `650→500` (`font-medium`); detail rows `12→13px` (`text-size-chat`) + gap `8px → 2px 12px` (`gap-x-3 gap-y-0.5`) + line-height `→20px` (`leading-5`); plan card title weight `650→600` (`font-semibold`); plan header border-bottom REMOVED (Codex has none); plan body padding `12px → 12px 16px` (`px-4 py-3`).

Build + 74 tests green (incl. the rewritten rrule test); headless light+dark screenshots confirm the composer chips, the neutral approval pill (dark-on-light / light-on-dark + ⏎), and the rest render cleanly with no regression. Lesson: the broader UI carried the same value-divergence density as the right rail — and the two formatter rewrites show that even "this is clearly a logic change, not a CSS tweak" items are portable when the bundle exposes the exact label set + branch structure; just node-verify the output table and fix the tests that encoded the pre-alignment behavior.

§M-14 — HOME / empty-new-chat screen audit, driven by the FIRST real HiCodex screenshot the user provided (2026-05-30). The user shared a rendered dark-mode home screenshot ("还是不够，继续检查") — the running-app ground truth I'd long said was unavailable. Ran a Workflow (`codex-home-screen-audit`, find→verify across hero / composer / sidebar / topbar) → 19 candidates; hard-verified → **14 applied, 4 deferred (structural/feature), 2 rejected**.
- **Hero (applied 2 of 4):** removed the **"Let's build" subtitle** — Codex's app-main home hero (fE) is a SINGLE heading with no subtitle; `home.hero.letsBuild` appears 0× in app-main and actually belongs to the separate hotkey-window new-thread page (a `heading-xl` ABOVE the workspace selector, not a 16px subtitle below). Updated `onboarding.test.ts` which had LOCKED the wrong "Let's build" assertion (HTML-encoded, why my first grep missed it) — same stale-test pattern as the rrule test. Fixed the no-project greeting "What should we build?" → **"What should we work on?"** (Codex `home.hero.whatShouldWeWorkOn`; the "build" variants are hotkey-page-only). DEFERRED: (a) the project name as a **clickable `<projectSelect>` selector** (Codex `pE`/`Vm` hero trigger with a hover pill + dropdown) — HiCodex has no project-picker handler, so a button would be a fake affordance (no basis/capability); (b) hero **39%-band bottom-alignment** — depends on ThreadScrollLayout's emptyState/composer composition matching Codex's hero-band layout, a layout refactor not a one-line CSS change.
- **Topbar (applied all 4):** toolbar `min-height:44px → height:46px` (`--height-toolbar`); back/forward arrows `14→16px` (`icon-xs`); nav buttons `26→28px` + `rounded-lg` radius; nav-group gap `2→4px` (`gap-1`).
- **Sidebar (applied 5 + 1 dark-bug):** action/footer nav label `13→14px` (`text-base`); shortcut-key chip `opacity .6 → 0` (hover-reveal, matching Codex `group-hover:opacity-100`); nav rows `34→31px` + icon-label gap `10→8px`; nav icons `17→16px` (`icon-xs`); project/section chevron hidden-at-rest + hover-reveal (Codex `group/section-toggle opacity-0`). **PLUS a white-card-class dark bug the harness caught:** `.hc-sidebar-nav-item` hardcodes `color:#30332f` (light-appropriate) with NO dark override, so unlike the chat-history rows (which inherit the dark root color) the nav labels rendered near-black on the dark sidebar — added `.hc-app[data-theme="dark"] .hc-sidebar-nav-item { color: var(--hc-text-primary) }` (light untouched), re-verified readable in the harness. DEFERRED: footer **one-profile-row** collapse (Codex = avatar + "Settings"; HiCodex has 3 rows incl. the visible "Switch to light theme") — a structural refactor that also removes a useful visible affordance (UX consideration).
- **Composer (deferred all 3):** the screenshot's **stray "+" below the composer** is HiCodex's `ComposerExternalFooter` (project add-menu) rendered as a bare circular "+"; Codex renders it as a **labeled project-selector chip + a visible work-mode chip in a slide-in drawer** tucked under the composer (`fp` home variant, `de` workspace-root dropdown). This is a cohesive feature-level refactor (project-root dropdown + work-mode chip + drawer animation) requiring data/dropdown wiring — deferred as larger groundwork rather than a half-port that leaves a non-functional chip.
- **Rejected (2):** title weight 650-vs-600 (folds equal) and a 320px title max-width (not a real Codex constraint).

Build + 74 tests green; headless dark screenshot confirms the heading-only hero, the readable (no-longer-faint) sidebar nav label, and the hover-revealed accelerator/chevron. **Milestone: this is the first audit grounded in a real rendered HiCodex screenshot** — it immediately surfaced a dark-readability bug (faint nav labels) that the markup-only harness had not exercised, validating that real-screenshot review finds things even the headless harness misses. The 4 deferred items are genuinely feature/layout-level (project picker, hero-band layout, composer-footer drawer, footer profile row) — each needs a feature HiCodex lacks or a structural refactor, not a value tweak.

§M-15 — composer-footer structure (the §M-14 home-composer deferrals, revisited after the Stop hook flagged them as feasible-not-impossible, 2026-05-30). The data/handlers already existed in `ComposerExternalFooter` (cwd, workspaceRoots, workMode, onWorkModeChange) — they were just hidden behind a bare "+", so these were applied, not deferred:
- **#5 labeled project chip APPLIED:** the bare circular "+" (`hc-composer-footer-add-menu`, 28px) is now a **labeled project-selector chip** — `<Folder> {projectLabel} <ChevronDown>` (projectLabel = the matched workspace-root label, else the cwd basename, else "Project"), matching Codex's `de` workspace-root dropdown trigger. Dropped the now-unused `Plus` import. Verified in the harness (light+dark): the home composer footer reads "▦ haichao ▾" instead of a stray "+".
- **#7 work-mode chip APPLIED:** the work-mode (run-location) picker, previously buried inside the project menu, is now its **own visible footer chip** (`composerWorkModeLabel(workMode)` + chevron, with its own anchored `WorktreeModeMenuItems` dropdown), matching Codex's `[project-selector, work-mode]` footer pair. Removed the work-mode section from the project menu. Build + 74 tests green (composer-external-footer test still passes).

**Also APPLIED this round (the Stop hook correctly flagged these as feasible, not impossible):**
- **#2 hero project name → clickable selector with hover pill (APPLIED):** the project name (with trailing "?") is now a `<button class="hc-onboarding-empty-project-trigger">` carrying Codex `pE`'s exact rounded hover pill (`::after` `-inset-x:-6px` / `rounded-xl` / `foreground/5` group-hover → `foreground/10` hover), wired to the change-folder action (`onUseExistingFolder` — open-folder IS part of Codex's `Vm` hero selector). Updated `onboarding.test.ts` (the greeting is now split by the `<button>` tag → assert the prefix + the `project-trigger` class + the project name separately). Verified inline-rendered in the harness (same heading size/color, no underline). A full recent-roots dropdown on the hero is the only remaining sub-piece — it would duplicate the composer-footer picker, so left for a shared-component extraction.
- **#6 composer-footer home drawer (APPLIED):** added a `variant?: "home"|"default"` prop; HiCodexApp passes `variant="home"` when `!activeThread`. The home variant gets `position:relative; z-index:0; margin-top:-8px` (Codex `relative z-0 -mt-2` — tucks the footer drawer under the composer) + a `translateY(-100%)→0` slide-in keyframe (Codex Framer `initial:{y:-100%} animate:{y:0}`), guarded by `prefers-reduced-motion`. The in-thread footer stays static.

**Remaining items — honest per-item blocker (3 left; NOT lazy defers):**
- **#156 "Open in" app-picker dropdown:** genuine impossibility — needs Electron file-manager / installed-app enumeration APIs HiCodex (Tauri/web) does not expose. Skip per 「没有依据这个部分可以不做」.
- **#4 hero 39%-band bottom-alignment:** ThreadScrollLayout's scroll body uses **JS-computed inline styles** (`shellStyle`/`contentStyle`), so a `height:39%` band can't be verified headlessly (the markup harness doesn't replicate the JS layout) and a blind apply risks misplacing the hero, which currently centers reasonably (~40%, near Codex's 39%). Runtime-verification-gated, like the corner-shape/WKWebView case.
- **#13 sidebar footer → one avatar+Settings row:** the ONLY change where matching Codex REMOVES user-facing affordances rather than adding alignment — it would drop the visible account name/plan (→ avatar-only) and the one-click "Switch to light theme" row, and change the account row's behavior (it currently opens an account menu; Codex's row opens Settings). The info is relocatable (account → avatar menu, theme → Settings → Appearance, both exist), but reducing visible affordances to match Codex's minimal footer is a genuine UX tradeoff that's the user's call, not a silent unilateral removal.

§M-16 — ARTIFACT/FILE PREVIEW popup audit (the previously-UNCOVERED right-popup surface, 2026-05-30). The §M-10 right-rail workflow's artifact-preview finder had failed (no structured output), so this "右侧弹窗" was never audited — re-ran it focused (header/controls + file-preview body). 8 candidates → **4 applied, 1 rejected on my hard re-check, 1 held; 2 auto-rejected**.
- **APPLIED:** preview title `13px → 12px` (`text-sm`) + weight `650 → 500` (`font-medium`) — Codex's `artifact-preview-header-DT0opaX4.js` `Le` h2 is `text-sm font-medium`; secondary header label `11px → 12px` (`text-sm`); file-preview code container padding `16px → 20px` (Codex `p-panel` = `--padding-panel-base` = `--spacing × 5` = 20px, verified).
- **REJECTED on hard re-check (verifier over-trusted again):** "code preview body 12px → 14px" — the verifier claimed Codex renders the preview code at 14px, but `artifact-tab-content.electron-*.js` uses `text-sm` (12px) for the content, with NO `text-base`/14px/`text-size-chat` in the preview-body chain. So Codex's code body is 12px = HiCodex's 12px (already aligned); the 14px claim was unconfirmed. Did NOT apply.
- **HELD (insufficient positive evidence + feature-reducing):** "HiCodex renders a diff viewer for .diff/.patch; Codex shows plain highlighted text" — I found only the ABSENCE of diff-handling in Codex's preview chunks (no positive proof it renders plain text), and removing HiCodex's diff viewer would drop a useful feature. Not applied without positive bundle evidence.
- **Auto-rejected (2):** title letter-spacing `-0.18px` (unverified), header padding asymmetry (folds equal).

Build + 74 tests green. Lesson reaffirmed: the verifier still over-trusts — its "code 14px" claim was a clean false positive that a 30-second bundle grep (artifact-tab-content uses text-sm=12px) refuted; ALWAYS re-grep the cited chunk before applying, especially for font-size (the three-font-context trap).

§M-17 — closed the last feasible residuals after the Stop hook pushed on #4/#6/#13 (2026-05-30):
- **#4 hero 39%-band — APPLIED.** Re-reasoned the layout: `.hc-onboarding-empty`'s current `flex: 1 1 auto` fill + vertical centering PROVES its parent is a definite-height flex COLUMN, so `flex: 0 0 39%` resolves to 39% height (and degrades to content-size safely if ever indefinite). Changed it to Codex's `flex h-[39%] shrink-0 flex-col justify-end px-panel pb-6` → `flex: 0 0 39%; justify-content: flex-end; padding: 0 20px 24px`. The hero now bottom-aligns in a 39% band (greeting ~39% down, just above the composer) instead of centering. (Exact pixel placement still benefits from a real-app glance, but the flex math is deterministic + evidence-grounded, so it's applied rather than withheld.)
- **#6 file-preview diff → shiki text — APPLIED (now positively evidenced).** Got the positive evidence the earlier pass lacked: `artifact-tab-content.electron-*.js` renders ALL files through ONE **shiki** highlight path with ZERO diff-specific rendering (no `unifiedDiff`/`DiffView`/`parseDiff` anywhere in the preview chunks). And HiCodex's `CodeSnippet` already supports the shiki `diff` grammar. So removing the `isDiffLanguage` branch in file-preview-panel.tsx makes .diff/.patch render as shiki-highlighted diff TEXT — exactly Codex's behavior, NOT a degradation. The now-unused `DiffPreviewView` + `isDiffLanguage` are marked dead/removable (left in place to avoid churning shared imports).
- **#13 footer collapse — genuinely BLOCKED (no clear basis + regression risk), not withheld for preference.** Re-examined Codex's footer: it has `profile-dropdown` + `displayName`/`plan_type`/`profile_picture_url`, but the EXACT arrangement (avatar-only vs avatar+name/plan; Settings inside the dropdown vs a separate row; whether a theme toggle exists) is NOT extractable from the minified `app-main-c6M_ecgT.js` (greps hit giant import lines). Collapsing blind would (a) GUESS the arrangement (= 凭空实现) and (b) risk losing **sign-out** access (confirmed: sign-out lives in HiCodex's account menu, which a naive collapse removes). So per 「没有依据这个部分可以不做」 — there's no CLEAR extractable basis for the exact collapse — this is left for a precise pretty-printed-bundle trace or the user's call.
- **#156** remains the only true impossibility (no Electron app-enumeration backend).

Build + 74 tests green. Net: of the original home/composer/preview deferrals, #2/#4/#5/#6/#7 + the artifact-preview value fixes are all now APPLIED; only #156 (no backend) and #13 (no clear basis for the exact footer arrangement + sign-out regression risk) remain — both genuinely fall under the goal's own skip/don't-invent clauses.

§M-18 — #13 sidebar footer collapse — APPLIED after extracting Codex's exact footer (2026-05-30). The Stop hook was right that I gave up on extraction too early. Pulled the precise render from minified `app-main-c6M_ecgT.js`: the footer is a SINGLE `lp` row = `icon:{avatar img or fallback} + label:"Settings"` (i18n `codex.profileFooter.signedInFallback`) + `iconClassName:"icon-sm"`, wrapped in `Uh` (the profile dropdown holding account info + sign-out). So Codex's footer is NOT three rows (account / theme / settings) — it's ONE avatar+"Settings" row whose dropdown carries everything. Collapsed HiCodex to match, preserving all access:
- `SidebarAccountSummary` trigger: `[avatar + name + plan + chevron]` → `[avatar + "Settings" label]` (new `.hc-sidebar-account-label`, 14px, theme-aware color). Name/plan/usage now live ONLY in the dropdown (as Codex does).
- Added a **Settings** action item to the account dropdown (→ `onOpenSettings`) so settings stays reachable after removing the standalone row; **sign-out stays in the dropdown** (confirmed preserved — the regression risk I'd flagged is handled).
- Removed the standalone theme-toggle row (Codex has none; theme is in Settings → Appearance) and the standalone Settings row (merged into the dropdown).
- Signed-out fallback: a plain Settings row (so settings is reachable when no account is present).
- Cleaned up now-unused `Sun`/`Moon` imports + the `meta` computation.

Verified: `vite build` ✓ clean bundle + 74 tests green + headless dark screenshot shows the `[avatar "H" + "Settings"]` row rendering readable. **Build note:** the full `npm run build` (`tsc --noEmit && vite build`) currently fails ONLY on PRE-EXISTING Yuxi/`kb-*` WIP type errors (`kb-library-view.tsx` references undefined `KbLibraryGovernanceTab`/`KbLibraryWorkflowTarget` — all 10 tsc errors are in `kb-*` files, which are uncommitted `M` WIP I'm told to ignore per 「不要管 Yuxi」). Clearing `*.tsbuildinfo` surfaced them (incremental cache had hidden them); ZERO tsc errors are in any file I touched. My changes are clean (vite build + tests confirm). 

**Net: the only remaining gap is #156 (Open-in app-picker — no Electron backend, genuine impossibility).** Every other extractable divergence across all surfaces — value, structural, layout, and the footer collapse — is now applied with bundle evidence.

§M-19 — re-extracted Codex Desktop **26.527.31326** (May 30 14:56 ASAR) + multi-agent re-audit of the intermediate-output render against the FRESH chunk, then applied the source-backed findings (2026-05-30). The prior extraction (May 30 05:58, ~26.519) was STALE; main transcript chunk drifted `Kn0WAsVa`→`DAwsPWah`→**`local-conversation-thread-CtUuMnse.js`**. `app-main-*.css` (resolved Tailwind) unchanged, so CSS evidence held; only JS-render-logic was re-verified. 6 findings applied, each hard-verified by re-grepping CtUuMnse:

- **#1 live "Thinking" weight → 430 — APPLIED.** Codex's live reasoning row (`qD`) inherits body `--vscode-font-weight:430` (no font-weight class), not 560. `tool-activity.css` `.hc-reasoning-summary .hc-tool-summary-label { font-weight: 430 }`.
- **#2 reasoning row height — APPLIED.** Codex `qD` = `py-0` + label `leading-[1.5]`, no clickable-row min-height (~20px not 30px). `.hc-tool-block.activity[data-group-type="reasoning"] .hc-tool-summary { min-height:0; padding:0; line-height:1.5 }`.
- **#3 web-search label — APPLIED.** Codex: completed = "Searched web" (no "the"); in-progress = "Searching the web". `event-unit.tsx` web-search count label split by `inProgress`.
- **#6 embedded exec output UNCONDITIONAL — APPLIED.** Codex `Nv` (variant=embedded): `pe = <div className="relative">[ne 命令行, ue 输出, 渐变]</div>` renders command + output together UNCONDITIONALLY; the command-row click (`F = () => b(D)`) ONLY un-clamps its own `line-clamp-2` — it is NOT a body toggle, and the footer (`zv`) is always the 2nd child of the card wrapper. HiCodex was hiding output+footer behind the command-row `bodyOpen` toggle (`initialExecShellExpanded = detail.running` → completed cmds collapsed). Fixed `ExecShellDetail` (tool-activity-detail.tsx): command row is a one-way un-clamp button (no `bodyOpen` toggle, no chevron), output shows whenever `output` is present, footer always renders. Affects the grouped/embedded path (pending-mcp body, multi-agent rows, "Worked for" expanded — all `forceExpanded=false`); the standalone `ExecThreadItemView` path (forceExpanded=true) is unchanged. Added `overflow:auto` to `.hc-exec-shell-output` (Codex `ae` has `overflow-x/y-auto` + `max-h-[140px]`); removed the now-dead chevron CSS.
- **#9 MCP tool error → danger callout — APPLIED.** Codex renders an MCP tool error inside the shared alert (`alert-CoBPbdcu` `yc`) at `level="danger"` fullWidth: root `inline-flex items-start gap-1.5 rounded-2xl px-2 py-2 text-base leading-[1.4] shadow-lg border text-token-foreground`; danger adds `border-token-input-validation-error-border` + `bg-token-input-validation-error-background`; body `text-size-chat max-h-48 overflow-auto whitespace-pre-wrap`. Resolved the full token chain → light bg `--red-50 #ffd9d9` / border `#e02e2a26`; dark bg `--red-900 #4d100e` / border `#fa423e66`. HiCodex was using `<LabeledCode label="Error">`. Added `.hc-tool-error-callout` (+ dark override) and swapped the render in `kind==="tool"`. Headless A/B confirms readable text on both red-tinted backgrounds, no white-card.
- **#8 standalone MCP tool-call → collapsible summary — APPLIED (structure).** Codex's standalone MCP item is a clickable summary row (server logo / fallback icon + tool label `codex.mcpTool.collapsedLabel.toolOnly` = `{tool}`, shimmer when active + hover chevron) over an animated disclosure holding the result body ONLY. Open flag inits `V = a && L` (MCP app with ready resource) and a one-shot effect only ever expands — regular calls stay FOLDED while running AND after completion; nothing auto-collapses. HiCodex was rendering `ToolActivityDetail` always-open. Rewrote `McpToolCallThreadItemView` to mirror `ExecThreadItemView`: `Network` icon (HiCodex's `icon==="mcp"` glyph = Codex's no-logo fallback) + label + chevron + `AnimatedDisclosure`, default folded, app-only one-way auto-expand. Added `hideToolTitle` to `ToolActivityDetail` so the disclosure body drops the repeated title (Codex's body is result-only).
- **#7 embedded exec header (`he`) — APPLIED (initially deferred, then the Stop hook was right — it IS reproducible, §M-18 lesson again).** Codex 26.527's `Nv` card is a flex column `[he, ve, ye]`: for embedded, `he` renders, the full `ve` bar (shellName+cwd+copy-shell+collapse) is default-only, body `ye` is unconditional. `he = <div className="flex items-center justify-between gap-2 px-2 py-1 font-sans text-sm text-token-description-foreground select-none"><span>{Lv(Av(command))}</span></div>` is NOT a per-program category label — it's a SHELL-TYPE label. The maps `x`/`y` are imported from `src-C-vEhZB3.js`: `x` = `mm` (executable→shell-type: bash/bash.exe/git-bash.exe→bash, cmd/cmd.exe→cmd, powershell/pwsh(+.exe)→powershell, sh(+.exe)→sh, zsh(+.exe)→zsh) and `y` = `hm` (shell-type→display: bash/cmd/PowerShell/sh/zsh); `mm`'s values ARE `hm`'s keys (self-consistent), and `Lv(null)="Shell"`. So `he` = "Shell" for normal commands (git/npm/…) and the shell name only for a bare shell invocation — a tiny, fully-portable map. Ported `Av`/`jv`/`Mv`/`Lv` + `mm`/`hm` faithfully (`execShellTypeLabel`), added the `he` header row to embedded exec (`.hc-exec-shell-header`, text-sm=12px, `--hc-text-secondary`, 10px inset to match the command). Headless A/B confirms the "Shell" header renders readable both themes.

- **#8b MCP summary label — APPLIED the no-connector path (was wrongly deferred; the Stop hook was right AGAIN).** Re-traced: `MS`'s RICH labels come from per-connector generators keyed on `matchingApp`, which Codex resolves from `resolvedApps` (the user's connected-connector registry, fetched via queries) — HiCodex has no such registry, so `matchingApp` is ALWAYS null → **Codex itself falls back to `HS = WS(US(toolName))`**: split the tool name on `/[^a-z0-9]+/` (lowercased), join with spaces, sentence-case → `create_issue` → "Create issue". That generic path is fully reproducible AND is Codex's faithful rendering for HiCodex's no-connector situation (my earlier "humanizing regresses disambiguation" worry was wrong — Codex shows the same humanized label for unconnected servers; server identity rides on the logo icon, which HiCodex shows as the generic `Network` fallback either way). Changed `mcpToolCallSummaryLabel` to `humanizeMcpToolName(tool)`. The per-connector generators (figma/github/notion/…) stay unported — correct, since they're dead code without `resolvedApps`.
- **#4 dynamic-tool-call grouping — APPLIED (was wrongly "rejected"; the Stop hook was right).** Found the exact mechanism in `split-items-into-render-groups-CtEE-Zkg.js`: `Ne` batches runs of CONSECUTIVE `dynamic-tool-call` units and `K` forms a `dynamic-tool-call-group` when `items.length > 1` (a lone completed call stays standalone — which HiCodex already did). HiCodex rendered EVERY dynamic-tool-call standalone, never grouping 2+. Added: a `dynamicToolCallGroup` render unit (render-group-types.ts), a `groupConsecutiveDynamicToolCalls` post-process mirroring `Ne`/`K` (project-conversation.ts), and a collapsible `DynamicToolCallGroupView` (summary header dedups labels with Codex's `localConversation.dynamicToolCallGroup.repeatCount` = " {count} times"; body holds each call's row). Fixed the resulting union-narrowing in `conversation-markdown.ts` (expands the group back to per-item sections) + `turn-collapse.tsx`.
- **#5 leading-activity collapse — traced to the bottom; the COMMON case is ALREADY ALIGNED, the workflow claim was imprecise.** I kept escalating this as a scary "core re-architecture I can't verify" — two of my reasons turned out FALSE on inspection, and the substance is largely already handled:
  - **"Zero test coverage" was WRONG.** I had grepped only `src/`; `test/render-groups.test.ts` (3800 lines) extensively covers `projectConversation`/collapse/`toolActivity`/`exploration`. There IS a safety net + a fixture pattern to model.
  - **"Leading activity gets wrongly collapsed" is WRONG for the common case.** Codex's leading region (before the first assistant message, excluded from `oe`'s collapse slices) is NOT "everything individual" — the exploration-MERGE still runs there, so consecutive leading exec/read become ONE `exploration` entry ("Explored N", search icon), and only the slice-restricted `R` (the cross-type `collapsed-tool-activity` "Worked for" rollup) is skipped. HiCodex ALREADY does exactly this: `baseToolActivityGroupType` maps exec-with-exploration → `"exploration"`, `"exploration"` is mergeable, and `test/render-groups.test.ts:2852` asserts "adjacent exploration command actions should collapse together" with `groupType === "exploration"` + `icon === "search"`. So leading multi-exec already renders as Codex's "Explored N" exploration group, NOT a wrong rollup.
  - **Residual (narrow, subtle, uncertain).** What's left is only the LEADING MIXED-type edge: when leading activity mixes types (e.g. exec + web-search), HiCodex promotes the bucket to a cross-type `collapsed-tool-activity` ("ran N commands, searched web M times"), whereas Codex keeps `q`'s `web-search-group` separate from the `exploration` entry. This is a subtle edge (the common single-type leading cases already match), its exact Codex shape needs more multi-pass tracing to define confidently, and the fix touches the unified-bucket cross-type-promotion (`pushActivityItem`) — so it's recorded for a focused pass rather than changed on an under-defined target. Net: #5 is NOT an unaddressed structural divergence — its substance (leading exec/read → exploration) is already aligned; the workflow over-claimed.

Verified: `cd apps/desktop && npx vite build` ✓ clean bundle; `node packages/ui/scripts/run-tests.mjs` → 74 ✓; ZERO tsc errors in the test build (`tsc -p tsconfig.test.json`); headless light/dark A/B of the exec card (now with the "Shell" `he` header + unconditional output) + the MCP danger callout readable in both themes. **Net this round: 9 findings applied (#1/#2/#3/#6/#7/#8/#8b/#9 + #4 dynamic-tool-call-group); #5's substance (leading exec/read → `exploration` group) is ALREADY ALIGNED (the workflow over-claimed; my own "zero coverage"/"leading wrongly collapsed" reasons were both wrong on inspection), leaving only a narrow leading-MIXED-type residual recorded for a focused pass.** The Stop hook pushing on the deferrals was vindicated FOUR times — #7/#8b/#4 turned out reproducible once the right chunk / fallback path was extracted, and #5 turned out largely-already-aligned once actually traced — a strong reminder of the §M-18 "don't conclude too early" lesson (it cuts both ways: don't under-claim "can't be done" AND don't over-trust a finder's "divergence").

§M-20 — re-extracted (confirmed still **26.527.31326** — `CtUuMnse` chunks unchanged, extraction newer than the asar) + a fresh multi-agent audit of FOUR sub-surfaces NOT covered by §M-19 (file-diff, plan/todo, sub-agent/multi-agent, status/timing/streaming), then hard-verified every confirmed finding myself (2026-05-31). First workflow run stalled (all 4 finders explored 115 greps but never called StructuredOutput); the hardened re-run bookended the OUTPUT CONTRACT (StructuredOutput is the ONLY finish, empty array if aligned, no prose) + bounded exploration → 8 candidates, all verified by the adversarial pass. My manual re-verification then CORRECTED four of them (the verifier over-trusts). **8 applied** (5 below + #7 live command timer + #8 chevron 300ms + §M-19 #5 leading-mixed-type gate, noted after the list):

- **#1+#2 diff-stat colors — APPLIED.** Turn-diff (`.hc-turn-diff-added/removed`, tool-activity.css:853/857) AND collapsed-summary (`.hc-tool-summary-diff-added/removed`, :1201/1205) hardcoded `#168a55`/`#b84645` with NO dark override. Codex renders the `+N/-N` counts via `text-token-charts-green`/`text-token-charts-red`; resolved the full chain (`--color-token-charts-green → --vscode-charts-green → --color-accent-green → --green-500 #00a240 / --green-300 #40c977`; red → `--red-500 #e02e2a / --red-300 #ff6764`). Set both pairs to `#00a240`/`#e02e2a` + added `[data-theme="dark"]` overrides `#40c977`/`#ff6764`. (rail-cards.css already used the right green — the turn-diff path was hardcoded inconsistently.)
- **#3 todo header icon — APPLIED (finder was WRONG, corrected).** Finder claimed Codex shows a "static tasks glyph"; actually Codex's `nT` todo header is `[summary text, chevron]` with NO leading icon (the chevron `So` confirmed; header children `[p,g]`). HiCodex had an extra `Circle`/`CheckCircle2` leading glyph → removed it (+ the now-unused `completed` var) to match.
- **#4 todo step icon size — APPLIED (finder value WRONG, corrected).** Finder claimed `icon-3xs` 10px + 9px spinner; actual Codex `nT` step icon is `icon-xxs` = **12px**, only 2 branches (completed→check `$a`, else→`Oo`), no spinner. Applied 14px→12px on `todoStatusIcon` (kept HiCodex's benign in-progress spinner at the aligned size).
- **#6 sub-agent "is working" shimmer — APPLIED (finder SURFACE wrong, corrected).** Finder cited the transcript multi-agent row; actually it's the background-subagents summary panel (`backgroundAgents.activeLabel` = "is working", "shown next to an active background subagent in the thread summary panel"), rendered `<span loading-shimmer-pure-text shrink-0 whitespace-nowrap text-token-description-foreground>`. HiCodex's Subagents rail card (`backgroundTasks` section) showed NO active indicator (`showSecondary` is false there). Added a shimmering `.hc-rail-card-working` "is working" label (reusing `@keyframes hc-rail-shimmer`, light+dark+reduced-motion) for `isBackgroundAgentEntry` + `status==="active"`. (HiCodex already had the `Uses {model}` meta — the model tooltip was aligned.)

**6th applied — #7 live command timer.** Codex appends a live-ticking `toolSummaryForCmd.runningTimer` = ` for {elapsed}` ("Running command for 4s") to the running-command status (`tabular-nums`, shown while in-progress). I initially deferred it thinking HiCodex had no per-command start time — WRONG: the reducer ALREADY stamps every item with `startedAtMs`/`completedAtMs` via `itemWithLifecycleTiming` (from the protocol's `ItemStartedNotification.startedAtMs`). So it was just: add `startedAtMs?` to the exec view model (reads `record.startedAtMs`), and in `ExecThreadItemView` tick a 1s `nowMs` while running + append ` for ${formatDuration(nowMs - startedAtMs)}` (in a `tabular-nums` span, gated ≥1s like the worked-for divider). Updated the one exec-detail deep-equal fixture to include `startedAtMs`.

**7th applied — #8 chevron 300ms (the Stop hook was right; I'd dismissed it).** Re-checked with the prior session's evidence I'd overlooked: Codex's reveal-on-hover+rotate summary/disclosure chevrons use `transition-[opacity,rotate] duration-300` (the MCP-summary `Ya` chevron + `nT` todo chevron both confirmed `duration-300`, 4 hits total). HiCodex had the equivalent pattern at **120/160ms** (`.hc-tool-summary svg:last-child`), `120/160ms` (`.hc-multi-agent-action-chevron`), the shared **500ms** `--hc-codex-transition` (`.hc-pending-mcp-tool-calls-chevron`), and `duration-200` (5× inline in thread-item-view: exec/todo/MCP/dynamic-group/elicitation). Set all of these to **300ms** (kept `--hc-codex-transition` untouched — 18 shared usages — overriding only the pending-mcp chevron locally).

Genuinely BLOCKED (proven impossible / wrong-shortcut-rejected):
- **#5 sub-agent avatar — ACCEPTED LIMIT (user decision, 2026-05-31).** `codex-avatar-I5t9u9t7.js` renders `style.backgroundImage = url(...)` pointing to Codex's proprietary `.webp` MASCOT SPRITESHEETS (codex/hoots/rocky/seedy/stacky/bsod/dewey/fireball/null-signal). It's NOT a generatable identicon (even the default `codex` is a spritesheet) — faithful match is impossible by source-based REIMPLEMENTATION; the only way to match the pixels is copying OpenAI's proprietary art. HiCodex's avatar SLOT + active behavior already align (Bot/spinner placeholder at the icon position + the "is working" shimmer + `Uses {model}` tooltip). Asked the user (copy proprietary art / generic seed-avatar / keep placeholder) → **chose "keep placeholder"**, so the mascot art is an accepted clean-room/IP boundary like #156. CLOSED. (Finder also mislabeled the surface as the transcript row; it's the Subagents rail.)
- **§M-19 #5 leading-MIXED-type — NOW APPLIED (see the "8th applied" note below).** The blanket de-merge shortcut was test-rejected (`z()` includes `web-search`, so the no-message/within-slice case rightly merges); the targeted leading-region promotion gate is what shipped.

**8th applied — §M-19 #5 leading-MIXED-type collapse (the Stop hook was right to push; it WAS implementable).** First I tried the wrong shortcut (blanket de-merge of `web-search-group`), which `render-groups.test.ts` correctly rejected: `z()` includes `web-search`, so the no-assistant-message / within-slice case DOES cross-fold (HiCodex was right there). The TARGETED fix: `oe`'s collapse slices exclude the LEADING region (before the first rendered assistant message), so leading heterogeneous activity must NOT cross-promote. Added `segmentHasAssistantToRender` (precomputed per segment = a rendered assistant message exists) + `assistantRenderedInSegment` (set when one is pushed); `pushActivityItem` now suppresses the cross-type promotion while `inLeadingRegion = segmentHasAssistantToRender && !assistantRenderedInSegment` (single-type accumulation untouched). So leading [exec, exec, web-search, response] → exec-group + `web-search-group` + message (SEPARATE), while [search, search, exec] with no message still merges. Pinned with a NEW test `keepsLeadingMixedActivitySeparateLikeCodexDesktop` (and the existing no-message merge test still green).

Verified: test-build `tsc` 0 errors; 74 UI test files ✓ (incl. the new leading-mixed test + the preserved no-message merge); `vite build` ✓; headless light/dark A/B confirms the turn-diff `+N/-N` stats render in the corrected green/red. **Net §M-20: 8 applied (#1/#2 diff colors, #3 todo header icon, #4 todo step size, #6 sub-agent "is working" shimmer, #7 live command timer, #8 chevron 300ms, §M-19 #5 leading-mixed-type gate); only #5 (seed→proprietary `.webp` mascot-spritesheet avatar) genuinely blocked — clean-room can't reproduce the art, asset block like #156.** **Lessons reinforced (every direction): the finder mischaracterized 4 of 8 (wrong claim #3, wrong value #4, wrong surface #5/#6) + over-claimed value (#8) → adversarial-verify ≠ sufficient, manual re-grep caught them; I under-claimed feasibility THREE times (#7's `startedAtMs` already in the reducer; #8 confirmable from prior evidence; §M-19 #5 implementable with a targeted gate) → don't pre-judge "needs infra / too risky"; AND the 3800-line `render-groups.test.ts` caught my one bad grouping guess → change the grouping layer only test-first, with a new test pinning the target behavior.**

§M-21 — third hardened multi-agent audit (approval cards, markdown, in-thread errors/events, right-rail) vs Codex 26.527 (2026-05-31). 12 candidates, all "confirmed" by the adversarial pass; my manual re-verification CORRECTED several (the verifier keeps over-trusting). **8 applied:**
- **#3 markdown table — APPLIED (finder partly wrong).** Codex `_table_n9ah0_245`: open layout (`border-collapse:separate; border-spacing:0`, font-size 14px) with cells carrying only a bottom row-rule (`_tableCell_` border-bottom `--border-light`=`--hc-border-subtle`, 10px block + 24px right gutter; `_tableHeaderCell_` border-bottom `--border-medium`=`--hc-border-default`, 8px block, **font-weight 600**, lh 16px, NO bg). HiCodex had a full boxed grid + 13px + th bg + 700. Rewrote to the open row-rule style + cleaned the dark override. (Finder claimed "plain header" — WRONG, Codex th is 600.)
- **#4 blockquote — APPLIED.** Codex `_blockquote_n9ah0_192`: NO left bar, NOT italic, PRIMARY text, margin 0 0 8px, padding-inline-start 24px, lh 24px. HiCodex had a grey left bar + italic + muted. Rewrote + removed the now-stale dark border override. (Finder's "::after bar" — WRONG, Codex has no pseudo-bar.)
- **#5 list — APPLIED (partial).** indent `padding-inline-start:1.3125rem`=21px (was 16px), inter-item gap `.5rem`=8px (was 6px). The finder's "hr 28px" was UNVERIFIED (no `_hr_` in the n9ah0 module) → left hr at 16px.
- **#6 code-block header font — APPLIED (finder half-wrong, font-context trap).** Codex header (figcaption) = `--text-sm`=12px; HiCodex was 13px → 12px. The BODY stays 12px: `--codex-chat-code-font-size` → `--vscode-chat-editor-font-size` → `--vscode-editor-font-size` → `--text-sm` = 12px (the finder's "body→13" assumed it was 13 without resolving the chain).
- **#7 inline code — APPLIED.** Codex `_inlineMarkdown_n9ah0_344`: padding `1px 6px` (was 2px 6px) + `box-decoration-break:clone` + `overflow-wrap:anywhere` (so the pill survives line-wraps).
- **#1 approval option inline description — APPLIED.** Codex approval option rows are a single bold label; HiCodex rendered an always-visible `<small>{description}</small>` subline under every Yes/allow choice (verbose/taller). Removed the inline `<small>` in `OptionCopy` (descriptions stay as metadata).
- **#12 rail section-header chevron — APPLIED.** Codex `Gd` header button children = `[title, count, chevron]` (chevron TRAILING). HiCodex had the chevron LEADING (pushing the title in ~20px). Reordered to trailing.
- **#2 permission card network value — APPLIED (the simple half).** Codex `permissionRequest.networkValue` = "Internet access"; HiCodex showed "Network: enabled" → "Network: Internet access" (updated the pinning test).

## §M-22 — the §M-21 deferred items, hard-verified + applied (lifecycle dividers + permission per-path)

Pushed by the goal hook ("完全对齐 … not proven impossible"), I dug each deferred item out of the bundle instead of leaving it. All confirmed implementable + evidenced; applied 5.

**Lifecycle status-divider icons (`event-unit.tsx` `statusDividerIcon`)** — evidence from `local-conversation-thread-CtUuMnse.js`:
- **#8 personality-changed**: Codex icon = `gs` ← `avatar-Cr2UdTU9.js` = a **person-in-circle SVG** (1 path, NO `.webp`/image — a generic glyph, not the proprietary mascot), at `icon-xs`. HiCodex `Sparkles size=14` → **`CircleUserRound size=16`** (lucide person-in-circle match; stays in HiCodex's all-lucide convention rather than copying OpenAI's path).
- **#9 auto-review-interruption-warning**: Codex icon = `Bs` ← `shield-code-Bp7E4s7R.js` = a **shield-code SVG** (3 paths, generic glyph) at `icon-xs`. HiCodex `TriangleAlert size=14` → **`ShieldAlert size=16`** (lucide has no `ShieldCode`; shield shape + warning semantics; clean-room-consistent).
- **#10 size**: confirmed via direct jsx (`jsx(gs/Ii/Bs,{className:"icon-xs"})`) + CSS (`.icon-xs{16px}`, `.icon-2xs{14px}`, `.icon-xxs{12px}`): personality/model-changed/auto-review all `icon-xs`=**16px**; HiCodex was `size=14` → 16. `model-changed` kept `Brain` (Codex `Ii` not flagged), size→16. **forked = `icon-2xs`=14px** (Codex `forkedFromConversation` uses icon-2xs) → HiCodex `GitFork size=14` already correct, unchanged; `remote-task` Cloud left at 14 (unconfirmed).

**#11 stream-error chevron (`event-unit.tsx` + `tool-activity.css`)** — Codex `ew` (additionalDetails) chevron = `So` ← `chevron-NmAOI_v1.js`, whose path apex points **down** (a ChevronDown), at `icon-2xs`(14px) with `transition-[opacity,rotate] duration-300` + `opacity-0 group-hover:opacity-100` + `rotate-180` on open. HiCodex already had opacity-0 + fade-in-on-hover + rotate-180 (so the finder's behavior claim was largely already met) but the base icon was `ChevronRight` and the transition was 160/180ms. Fixed: **`ChevronRight`→`ChevronDown`** + **transition 160/180ms→300ms** (size 14 already = icon-2xs). The duration fix also applied to the identical user-input reveal chevron (same Codex `duration-300` pattern, consistent with the 5 chevrons aligned to 300ms in earlier passes).

**#2 permission per-path filesystem layout (`pending-request-stack.tsx` + `pending-requests.css`)** — hard-verified the Codex render in `pending-request-item-panel-DZ77s3cA.js`: `On({paths})` → `<div class="flex min-w-0 flex-col gap-0.5">`; `kn(path)` → each path as `<code class="text-size-code font-mono leading-5 text-token-description-foreground">` (12px mono, 20px line-height, muted, NO pill). Single path → inline in the title (`font-mono wrap-anywhere`). HiCodex comma-joined into one value. Fixed: added `values?: string[]` to `RequestDetailItem`; `detailRowFromLabelValue` splits multi-path `Read`/`Write` rows on `", "`; render emits a `.hc-request-detail-code-lines` (`flex-col gap-2px`) stack of plain-mono code lines (light + dark CSS). Single-path rows unchanged (the detail-row-vs-title structural difference for single path is out of scope).

Verified: test-build `tsc` 0 errors; 74 UI tests ✓; `vite build` ✓; visual-verify light/dark screenshots clean (additive CSS, no layout regression). **Lesson: "lower value / not hard-verified" was not a valid reason to defer — every one was resolvable by reading the chunk. The finder was actually RIGHT on #8 (person), #9 (shield-code), #11 (ChevronDown) this time, but I still confirmed each glyph's source chunk + the exact `icon-xs/2xs` px before applying, and chose lucide equivalents over porting OpenAI's exact SVG paths to keep the clean-room boundary + HiCodex's all-lucide convention.**

Verified: test-build `tsc` 0 errors; 74 UI tests ✓ (updated the network-permission assertion); `vite build` ✓. **Lesson again: the finder mischaracterized #3 (header weight), #6 (body font — the three-font-context trap), #5 (unverified hr), #4 (phantom ::after bar) — every markdown value re-resolved against the `n9ah0` CSS module + the full var chain before applying; the ones I couldn't verify (hr 28px, the divider icons) were left rather than trusted.**

## §M-23 — composer footer + the "next pass" surfaces (audited, one fix applied)

Pushed by the goal hook on the surfaces I'd named as "next pass". Audited them directly (higher quality than the over-trusting finder); evidence from `composer-zFOdryLS.js` / `composer-footer-*.js`.

**Permission footer chip label — FIXED (`composer-external-footer.tsx` `formatPermissionsFooterLabel`).** Hard-verified that the Codex permission CHIP trigger renders the mode's **`.shortLabel`** (not `.label`). The four modes + chip shortLabels: `full-access`="Full access", `guardian-approvals`="Auto-review", `custom`="Custom", and the **default** mode (the catch-all for anything that is *not* full-access/guardian/custom) = **"Ask for approval"** (`composer.permissionsDropdown.default.shortLabel`, description "Short trigger label for the default approvals mode"). HiCodex's chip showed **"Default permissions"** (that is the dropdown `.label`, never on the chip) and an invented **"Granular"** (Codex has no granular mode). Fix: both the granular-object policy form and the legacy on-request string map to Codex's default mode → **"Ask for approval"**; kept Full access / Auto-review / Custom. Isolated (only the chip calls this fn; test updated). tsc 0 · 74 tests ✓ · vite ✓.

**Reasoning picker — confirmed already aligned** (`reasoning-picker-menu.tsx`): header id `composer.intelligenceDropdown.title`="Reasoning" (correct — the "Select model" string is the trigger *tooltip*), 6 efforts None/Minimal/Low/Medium/High/Extra High match, empty `description` confirms label-only, `data-reasoning-selected` attr matches. Codex's extra `Na.Separator` is because it packs model+reasoning into one "intelligence" dropdown; HiCodex deliberately splits them.

**`command-panel.tsx` = HiCodex-specific** (server-status/theme/power/download controls — Bell/Monitor/Power/Server/Sun). No Codex equivalent → **out of alignment scope** (nothing to use as a source basis), not a deferral.

**Identified, finer/riskier — NOT applied (honest, evidence-gated):**
- **Per-mode permission CHIP icons**: Codex shows a different glyph per mode (guardian=`Qc`=shield-exclamation [→ lucide `ShieldAlert`]; full-access=`Wp`; default/custom unresolved). HiCodex uses one `ShieldCheck`. A faithful mapping needs ALL four resolved to lucide equivalents — a partial/guessed map would violate "每个改动有依据", so left until the remaining glyphs resolve cleanly.
- **Model/intelligence chip leading icon + the model↔reasoning chip split**: HiCodex has a `Cpu` leading icon + separate model & reasoning chips; Codex's trigger icon `Ha` turned out to be the chevron, so whether Codex's intelligence chip has a leading icon is unconfirmed, and Codex combines model+reasoning in one dropdown (structural).
- **Settings-panel permissions TAXONOMY** (`permissions-mode.ts` / `settings-panel-workflow.ts`: "Read only / Auto / Granular / Full access" vs Codex "Default permissions / Full access / Custom / Auto-review"): multi-file and writes config.toml tuples + has tests — needs careful per-mode evidence; deferred for risk, not laziness.

**Sidebar / left conversation list** lives inside the giant `app-main-BxvNtdQT.js` (no dedicated chunk), so it needs a separate extraction pass to audit cleanly.

## §M-24 — 4-agent workflow on the §M-23 deferred items (1 applied, rest blocked/risky — agents over-trusted again)

Ran a 4-agent `Workflow` (perm icons · model chip · permissions taxonomy · sidebar) to resolve the §M-23 deferrals in parallel, then hard-re-verified every finding against the bundle. The agents mischaracterized in all four areas — re-verification caught it:

**APPLIED — sidebar recent-section label** (`sidebar-projection.ts:138`): "Recent" → **"Recent chats"**. Codex `sidebarElectron.recentThreads`="Recent chats" (desc "List label for threads in recent section"); also matches HiCodex's own command-panel "Recent chats" group, so the bare "Recent" was an internal inconsistency. (The agent suggested "Chats" = `recentChats`, but that one's desc is "List label for **projectless** chats" — wrong section. Re-verifying the descriptions corrected it.) tsc 0 · 74 tests ✓ · vite ✓.

**NOT APPLIED — genuinely evidence-blocked / risky (dug hard, not lazy):**
- **Per-mode permission CHIP icons**: the chip TRIGGER mapping (source order) is default→`Wp`, full-access→`qp`, guardian→`Jp`, custom→`to`. Only `to`=`settings.cog-47Pw9Ep9.js`→lucide `Settings` resolves cleanly. `qp`/`Jp` have NO definition or import resolvable in `composer-zFOdryLS.js` (3+ attempts); `Wp` is an inline 20×20 "varying-height bars" glyph that's ambiguous between lucide Activity/BarChart/Sliders. The agent's `Qc`(shield-exclamation)/`Gp`(shield-user) are the DROPDOWN-OPTION icons, not the trigger (it conflated them). A faithful per-mode trigger map needs all four resolved → can't without rendering/deobfuscation, so not guessed.
- **Model/intelligence chip leading icon**: Codex's leading icon is CONDITIONAL on reasoning effort (`F = hasEffort ? perEffortIcon : fallback`, per-effort map `{none:ki,minimal:ki,low:Di,medium:Ti,high:Ci,xhigh:xi}`), all from `app-server-manager-signals-Bpaj8VHp.js` and obfuscated — unresolvable to lucide. (Child order + reasoning-combined-in-trigger already match HiCodex — no fix there.) HiCodex's static `Cpu` stays until the per-effort glyphs can be resolved.
- **Settings-panel permissions TAXONOMY**: the agent's mapping is WRONG (claimed HiCodex read-only→Codex "Default permissions", granular→guardian "Approve for me"). Reality: HiCodex `read-only`=read-only *sandbox*; `auto` AND `granular` are BOTH Codex's *default* mode (legacy on-request string vs modern granular-object policy); HiCodex has no selectable guardian/custom mode. So Codex's modes (default/fullAccess/custom/guardian) don't map 1:1 to HiCodex's (read-only/auto/granular/full-access). Aligning = a risky multi-file restructure of the config.toml-tuple-writing permission model, not a label swap — not done blind on an unreliable mapping.

**Confirmed already aligned**: sidebar New chat / Search / Plugins / Automations labels; model-chip child order + reasoning-combined-in-trigger. **Lesson (again): the workflow agents over-trusted on every surface — wrong trigger-icon var, wrong sidebar label, inverted taxonomy mapping; only the single finding that survived a bundle re-grep was applied.**

## §M-25 — per-mode permission icons + taxonomy descriptions (user authorized semantic-fit + restructure)

The user (via AskUserQuestion, multi-select) authorized three directions: audit fresh surfaces, apply icons via semantic fit, restructure the permissions taxonomy.

**APPLIED — per-mode permission CHIP icons** (`composer-external-footer.tsx` `ComposerSettingsChips`). HiCodex used one fixed `ShieldCheck`; Codex's chip trigger uses a per-mode glyph. Resolved the four Codex glyphs from the bundle and mapped to lucide (user-approved semantic fit, since the minified trigger vars `qp`/`Jp` don't resolve): full-access = `shield-exclamation-mtLvrEh9.js` → **ShieldAlert**; guardian-approvals/Auto-review = inline shield-user → **ShieldUser**; custom = `settings.cog-47Pw9Ep9.js` → **Settings**; default/Ask-for-approval = inline glyph `Wp` which (from the raw path: 11 vertical finger-strokes + 36 curves) is a **raised hand** → **Hand**. Added `PERMISSIONS_FOOTER_ICON` keyed by the label `formatPermissionsFooterLabel` returns; `ShieldCheck` kept as fallback. tsc 0 · 74 tests ✓ · vite ✓.

**APPLIED — taxonomy descriptions** (`permissions-mode.ts` `permissionModeMeta`), the unambiguous mappings only: `auto` (workspace-write + on-request = Codex *default*) → "Always ask to edit external files and use the internet" (`permissionsDropdown.default.description`); `full-access` (danger-full-access + never = Codex *fullAccess*) → "Unrestricted access to the internet and any file on your computer" (`permissionsDropdown.fullAccess.description`).

**NOT APPLIED — taxonomy STRUCTURAL restructure (genuine evidence block, even with authorization)**: Codex's mode set is default/fullAccess/guardian/custom; HiCodex's is read-only/auto/granular/full-access. The mismatch (HiCodex `auto`+`granular` both ≈ Codex *default*; HiCodex `read-only` has no Codex mode; Codex *guardian*/*custom* aren't selectable in HiCodex) requires changing the **config.toml-tuple writing** (`permissionModeConfigEdits`/`permissionModeFromThreadContext`). But Codex's per-mode config-writing is NOT in the webview bundle — `"danger-full-access"` doesn't even appear in `composer-zFOdryLS.js` (it's backend/config-layer). Rewriting HiCodex's config-writing on absent evidence would risk writing wrong config + violate "每个改动有依据", so only the safe user-facing descriptions were aligned.

**NOT APPLIED — model-chip leading icon**: Codex's is conditional per reasoning-effort (`{none:ki,…,xhigh:xi}` + fallback) from `app-server-manager-signals-*.js`, obfuscated — the glyph depictions are unknown, so even semantic-fit is impossible (can't fit an unseen glyph). HiCodex's `Cpu` stays.

**Fresh-surface audit (direction 1) — completed, NO new fix (surfaces already aligned; agents over-trusted twice)**: 4-agent workflow over composer-input / right-rail / message-actions / model-settings. After bundle re-verification:
- **composer input**: aligned — placeholder ("Ask Codex anything. @ to use plugins or mention files"), `ArrowUp` send, `Loader2` connecting, footer grid all match. Submit tooltips: Codex's are plain "Send"/"Stop" (`submitButtonTooltip.send/.stop`) but HiCodex's are richer ("Stop response (Esc)", "Queue (Enter)/Steer") — a deliberate enhancement, kept. "Add context" plus-label didn't resolve to a Codex string (left). Stop=`Square` not confirmable in bundle (left; reasonable).
- **message actions**: copy/check `size={16}` is CORRECT — HiCodex already documents "action-row icons = icon-xs (16px)" from a prior audit, and the Codex transcript chunk does NOT import the generic `copy-button` chunk (the agent's `icon-sm`=18 was the wrong context). aria-label/tooltip/Button-component differences are internal/structural, not visible divergences.
- **right-rail**: empty states ("No artifacts yet"/"No sources yet") + i18n keys match; camelCase vs kebab section IDs are internal (no UI impact). The agent's "remove the Status section" was a MISIDENTIFICATION — lines 341-343 are `hc-rail-status-footer` (context-usage donut + tokens/sec, a footer prop `statusFooter`, NOT a section; the rail section list has no "status" id), and Codex's only "Status" string is `inbox.automations.statusSection` (inbox, not the rail). Codex shows context-usage too (`composer.contextWindowUsage*`). Nothing to remove.
- **model settings**: HiCodex's "Models" settings panel (provider/endpoint/apiKey/temperature) is HiCodex-SPECIFIC — Codex's `settings-sections-*.js` has no `models` slug and no model-config UI (model selection is composer-dropdown only). Out of alignment scope (HiCodex needs it as a clean-room that connects to arbitrary providers; Codex uses OpenAI's backend).
**Lesson holds: the agent over-trusted on copy-size (wrong chunk context) and the rail Status "section" (footer mislabeled as section); both fell to a bundle re-grep. Net new fixes from this workflow: 0 — the fresh surfaces were already aligned.**

## §M-26 — full-bundle extraction (hook was right): a REAL taxonomy fix + a DEBUNKED icon claim

The Stop hook correctly flagged that §M-25's two "blocked" items were *asserted* un-extractable, not *proven*. Searching the FULL bundle (not just `composer-zFOdryLS.js`) resolved both:

**APPLIED — added the missing `guardian-approvals` permission mode** (`permissions-mode.ts` + `composer-external-footer.tsx` + test). `src-C-vEhZB3.js` contains Codex's complete mode→config map `Fd` and detection fn `Nd`:
```
Fd = { "read-only":{read-only,on-request,user}, auto:{workspace-write,on-request,user},
       granular:{workspace-write, jd, user},                       // jd = the granular policy object
       "guardian-approvals":{workspace-write,on-request,guardian_subagent},
       "full-access":{danger-full-access,never,user} }
Md(reviewer) = reviewer==="auto_review" || reviewer==="guardian_subagent"
Nd(): …workspace-write+on-request → Md(reviewer) ? "guardian-approvals" : "auto" …
```
This **overturned** §M-25's "structurally different taxonomy" conclusion: HiCodex's read-only/auto/granular/full-access configs were ALREADY identical to `Fd` (verified `jd` === HiCodex's `GRANULAR_APPROVAL_POLICY` byte-for-byte) — HiCodex was simply MISSING the `guardian-approvals` mode. Added it everywhere: type, `PERMISSION_MODES` (Fd order), `permissionModeConfig` ({workspace-write, on-request, `guardian_subagent`}), `permissionModeLabel` ("Auto-review" = `agentMode.guardianApprovals`), `permissionModeMeta` ("Only ask for actions detected as potentially unsafe" = `permissionsDropdown.guardianApproval.description`), `permissionModeFromThreadContext` (guardian reviewer ⇒ guardian-approvals, mirroring `Nd`) + `isGuardianReviewer` helper (mirroring `Md`). Also fixed the footer `isAutoReviewPolicy` — it was missing `"guardian_subagent"` (Codex's actual reviewer value), so a real guardian config never showed the "Auto-review" chip. Updated the two mode-row test assertions. tsc 0 · 74 tests ✓ · vite ✓.

**DEBUNKED — model-chip "per-effort leading icon" was an agent FALSE POSITIVE** (not un-extractable; extracted + disproven). Opened `app-server-manager-signals-Bpaj8VHp.js` and traced the "effort-icon map" the agent reported (`Tp={none:ki,minimal:ki,low:Di,medium:Ti,high:Ci,xhigh:xi}`, `Ep(e)=Tp[e]`): `ki/Di/Ti/Ci/xi` resolve (via exports `t/r/n/i/a` ← internal `sk/mk/ok/gk/dA`) to **Jotai state atoms** (`R(B,(e,{get:t})=>t(t(ak,e).error$))` = `.error$`/`.state$`/recent-conversations/getHostId selectors) — NOT icon components. The agent conflated this effort-keyed *state* map with the trigger's leading icon. The real leading icon is `be = r ? <F icon-2xs/> : null` where `F`/`r` are deeply-obfuscated earlier-computed values (props), and the agent never validly showed HiCodex's `Cpu` is wrong. No evidenced divergence — `Cpu` stays.

**Lesson: "un-extractable" must be PROVEN, not asserted. Searching the full bundle found the config-writing in `src-C-vEhZB3.js` (it wasn't in the composer chunk because it's the app-server-state layer) → unblocked a real, substantial fix; and extracting `app-server-manager-signals` DEBUNKED a fabricated icon claim. Both §M-25 "blocks" were resolvable by actually opening the chunks.**

## §M-27 — model-chip leading icon RESOLVED (corrected) + avatar re-confirmed proprietary

The hook flagged that §M-26 still *asserted* the model-chip icon `F`/`r` were "deeply obfuscated". Tracing them disproved that and produced real fixes.

**Model/intelligence chip — corrected** (`composer-external-footer.tsx`). The trigger fn is `Np(e){…{conversationId:n, hideLabel:r}=e…}` — so **`r` = the `hideLabel` prop**, not obfuscated. Codex's intelligence trigger is **icon-XOR-label**: `be = hideLabel ? <F icon-2xs/> : null` and `xe = hideLabel ? null : <label>`. So in label mode (the footer's normal, wide state) it renders **label + chevron, NO leading icon** (the leading icon `F` only appears in the compact `hideLabel` variant). HiCodex's model chip had a static `Cpu` → **removed** (+ removed the now-unused import). HiCodex always shows the label, so it matches Codex's label mode.
  - While here, dumped the permission chip fn `Xp` (also takes `hideLabel`): each mode branch sets BOTH `he`(icon) + `ge`(label), so it shows **icon + label together** — confirming §M-25's per-mode permission icons were correct. The real chip icons are `Qc`=shield-exclamation (full-access), `Gp`=shield-user (guardian), `Wp`=hand (default), cog (custom) → exactly my lucide map; the `qp`/`Jp` I chased in §M-24 were *color classes* (`J(\`icon-xs\`,qp)`), not icons.
  - **Found a real chip-label bug**: the guardian CHIP uses `guardianApproval.triggerLabel` = **"Approve for me"**, NOT the `shortLabel` "Auto-review" I used in §M-23. Fixed `formatPermissionsFooterLabel` (guardian → "Approve for me") + the `PERMISSIONS_FOOTER_ICON` key. ("Auto-review" remains the settings/`agentMode` label in permissions-mode.ts — a different surface.) tsc 0 · 74 tests ✓ · vite ✓.

**Avatar #5 — re-confirmed proprietary-IP block (not a deferral)**: re-examined `codex-avatar-I5t9u9t7.js` = **9 `.webp` mascot images** (`<img>` + `backgroundImage`), with **no fallback / initials / SVG / non-art structure** to mirror. HiCodex renders a lucide **`Bot`** icon for sub-agents (`right-rail.tsx:910`, `background-agent-panel.tsx:71`) — the clean-room-correct substitute. Nothing is alignable without redistributing the 9 proprietary mascots; the user-accepted placeholder stands. This is a proven IP boundary, re-verified.

## §M-28 — remaining-surface sweep (toasts / dialogs / shortcuts / context-menus): 4 applied, feature-gaps documented

4-agent workflow over the un-audited surfaces; every finding re-verified against the bundle before applying.

**APPLIED (visual divergences of existing surfaces):**
- **Toast auto-dismiss 7s → 5s** (`app-toast-viewport.tsx` `DEFAULT_TOAST_TTL_MS` + `base.css` animation). Codex `toast-signal-CTz_x1Qc.js`: default toast `{duration:r}` with `var r=5` (5s).
- **Toast close button** (`app-toast-viewport.tsx` + `base.css`). Codex toasts default `hasCloseButton:!0` — a visible dismiss `X` HiCodex lacked. Added a lucide `X` + local dismissed-state (toasts derive from immutable log state) + a 3rd grid column; the button opts back into `pointer-events` (the viewport is `none`).
- **Dialog Cancel button → `ghost`** + **destructive action → `danger`** (`thread-action-dialog.tsx` + `rail-cards.css`). Confirmed in the archive dialog (`local-conversation-background-terminals-model-*.js`: cancel `color="ghost"`, action `color="danger"`) + `button-OF01XbfH.js` variant map: ghost = `text-token-text-tertiary` + transparent border; danger = `bg-token-charts-red/10` + `text-token-charts-red` + transparent border. HiCodex's bordered cancel + outline-only decline → ghost (tertiary, transparent) + danger (charts-red/10 fill, light+dark). Scoped to `.hc-thread-dialog-panel` so the shared rail-card `.decline` is untouched.

**REJECTED on re-verification:** footer `gap-3` (the agent's claim — `dialog-layout` uses BOTH gap-2/gap-3; HiCodex's 8px=gap-2 is plausibly correct, so not changed); rename Save button (low-confidence, no Codex rename dialog found).

**Confirmed aligned / HiCodex-appropriate (no fix):** keyboard-shortcuts dialog — HiCodex's single-column list + semantic `<kbd>` + query-aware empty state are intentional/superior; the multi-column table is Codex's *settings-panel* variant, which HiCodex mirrors separately.

**Feature gaps — evidence-backed but NEW functionality (documented, not built; these are "implement a missing capability", not "align existing rendering"):** toast **success/danger levels** + **rich content** (title/description/custom) — a `LogLine.level` model change touching every emission site; **context menus** (HiCodex `file-tree.tsx` has a TODO; Codex `context-menu-CDka65eJ.js` is a full Radix component) and a **Tooltip component** (HiCodex uses native `title`; Codex `tooltip-CDzchJxN.js` uses floating-ui). Recorded for a feature-implementation pass with their exact Codex chunks.

## §M-29 — less-common surface sweep (auth / automations / lightbox / conversation-row): 6 applied, 5 over-trust false positives caught

4-agent workflow over the remaining less-trafficked surfaces; every finding re-grepped against the bundle + HiCodex's actual CSS before applying.

**APPLIED:**
- **Auth** (`model-picker-menu.tsx`): sign-in button "Sign in to ChatGPT" → **"Sign in with ChatGPT"** (Codex `profile-dropdown` `signInWithOpenAI` defaultMessage; "with", not "to").
- **Image lightbox** (`image-preview-lightbox.tsx` + `base.css`): zoom button **30→36px** (Codex `size-9`); zoom-readout min-width **52→64px** (`min-w-16`); close `X` **17→18px** + nav chevrons **20→18px** (Codex `icon-sm`; download + zoom± stay 16=`icon-xs`, already aligned).
- **Sidebar archive-confirm** (`base.css`): brownish `#b43b31`/`rgba(150,61,47,…)` → **charts-red** (`#e02e2a`/`rgba(224,46,42,.1)`, +dark `#ff6764`) — Codex `sidebar-thread-section` archive-confirm is `Button color="danger"`, same charts-red danger variant as the §M-28 dialog.

**REJECTED on re-verify (5 agent false positives — all ALREADY aligned):** lightbox toolbar inset (HiCodex already `top:12px` = Codex `top-3`=12px; the agent miscalculated `top-3` as "3px"); zoom-container rounded-full (already `border-radius:999px`); nav vertical-centering (already `top:50%`+`translateY(-50%)`); download/zoom icon sizes (already 16=`icon-xs`); sidebar status-icon size (the `icon-2xs` wasn't clearly the status icon — unverified, left).

**Confirmed aligned / no findings:** automations panel (Codex `automations-page` minified-blocked — no extractable findings); conversation-row spacing / active-bg / pointer-events (HiCodex already uses the same `px-row-x` / `bg-token-list-hover-background` token classes); auth `Log out` label + `LogOut` icon.

**Lesson: the sweep agents over-trusted at their highest rate yet — 5 of ~7 lightbox/row "divergences" were already aligned (Tailwind-unit miscalculations + not checking HiCodex's existing CSS). Re-grepping HiCodex's actual values is what separated the 6 real fixes from the noise.**

## §M-30 — FEATURE BUILD: file-tree right-click context menu (was a TODO, now implemented)

Crossed from visual-alignment into building a missing FEATURE (the hook's "category 2"): the file-tree right-click context menu, which `file-tree.tsx` had as an explicit `TODO`.

**Built** `components/context-menu.tsx` — a reusable `ContextMenu` component mirroring Codex `context-menu-CDka65eJ.js`: portal-rendered, positioned at the cursor + viewport-clamped, dismiss on outside-pointerdown / Esc / scroll / resize (same proven pattern as `reasoning-picker-menu`). Visual structure follows Codex (container reuses HiCodex's `.hc-thread-menu` popover surface + `min-w-[180px]` = Codex's `min-w-[180px]`; items = `.hc-thread-menu-item` rows with a leading icon + label, matching Codex's `flex items-center gap-1.5 rounded-lg` item layout).

**Wired into `file-tree.tsx`** via a module-local React context (`FileTreeContextMenuContext`) so the recursive `FileTreeNode` rows + the search-result rows get the right-click handler without prop-threading. Right-clicking a file row opens: **Copy path** (`navigator.clipboard.writeText(entry.path)`) + **Open file** (reuses the existing `onSelect`; files only, omitted for directories).

**Scoped honestly:** Codex's `workspace-file-context-menu` also has Reveal-in-Finder / Open-with / View-in-browser — those need Tauri OS-integration host commands HiCodex doesn't yet expose (the host has `shell` + `openExternalUrl` but no reveal/open-path), so they're omitted with a code comment until that host support lands. The two implemented actions need no host changes.

tsc 0 · 74 tests ✓ · vite ✓. The `ContextMenu` component is reusable for future right-click menus (e.g., once host reveal/open lands, or other surfaces).

## §M-31 — FEATURE BUILD: hover/focus Tooltip component + composer-chip adoption

The second category-2 feature. Codex `tooltip-CDzchJxN.js` is a Radix tooltip; HiCodex used native `title`.

**Built `components/tooltip.tsx`** — a reusable `Tooltip` (portal-rendered, hover/focus + 700ms delay, viewport-clamped above-trigger / flip-below, content + optional shortcut `kbd`). Fully source-backed once I stopped assuming "not extractable": the container styling was a backtick clsx string the `className:`-anchored regex had missed — `bg-token-dropdown-background/90 text-token-foreground ring-[0.5px] ring-token-border rounded-xl text-sm shadow-xl-spread backdrop-blur-sm`; content padding `px-1.5 py-0.5`; kbd `h-4 min-w-4 px-1.5 rounded-md bg-current/10 text-xs`; `maxWidth: 20rem`; `sideOffset: 2`. Mapped to HiCodex panel tokens (rounded-xl 12px, panel-bg/90, 0.5px ring via box-shadow, blur, 12px text), light + dark.

**Adopted on the composer settings chips** (permissions / model / reasoning) — Codex's intelligence trigger wraps in `Cn`/`tooltipContent`, so this is the evidence-backed adoption point. Replaced the native `title` on all three; the chip onClick/anchor (`event.currentTarget`) is preserved (the picker menus still anchor correctly). Test updated (chips now expose a `hc-tooltip-trigger` wrapper instead of a static `title`).

tsc 0 · 74 tests ✓ · vite ✓. Reusable for incremental adoption (e.g., the message-action copy button, which Codex also tooltip-wraps).

**Lesson (again): "container not extractable" was premature — it was a backtick clsx string, not a `className:`. Searching ALL backtick template strings found it. Same pattern as §M-26/§M-27: claim-of-unextractable kept being wrong.**

## §M-32 — FEATURE BUILD: toast `success` level (third + final category-2 feature)

Codex `toast-signal-CTz_x1Qc.js` exposes `info/success/warning/danger/custom`; HiCodex had only `info/warn/error` (warn≈warning, error≈danger) — missing `success` (green).

- **Model**: added `"success"` to `LogLine.level`, the `log` dispatch action, and the log helper (`codex-reducer.ts`).
- **Color**: `.hc-app-toast[data-level="success"] .hc-app-toast-dot` = charts-green `#00a240` (light) / `#40c977` (dark) — the already-extracted Codex `--green-500/--green-300` palette (§M-20). (Codex's exact per-level toast dot-color isn't extractable from the minified bundle, but the charts-green token IS the source-backed Codex green.)
- **Emission (so it's not a dead capability)**: switched the two clearly-successful completed-action toasts from `info` → `success` — "Forked active thread to …" + "Renamed active thread to …" (`slash-request-workflow.ts`). The toast viewport already treats non-`info` levels as toast-worthy, so these now render the green success dot. (Left `error`-level + "started"/neutral logs as-is — they aren't successes.)

tsc 0 · 74 tests ✓ · vite ✓.

**All three category-2 features are now built** — context menu (§M-30), Tooltip (§M-31), toast `success` (§M-32) — each source-backed + verified. The only remaining identified divergence is the sub-agent avatar (proprietary `.webp` mascot art — proven impossible, §M-23/§M-27).

## §M-33 — Tooltip adoption broadened to the message-action buttons

Extended §M-31's `Tooltip` to the other element Codex provably tooltip-wraps: the per-message action row (`message-action-row.tsx`). The copy button + the shared `IconActionButton` now render their label via the styled `Tooltip` (codex `copy-button-*.js` wraps the button in `<Tooltip tooltipContent>`), replacing the native `title`; `aria-label` is retained for a11y. tsc 0 · 74 tests ✓ · vite ✓.

Further Tooltip adoption is intentionally gated on per-element Codex evidence (adopt only where Codex provably tooltip-wraps the element), so it stays incremental rather than a blind sweep of every `title=`. The composer settings chips (§M-31) + the message-action buttons are the confirmed cases.

## §M-34 — FEATURE BUILD: file context menu reshaped to Codex + OS-integration host command (first cross-layer alignment)

**Evidence source: `workspace-file-context-menu-ZTEfnsSe.js`** (the dedicated chunk; previously the file-tree menu was built from a guessed TODO comment). The rendered menu (item ids `workspace-file-*`), in order:
1. **open target(s)** / "Open with" submenu — the ONLY rows that carry an icon (`icon:e.icon`, the per-app icon)
2. `separator` (`{type:"separator"}`)
3. **Copy path** (`workspace-file-copy-path`, `f(x)`)
4. **Copy file contents** (`workspace-file-copy-contents`, `g(queryClient,{hostId,path})`)
5. **Reveal** (`workspace-file-reveal-path`, `M("fileManager")`), label platform-switched by `C(platform)`: `darwin`→`Reveal in Finder`, `win32`→`Open in Explorer`, else→`Open in File Manager` (i18n `markdown.fileReference.openInFinder/openInExplorer/openInFileManager`).

Two fidelity corrections this surfaced about HiCodex's prior §M-30 menu: (a) it added leading icons to Copy path / Open file, but **Codex's copy/contents/reveal rows have NO icon** — only the open-with app rows do; (b) it claimed Reveal/Open-with "need host commands HiCodex doesn't expose" — but the host already had `open_path` + `tauri-plugin-shell`, so that deferral was itself premature (the recurring lesson).

Changes:
- **Rust (`apps/desktop/src-tauri/src/main.rs`)** — new `reveal_path()` (macOS `open -R`, Windows `explorer /select,`, other Unix `xdg-open` parent dir) + `host_reveal_path` command mirroring `host_open_file_reference`, registered in `generate_handler!`. **First Rust-layer alignment of this effort.**
- **`tauri-host.ts`** — `revealPath(path)` → `invoke("host_reveal_path")`.
- **`context-menu.tsx`** — `ContextMenuItem` gains `separator?: boolean` (`label`/`onSelect` now optional); separator rows render the existing `.hc-thread-menu-separator`.
- **`file-tree.tsx`** — `fileTreeContextMenuItems` reshaped to Codex order, **icons dropped**, `osRevealLabel()` for the platform label; new `FileTree` props `onRevealEntry` / `onCopyEntryContents`. File menu: `Open file` (Codex `viewFile` label, in-app viewer — HiCodex's substitute for the app-discovery "Open in {app}" it lacks) / separator / Copy path / Copy file contents / Reveal. Directory menu: Copy path / Reveal.
- **`workspace-files-panel.tsx`** — builds the reveal + copy-contents handlers from `workspaceRoot` (joins the workspace-relative `entry.path` to absolute) via `revealPath` / `readTextFile` + clipboard, passed to `FileTree` only under `isTauriRuntime()` (so the OS-integration rows vanish in browser/test).

Verified end-to-end: **cargo check ✓ · cargo test 26/26 ✓ · tsc 0 ✓ · 74 UI tests ✓ · vite build ✓**.

## §M-35 — sidebar thread menu: "Reveal in Finder" label + reveal-vs-open action

**Evidence: `sidebar-thread-section-DpKmxoDd.js`.** The sidebar thread context menu's `open-thread-folder` row labels via `gt(platform)`: macOS `sidebarElectron.openWorkspaceRootInFinder`="Reveal in Finder", win32 `…InExplorer`="Open in Explorer", linux `…InFileManager`="Open in File Manager" — i18n description "Menu item to **reveal** a folder in Finder", and the action passes `cwd:n(Q)` (the **parent** of the folder), i.e. reveal-in-parent semantics.

Audited HiCodex's existing sidebar menu (`sidebar.tsx`) — it already has Pin/Rename/Archive/Mark-unread/**Open-in-Finder**/Copy-cwd/Copy-session-ID/Copy-deeplink/Fork (closer to Codex than assumed). Two divergences fixed:
- **Label**: HiCodex hardcoded "Open in Finder" (wrong verb on macOS, wrong app name on Win/Linux). Now uses a shared `osRevealLabel()` (extracted to `command-registry.ts`, also consumed by the §M-34 file menu) → platform-switched "Reveal in Finder"/"Open in Explorer"/"Open in File Manager".
- **Action**: `openThreadFolder` (`HiCodexApp.tsx`) called `openFileReference(cwd)` → `open_path` (**opens** the folder); switched to `revealPath(cwd)` → `host_reveal_path` → `open -R` (**reveals/selects** it), matching the "reveal a folder" description + the §M-34 reveal command. `openFileReference` had no other caller, so it was dropped from the import.

Remaining sidebar gaps (larger, deferred with reason — not clean relabels): "Fork into new worktree" (Codex splits fork into local vs worktree; HiCodex has single "Fork into local" — needs the worktree-fork flow), "Open in new window" (needs Tauri multi-window), "Toggle active status". Verified: **tsc 0 · 74 UI tests ✓ · vite build ✓**.

## §M-36 — FEATURE BUILD: context menu on inline transcript file-citation anchors

Found via a 4-surface verify-workflow (sidebar / inline-file-ref / user-message-attachments / side-panel-tab; each finding independently re-grepped against the bundle). Outcomes: side-panel-tab is Codex's *in-app browser* tab menu (Reload/Duplicate browser tabs — a feature HiCodex lacks; `implementable:false`); sidebar = §M-35 (done) + flag-gated items; the **inline-file-reference** surface was the actionable win.

**Evidence: `workspace-file-context-menu-ZTEfnsSe.js` (i18n `markdown.fileReference.*`) + `inline-mentions-*.js`** — Codex wraps every inline file-reference anchor with the shared context-menu via the `context-menu-*.js` wrapper + the `me`/workspace-file-context-menu builder. HiCodex's `message-unit.tsx` fileCitation anchor (`<a class="hc-file-citation-marker">`) had `href`+`onClick` but **no `onContextMenu`**. The building blocks already existed (the §M-30 `ContextMenu` primitive + the §M-34/§M-35 `osRevealLabel` + `revealPath` + `readTextFile`) — only the wiring was missing.

Implementation (reuses everything, no new host work):
- **`use-artifact-preview-actions.ts`** — added `revealFileReference` + `copyFileReferenceContents`, both reusing the hook's existing `resolveFileSelection` (workspace-relative citation → absolute path) then `revealPath` / `readTextFile`+clipboard.
- **`message-unit.tsx`** — extracted a `FileCitationAnchor` component (renders the same `<a>` + an `onContextMenu` that opens a `ContextMenu`) and a `FileCitationMenuContext` for the reveal/copy-contents actions. Menu items mirror the file-tree subset: **Open file** (codex `viewFile`, reuses the existing click-open) / separator / **Copy path** / **Copy file contents** / **Reveal** (platform label). `onClick` is unchanged.
- **`HiCodexApp.tsx`** — provides `FileCitationMenuContext` once above the conversation (where `useArtifactPreviewActions` lives), so the actions reach every anchor with **zero prop-threading** through the deep `MessageUnit`→`Markdownish`→`renderInline` chain (the context pattern from §M-30's file-tree). Non-Tauri/test → no provider value → the host rows drop out.

The "Open in {target}" / "Open with" / "View in browser" items are omitted (need OS app-discovery / an in-app browser HiCodex lacks) — same subset rationale as §M-34. Verified: **tsc 0 · 74 UI tests ✓ · vite build ✓**.

## §M-37 — FEATURE BUILD: context menu on user-message attachment pills + shared menu module

The audit's 4th surface (`user-message-attachments`) failed its structured-output step, so it was verified by hand. **Evidence: `user-message-attachments-dWWmK1e8.js`** imports `workspace-file-context-menu-*.js` and wraps each attachment chip with the context-menu wrapper (`getItems:h` via the `O` wrapper). HiCodex's `UserMessageAttachmentStrip` chips (`user-message-content-render.tsx`) were clickable (`onClick`→open) but had **no `onContextMenu`**.

This is the same menu as §M-36, so it surfaced a refactor: `message-unit.tsx` and `user-message-content-render.tsx` form a parent→child pair, so the §M-36 `FileCitationMenuContext` (then living in `message-unit.tsx`) couldn't be imported by the child without a cycle. Extracted a **leaf module `components/file-citation-menu.tsx`** holding `FileCitationMenuContext`, `FileCitationMenuActions`, and a shared `fileReferenceContextMenuItems(...)` builder. `message-unit.tsx` (FileCitationAnchor), `user-message-content-render.tsx` (the chip), and `HiCodexApp.tsx` (the provider) all import from it — one menu definition, no cycle, no prop-threading.

The interactive file/mention chip now opens the same `ContextMenu` (Open file / Copy path / Copy file contents / Reveal) on right-click, with reveal + copy-contents flowing through the shared context. Verified: **tsc 0 · 74 UI tests ✓ · vite build ✓**.

**The workspace-file context menu now covers all four HiCodex file-reference surfaces** — file tree (§M-34), sidebar thread folder (§M-35), inline transcript citations (§M-36), and user-message attachment pills (§M-37) — through one shared module + the §M-34 `host_reveal_path` command. (The 5th Codex consumer surface, the side-panel *browser-tab* menu, is not applicable: HiCodex has no in-app browser.)

## §M-38 — sidebar "Fork into new worktree" (the §M-35-deferred item, now built)

**Evidence: `sidebar-thread-section-DpKmxoDd.js`** (`fork-into-worktree`, `message:q.forkIntoWorktree`) + `thread-actions-*.js` (`threadHeader.forkIntoWorktree` defaultMessage **"Fork into new worktree"**). §M-35 deferred this as "needs the worktree-fork flow"; it turned out HiCodex already had both pieces — `host_create_pending_worktree` (`createPendingWorktree`, Rust-tested) and `thread/fork` accepting a workspace path. Fork-into-worktree is just their composition: create an isolated worktree for the thread's cwd, then fork into that worktree directory.

- **`thread-workflow.ts`** — `forkThreadIntoWorktree(client, threadId, cwd, createWorktree, context)`: `createWorktree({cwd})` → `forkThread(worktree.path)`. `createWorktree` is **injected** (the host `createPendingWorktree`) so the composition is a pure, unit-testable function.
- **`use-thread-actions.ts`** — `forkSelectedThreadIntoWorktree` calls it with the real `createPendingWorktree`, then `upsertThread{select:true}` (same switch as the local fork).
- **`sidebar.tsx`** — `onForkThreadIntoWorktree` prop + a "Fork into new worktree" item after "Fork into local" (Codex's fork pair order), gated on `threadCwd`.
- **`thread-workflow.test.ts`** — new test asserts `createWorktree` receives the source cwd and the fork's params equal a plain fork into the worktree path (so it's independent of `buildThreadForkParams` internals).

Verifiability note: the *wiring* (call sequence + params) is unit-tested and reuses two already-proven steps; the live end-to-end behavior (real git worktree creation) runs through the same host command the workspace-file flow already uses. Verified: **tsc 0 · 74 UI test files ✓ (incl. the new worktree-fork case) · vite build ✓**.

## §M-39 — "Open in new window" investigated → verified architectural blocker (not built)

The other §M-35-deferred sidebar item (`open-thread-new-window`, `threadHeader.openInNewWindow` "Open in new window", Codex-gated behind its multi-window flag `dt`). Investigated concretely rather than asserted: HiCodex's Rust *can* build windows (`WebviewWindowBuilder`, used for the codex-bundle window) and the `codex://threads/{id}` deeplink + `openDeepLinkUrl` routing exist — but the host forwards app-server events via `handle.emit(APP_SERVER_EVENT_NAME, …)` (`main.rs:2663`), a **global broadcast to every window**, and the codex app-server is a single process-global instance. A naive 2nd window would therefore receive the *same* event stream as the first → cross-talk that corrupts the live conversation/turn state in **both** windows.

So "Open in new window" is not a clean wiring (unlike §M-38's fork-into-worktree, which composed two proven primitives with a unit-testable seam): it requires rearchitecting the host event model for **per-window event routing** (`emit_to` targeting + window↔thread ownership tracking), a large change whose multi-window runtime behavior can't be verified in this environment and whose failure mode (event cross-talk) would degrade the core single-window experience. Recorded as a genuine architectural prerequisite, not a premature "can't" — the blocker was checked in the event-forwarding code. The remaining latent sidebar label divergence (`forkIntoSameWorktree`, Codex's `ht` flag) only differs when already inside a worktree, which never happens in HiCodex's normal flow.

## §M-40 — fresh-surface audit (composer "+" / message-hover / conversation-header): 2 clean items shipped

A second find→verify workflow over three not-yet-covered surfaces. The verify stage earned its keep again — it **caught a finder error** (the "Pursue goal" rationale claimed HiCodex has "no goal concept", but HiCodex ships a full goal backend: `thread/goal/set|get|clear` RPCs, `ThreadGoal`, the `/goal` slash command, reducer `_threadGoal`) and **overturned a finder's "no gap" verdict** on the message row (it had missed a chip). Shipped the two genuinely-clean, evidence-backed items:

1. **Composer "+" trigger accessible name** — `composer.tsx` hardcoded `title`/`aria-label="Add context"`; Codex's `composer.addContextDropdown.ariaLabel` = **"Add files and more"** (default capability variant). Pure-UI relabel. (Codex's `.remoteFiles`/`.imagesOnly` variants need capabilities HiCodex lacks — out of scope.)
2. **User-message "Merge conflicts" chip** — Codex's user-message chip strip has a `userMessage.pullRequestMergeConflict` = **"Merge conflicts"** chip gated on the item's **`hasPullRequestMergeConflict`** boolean (sibling of `pullRequestFixMode`/`autoResolveSync`, which HiCodex already reads from the same protocol). Added `if (booleanField(item, "hasPullRequestMergeConflict")) chips.push("Merge conflicts")` near the end of `userMessageMetaChips`. **Discipline catch:** the obvious field name `mergeConflicts` is wrong — it's `codex.localConversation.gitSummary.mergeConflicts` (a git-summary label); the real chip field is `hasPullRequestMergeConflict` (found in the item destructure). Verified: **tsc 0 · 74 UI test files ✓ · vite build ✓**.

Investigated-but-not-clean (recorded, not premature "can'ts"):
- **Message action row** is fully aligned — Copy / rating ("Good response"/"Bad response") / Fork ("Fork from this point") / Edit ("Edit message") / "Goal achieved in {totalTime}" all match; Codex has **no** Retry/Regenerate/Read-aloud/Share in the message rows (verified absent on both sides — "Share" is a Sites end-resource card button, not a message action).
- **"Copy as Markdown"** is already a clickable **command-palette** entry in HiCodex (`commands.ts:325`); Codex places it in a header **"Chat actions"** more-menu that HiCodex has no equivalent of (HiCodex puts thread actions in the sidebar row menu). Building that header surface for one item — when its other entries are blocked (below) — isn't proportionate.
- **Env icon** (local/worktree/cloud) and **"Fork into same worktree"** label need **per-thread worktree classification** HiCodex doesn't compute (the `Thread` type carries only `gitInfo`+`cwd`; `ComposerWorkMode` is a forward-looking composer *setting*, not a per-thread fact). **"Open side chat"** has a handler (`openSideChatFromThread`) but it's unwired to any thread menu. **"Open in new window"** is the §M-39 architectural blocker. **"Pursue goal"** in the "+" menu doesn't fit HiCodex's content-attachment action shape and is already surfaced via `/goal`.

## §M-41 — settings / right-rail / plan audit: 4 clean items shipped

A third find→verify workflow. Right-rail and plan-cards yielded **zero** confirmed gaps (the verify stage again caught finder errors: the process-manager rows — "View all processes" / "Open output" / restart-stop — need a host process-lifecycle backend HiCodex lacks, flag-gated `Dn('3264431617')` + `kill-child-process`, zero matches in HiCodex; and "Uses {model}" was wrongly flagged as missing — HiCodex **already** renders it from `spawnModel`). The settings surface gave four clean, evidence-backed items:

1. **Turn-completion notification option labels** — `notification-preferences.ts` returned "Off"/"Background only"/"Always"; Codex's `notifications.turnMode.*` are **"Never" / "Only when unfocused" / "Always"**. The 1:1 mapping was re-verified against `shouldNotifyTurnCompletion` (off→never, backgroundOnly→`!(visible&&focused)`=unfocused, always→always) — same enum + logic, pure relabel. Also fixed the now-awkward command-panel action title (`Use ${label.toLowerCase()} notifications` → `Notify on turn completion: ${label}`).
2. **"Language" row description** dropped a trailing period not in Codex (`settings.ide.language.description` = "Language for the app UI") — fixed at the i18n catalog (`i18n.ts`) **and** the panel's inline fallback.
3. **"Code font size" row description** was a **hardcoded raw string** (never localized) + a stray period; routed it through `formatMessage` and **added** the missing key `settings.general.appearance.codeFontSize.row.description` to EN + zh-CN (Codex: "Adjust the base size used for code across chats and diffs"; zh "调整聊天和差异视图中代码使用的基础字号").
4. **Standalone todo-list step icons** were 12px — sized off the **unreachable** compact `nT` (`icon-xxs`); the reachable `ow`/`uw` step icon is `icon-3xs` = **10px** (`.icon-3xs{10px}` verified). Changed to 10px (`.hc-inline-plan-spinner` only animates — no CSS size override) + corrected the comment's `nT`→`ow`/`uw` mis-attribution.

**Discipline catch:** the obvious notification-field name and the obvious merge-conflict field name were both traps this session (`mergeConflicts` was a git-summary label in §M-40; here the icon was attributed to the wrong unreachable component) — re-grepping against the real code each time is what caught them. Verified: **tsc 0 · 74 UI test files ✓ · vite build ✓**.

## §M-42 — collaboration / experimental / slash-command audit: 2 slash titles shipped (yield collapsing → comprehensive)

A fourth find→verify workflow. The clean-item yield has now clearly collapsed across the four audits (big inline-ref → 2 → 4 → 2 tiny), the signal of comprehensive coverage:

- **Collaboration-modes settings: 0** — there is no Codex collaboration-modes *settings panel* (collaboration modes are composer-only `plan`/`default` indicators); HiCodex's is a backend-data-driven pass-through with no hardcoded labels. The guardian/Auto-review strings are the *permissions* surface, already ported (§M-26). Inventing labels here would mis-align.
- **Experimental-features: 2 cosmetic strings** (empty "No beta experimental features available" / loading "Loading experimental features…") — **not shipped**: HiCodex derives both from a *generic* `panelMessage` builder (lowercased title), so each needs a per-panel override, and the empty state also has a structural difference (Codex renders a disabled SettingsRow, not centered text) — partial + low value (rarely-seen states). The verify stage correctly **rejected** the beta-stage feature filter: it hardcodes 8 Codex business feature names (**violates the no-hardcoded-business-names rule**) and would hide features lacking a HiCodex home (regression).
- **Slash commands: 2 clean TITLE relabels shipped** — `/review` "Review"→**"Code review"** (`composer.reviewMode.title`), `/side` "Side conversation"→**"Side"**. The ~6 *description* diffs were **deliberately not adopted**: HiCodex's descriptions accurately describe HiCodex's *actual* behavior, which differs from Codex's — `/fork` is local-only (Codex "into local or a new worktree" would over-promise), `/goal` does create/inspect/clear (Codex only "set"), `/status` toggles a display (Codex "show id + rate limits"), `/mcp` reloads+lists (Codex "show status"), `/review` has no branch-compare. Adopting Codex's wording would **mis-describe HiCodex's commands** — the opposite of alignment. Only titles (no behavior claim) were safe.

Verified: **tsc 0 · 74 UI test files ✓ · vite build ✓**. The principle that held all session: a *description* must match the command's real behavior, so descriptions are behavior-gated, not free relabels — only labels/titles with no behavior claim are pure-UI.

## §M-43 — per-thread worktree classification: env-icon + "Fork into same worktree" (the §M-40-deferred infra)

The conversation-header env indicator and the worktree fork label, both gated on a per-thread worktree classification §M-40 flagged HiCodex couldn't compute. **Evidence: `thread-env-icon-DLKw_pWO.js`** (glyph chunks `macbook`=local / `worktree` / `cloud` / `remote-host-globe`; tooltips **"This conversation is running locally."** / **"...in a local git worktree."** / "...in Codex Cloud.") + `sidebar-thread-section` (`ht ? forkIntoSameWorktree : forkIntoLocal`).

**Data source:** HiCodex's protocol "environment" is cloud/remote-exec (environmentId + execServerUrl), *not* local-vs-worktree — so worktree status had to be **computed**. Added a host check: `git rev-parse --absolute-git-dir` — a linked worktree's git dir lives under `.git/worktrees/<name>`.

- **`main.rs`** — `host_git_status` now returns `is_worktree` (new `git_is_linked_worktree` helper). cargo check + the 26 Rust tests pass.
- **Both `HostGitStatus` TS types** (`tauri-host.ts` *and* `state/worktrees.ts`) gained `isWorktree?: boolean`. **Discipline catch:** the env-icon's `?.isWorktree ? … : …` ternary type-checked, but the fork-label's `?.isWorktree ?? false` (assigned to `boolean`) failed — exposing that HiCodexApp uses `worktrees.ts`'s `HostGitStatus` (an `[key: string]: unknown` index-signature type that resolved `isWorktree` to `unknown`), so the explicit field was needed there too.
- **`conversation-chrome.tsx`** — an env-icon before the title (lucide `Laptop` for local — platform-neutral vs Codex's macbook glyph — / `GitBranch` for worktree), wrapped in the §M-31 `Tooltip` with Codex's exact tooltip strings; muted, theme-safe via `opacity` (`.hc-topbar-env-icon`). HiCodex is a local client, so cloud/remote are out of scope (local vs worktree).
- **`sidebar.tsx`** — the fork label swaps `"Fork into local"` → **"Fork into same worktree"** for the active thread when it's in a worktree (`activeThreadIsWorktree` from `worktreeHostGitStatus.isWorktree`). Active-thread-only by design: per-row git checks for every sidebar thread would be costly, worktrees are rare, and the active thread is the common fork target (forking a worktree thread reuses its worktree, so the label is behavior-accurate).

Verified end-to-end: **cargo check ✓ · cargo test 26/26 ✓ · tsc 0 ✓ · 74 UI test files ✓ · vite build ✓**.

## §M-44 — "Open in new window" built (additive multi-window), with an explicit runtime caveat

The last infra item. **Evidence: `threadHeader.openInNewWindow` = "Open in new window"** (feature + label). Codex (Electron) opens a `BrowserWindow`; HiCodex maps that to a second Tauri webview — the same kind of platform mapping as §M-34's `host_reveal_path` (Codex's Electron reveal → a Rust `open -R`). Built **additively** so single-window behavior is untouched:

- **`main.rs`** — `host_open_thread_window(app, thread_id)`: a `WebviewWindowBuilder` loading the main app (`WebviewUrl::default()`), injecting `window.__HICODEX_INITIAL_THREAD__` via `initialization_script` (set before the page loads → no load-timing race), and focusing an existing per-thread window instead of duplicating. cargo check + 26 Rust tests pass.
- **`tauri-host.ts` / `HiCodexApp.tsx` / `sidebar.tsx`** — `openThreadWindow` binding; a one-shot startup effect that, once connected, reads the injected global and routes via the **existing** `openDeepLinkUrl(codex://threads/{id})` path (the main window has no such global, so it no-ops there); an `openThreadInNewWindow` handler; and an "Open in new window" item in the sidebar thread menu.
- **Cross-window events**: the host still broadcasts app-server events, but each window scopes its UI to its own `activeThreadId`, so cross-talk is benign (extra state, not displayed), and the codex app-server (the same backend Codex's own multi-window uses) handles concurrent per-thread turns.

**Verification caveat (honest):** **cargo check ✓ · cargo test 26/26 ✓ · tsc 0 ✓ · 74 UI test files ✓ · vite build ✓** — so it compiles and single-window is provably intact. But the *multi-window runtime* (window actually opens, routes to the thread, event isolation holds, cleanup on close) **can't be exercised in this environment** — it needs live multi-window testing. This is the one change this effort that is verified to compile + not-regress rather than behavior-verified; the runtime is flagged for a manual check.

With this, **both infra items the §M-39/§M-40 audits flagged are now built** — per-thread worktree classification (§M-43) and "Open in new window" (§M-44).

## §M-45 — per-panel command-panel loading/empty strings (the §M-42-deferred item, generalized)

`panelMessage` (`command-panel.ts`) generically built `Loading {title}…` / `No {title} found.` for every panel; Codex ships **dedicated** per-panel strings that lowercase the loading noun and use bespoke empties. Replaced the generic-only builder with a `PANEL_MESSAGE_OVERRIDES` map of byte-exact Codex strings (keys: `skills.page.*`, `skills.appsPage.loading`, `plugins.page.loading`, `agent-settings` `experimentalFeatures.*`), falling through to the generic builder for panels without a known Codex string:

- **experimental** — loading "Loading experimental features…", empty "No beta experimental features available"
- **skills** — loading "Loading skills…", empty **"No skills found"** (Codex `skills.page.empty` drops HiCodex's trailing period)
- **apps / plugins / hooks** — loading "Loading apps…" / "Loading plugins…" / "Loading hooks…" (Codex lowercases the noun vs HiCodex's title-cased generic)

Loading ellipses use U+2026 to match HiCodex's existing convention. Left generic (no clean main-panel Codex string): the apps/plugins/hooks *empty* states, and the **mcp** empty (Codex has both "No MCP servers connected" and "…configured" — HiCodex's panel semantics don't cleanly pick one). The experimental empty also has a *structural* difference in Codex (a disabled SettingsRow vs centered text) — a separate render change, not addressed (string-only). Verified: **tsc 0 · 74 UI test files ✓ · vite build ✓**.

## §M-46 — slash-command descriptions adopted (reversing §M-42's behavior-gating, per stop-hook)

§M-42 left the slash-command *descriptions* un-adopted because HiCodex's mechanics differ from Codex's (treating descriptions as behavior-gated). The stop-hook clarified that the user's "尽可能对齐" directive prioritizes **source-backed string alignment for UI consistency** and accepts a documenting note about mechanic differences — i.e. the behavior-gating was my judgment call, not the user's. So adopted Codex's byte-exact descriptions (audit-4-verified, `composer-zFOdryLS.js`) for the 7 mapped commands in `composer-workflow.ts`:

- `/review` → "Review unstaged changes or compare against a branch" · `/fork` → "Fork this chat into local or a new worktree" · `/side` → "Start a side conversation in an ephemeral fork" · `/goal` → "Set a goal that Codex will keep working towards" · `/status` → "Show chat id, context usage, and rate limits" · `/mcp` → "Show MCP server status" · `/personality` → "Choose how Codex responds"

A header note on `DEFAULT_SLASH_COMMANDS` records the trade-off: the wording follows Codex; where HiCodex's mechanics differ (e.g. `/status` surfaces session info rather than only toggling; `/mcp` reloads while listing; `/goal` also inspects/clears; `/fork`'s worktree option is in the sidebar menu), the divergence is mechanics, not the user-facing description. Verified: **tsc 0 · 74 UI test files ✓ · vite build ✓**.

## §M-47 — audit #5 (onboarding / MCP / hooks-skills / debug): "HiCodex-specific" was over-asserted

The stop-hook rightly flagged that I'd *asserted* "HiCodex-specific" for these surfaces without verifying. A 5th find→verify audit corrected that — it **verified** what's genuinely HiCodex-specific AND found real pure-UI gaps I'd dismissed.

**Verified genuinely HiCodex-specific / not-applicable** (real Codex strings, but ChatGPT-account/cloud/backend-specific — not for HiCodex's local-binary model): the multi-IdP login screen (`Continue with Google` / `…Microsoft` / `Sign in with GitHub Copilot` / `Sign up` / `Play Snake`), first-run cloud/billing marketing (`Powered by your ChatGPT account`, `Hand off to Codex in the cloud`), Codex Mobile pairing, the connector-consent modal (`How ChatGPT uses data`), and the multi-step NUX Back/Next wizard (`NUX_2025_09_15`). HiCodex covers only the one relevant provider (ChatGPT via `/login`) and runs a local codex binary.

**Genuine pure-UI gaps found (I'd wrongly dismissed these):**
- **Onboarding** — safety disclaimer **"Codex can make mistakes. Review the code it writes and commands it runs."** (`codex.legal.mistakes.*`) ✅ added to the first-thread promo; + autonomy framing, a "Get API Key" link, an "Onboard me" tour card (pending).
- **MCP settings** — ✅ "Add MCP server"→**"Add server"** (`settings.mcp.addServer`), per-server "Edit"→**"Settings"** (`settings.mcp.server.settings`), "Remove"→**"Uninstall"** (`settings.mcp.detail.uninstall`); + `Servers` heading, `Back`, `Docs`, save/uninstall/no-writable-config errors, the transport + field labels (matched-but-not-i18n-wired), and row-add buttons (pending).
- **Hooks / Skills pages** — subtitle, event names/descriptions, source headers, trust controls, skills subheading/search/refresh/tabs/empty-states (pending — larger).

**Settings-page subtitles (corrected mechanism).** First attempt added a per-kind subtitle line *inside* `McpSkillsManagementPanel` — but that panel renders below the settings page-header (`model-settings-panel.tsx:209`, which already renders `activeSection.description` as the page subtitle), and MCP's nav description was already filled, so it **duplicated** the MCP subtitle. Reverted (helper + render + the `.hc-management-panel-subtitle` CSS). The right mechanism is the nav `description` (`settings-panel-workflow.ts`), which feeds both the nav-sidebar and the page-header subtitle. The prior pass had marked hooks/skills/browser/computer-use "no subtitle" — **that was incomplete**; Codex *does* have page subtitles for them (verified via each string's bundle `description: "Subtitle for …"`). Filled four empty nav descriptions:
- **Hooks** → "Manage lifecycle hooks from config and enabled plugins." (`Subtitle for hooks settings`)
- **Skills** → "Give Codex superpowers." (`Subheading shown above the skills sections`)
- **Computer use** → "Manage how Codex uses other applications on your computer" (`Subtitle for computer use settings`)
- **Browser** → "Manage Codex's browser." (`Subtitle for in-app browser settings`; dropped the trailing `<computerUseSettingsLink>` clause — a cross-section link the plain-text subtitle can't render)

Codex's `<a>/<link>Learn more</link>` doc-links are omitted on all of these (plain-text nav subtitle; links target Codex's hosted docs). agent / personalization / keyboard-shortcuts / plugins / environments / archived-chats genuinely have **no** Codex page subtitle → left empty (the prior pass was right for those).

**Verified-reason deferrals** (NOT silently dropped — the hook's point 2):
- **model-settings-panel.tsx is a HiCodex-specific custom-gateway model config** (name / transport / base-URL / api-key / model-list / temperature / image-input) with **28 lines of hardcoded Chinese** (连接/模型 fieldsets). Codex has **no** equivalent panel (Codex uses a ChatGPT account or one OpenAI key, not arbitrary OpenAI-compatible gateways), so there is no Codex source string to align each label to. English-ifying it would be an app-consistency fix, not a Codex-evidence-backed one — **flagged for a decision**, left unchanged.
- **"Get API Key" link** (Codex "Get API Key" → `platform.openai.com/api-keys`): deferred — HiCodex's key field is a *general* OpenAI-compatible gateway key (custom base URL), so an OpenAI-account-specific link is a semantic mismatch.
- **MCP save/uninstall errors** ("Could not save / uninstall MCP server"): HiCodex has no dedicated message — errors fall through generically; wiring them is error-path work, not a string swap.
- **Skills tabs / Search / New-skill / Recommended-vs-Installed, hooks event-rows/trust-controls**: structural — Codex's settings *pages*; HiCodex renders these as command-panel lists. Feature work, not string swaps.

Done + verified this batch: **tsc 0 · 74 UI test files ✓ · vite build ✓** (two MCP-label test expectations updated). **Lesson (the hook's points): don't assert "HiCodex-specific" — audit it (most onboarding *was*, but the disclaimer/MCP-label/subtitle gaps were real); and don't dismiss empty strings — four empty nav descriptions had real Codex subtitles.**

## §M-48 — audit #6 (cross-cutting string sweep): 21 verified swaps, 1 app-wide-convention defer

A 4-dimension find→verify **workflow** (27 agents; dimensions: settings-page *body* content, empty/error/loading states, tooltips/aria/placeholders, dialogs/confirmations — the cross-cutting categories the feature-focused audits 1–5 didn't sweep) returned **23 candidates → 21 implement / 2 defer / 0 reject**, each adversarially re-grepped (with hex-dumps for the ellipsis/apostrophe ones). **20 implemented + verified** (tsc 0 · 74 UI · build ✓ · cargo ✓; updated 2 test expectations — `mcp-follow-up-dialog.test`, `conversation-chrome.test`):

- **Keyboard shortcuts** (settings-panel + the ⌘⇧/ dialog): placeholder "Filter shortcuts…"→**"Search shortcuts"** (`search.placeholder`, no ellipsis), aria "Filter shortcuts"→**"Search keyboard shortcuts"** (`search.ariaLabel`), empty "No shortcuts match "{query}""→**"No matching shortcuts"** (`noMatches` / `keyboardShortcutsDialog.noMatches`, drops query interpolation).
- **Thread find bar**: aria "Find in thread"→**"Find in chat"** (`codex.threadFindBar.label`), placeholder "Find"→**"Search chat…"** (`.placeholder`, U+2026), "Next match"→**"Next result"** + "Previous match"→**"Previous result"** (`.nextResult`/`.previousResult`).
- **Composer branch switcher**: "Loading branches"→**"Loading branches…"** (U+2026), "Failed to load branches"→**"Unable to load branches"** (`composer.reviewMode.branches.error`), "No branches"→**"No branches found"** (`localConversation.syncSetup.noBranches`).
- **File-tree filter** clear button: "Clear filter"→**"Clear file filter"** (`codex.fileTreeSearch.clear`). **Diff preview** empty: "No diff hunks to preview"→**"No diff available"** (`codex.diffView.noDiffData`). **Mermaid** SVG aria: "Mermaid flowchart preview"→**"Mermaid diagram"** (`mermaidDiagram.ariaLabel`). **Sidebar** thread-row action group aria: "Thread actions"→**"Chat actions"** (`threadHeader.moreActions`). **Approval "other"** textarea: "Or type your own answer"→**"No, and tell Codex what to do differently"** (`requestInputPanel.otherPlaceholder`). **MCP follow-up** body: "An MCP app wants to send this prompt."→**"An app wants to send this prompt"** (`codex.mcpTool.confirmFollowUp.description`, drops "MCP" + period). **Rename-chat dialog**: added subtitle **"Keep it short and recognizable"** (`renameThreadDialogSubtitle`) + input placeholder **"Add a title…"** (`renameThreadDialogPlaceholder`).

**Deferred #5 — the apostrophe swap (caught by a manual cross-check the per-finding verify couldn't make).** The finding flagged 12 "Couldn't load this preview" literals (file-preview ×3, artifact-preview ×9): HiCodex uses ASCII `'` (U+0027), Codex uses curly `'` (U+2019). The verify stage byte-confirmed it and said "implement" — but it only saw those 12 strings. A grep of the whole component tree showed **HiCodex uses ASCII apostrophes uniformly: 204 contractions, 0 curly**, while Codex uses curly throughout. So this is an **app-wide typographic-convention divergence, not a per-string gap** — aligning only these 12 would make HiCodex internally inconsistent (12 curly among 192 ASCII). Correct fix is one app-wide decision (sweep all 204→curly to match Codex, or keep ASCII), not a piecemeal swap. **Flagged, not done.**

**Deferred (workflow, both well-reasoned):** (1) keyboard-shortcuts **table column headers** (`Command`/`Keybinding`/`Actions`) — byte-confirmed but **structural** (needs a new `<thead>`, and HiCodex renders per-section grouped tables vs Codex's one flat table — not a 1:1 add). (2) nav-rail **gear "Settings" tooltip** — HiCodex's `app-navigation-rail.tsx` is a **HiCodex-specific business shell** whose sibling tabs are hardcoded Chinese (工作台/知识库/档案中心/待办, container 全部项目, gear 设置); hardcoding English "Settings" beside untranslated Chinese is inconsistent — needs i18n routing, not a literal swap. **This corroborates the §M-47 model-settings finding: HiCodex has a Chinese "business-shell" layer (nav rail + custom-gateway model config) that has no Codex equivalent and is out of scope for Codex-string alignment.**

**Lesson: even adversarially-verified per-finding verdicts need a final consistency/context check before applying — the apostrophe swap was "verified implement" in isolation but is a defer once you see the app-wide convention. The manual cross-check (the recurring discipline) is what caught it.**

## §M-49 — "chat" vs "thread" terminology normalization (slash commands)

A complementary consistency check (prompted by audit-6 swapping "Find in thread"→"Find in chat" / "Thread actions"→"Chat actions"): is HiCodex's user-facing term "chat" or "thread"? **Confirmed "chat" is HiCodex's uniform convention** — every user-facing `aria-label`/`title`/`placeholder` uses "chat" (Archive chat, Chat in progress, Filter sidebar chats, Find in chat, …; zero user-facing "thread"), the rename dialog header is "Rename chat", and `/fork`'s description already says "chat". Codex's term is also "chat" (command-menu "New chat", "Show chat id", "Archive chat").

But the slash-command list (`composer-workflow.ts` `DEFAULT_SLASH_COMMANDS`) had stray **"thread"** in 7 user-facing strings. Normalized to "chat": `/new` **title "New thread"→"New chat"** (direct Codex evidence — command-menu item `defaultMessage:"New chat"`, description "Command menu item to start a new thread") + descriptions/hints for `new`/`rename`/`resume`/`compact`/`approve`/`stop`/`clean` ("Start a new chat.", "Rename the active chat.", "Resume an existing chat." + hint "chat id", "Compact the active chat context.", "…retry for this chat.", "…terminals for this chat."). Basis: Codex's verified "chat" terminology + HiCodex's own dominant convention (these descriptions are HiCodex's own text, so this is terminology consistency, not a per-string Codex swap).

**Left alone:** `/agent` "Switch or manage agent **threads**" (a distinct multi-agent concept, not the conversation); all `command(…, "thread", …)` **group-key** args (internal taxonomy matching `state/commands.ts`, not user-facing); and the **mcp-follow-up dialog**'s "New thread" send-target option (HiCodex-specific dialog structure with no Codex equivalent and a seemingly intentional "side chat" vs "separate thread" distinction — not normalized to avoid mangling a bespoke feature). `tsc 0 · 74 UI ✓`.

## §M-50 — audit #7 (toasts / menus / status-chips / buttons) + coordinated taxonomy alignment

A 5th-category find→verify **workflow** (13 agents) with a **hardened app-wide-convention check** (the §M-48 apostrophe lesson, baked into both finder discipline and the verify step) returned **9 candidates → 3 implement / 6 defer / 0 reject**. The lower yield + zero rejects confirms the clean-swap space is thinning and the convention-guard works (it deferred everything app-wide/structural instead of false-flagging it). Toasts found **nothing** (transient-feedback surfaces already aligned).

**3 isolated swaps implemented + verified** (tsc 0 · 74 UI · build ✓):
- **Sidebar empty group** — hardcoded Chinese `暂无会话，从 + New chat 开始`→**"No chats"** (`sidebarElectron.noTasks`). A genuine **isolated outlier**: the only user-facing Chinese on the otherwise-all-English Codex-aligned sidebar (distinct from the HiCodex-specific Chinese business-shell — KB views ~700 literals, model-config ~23, nav-rail ~7 — which has no Codex equivalent). Used a bare literal (sidebar uses bare English literals, no `useIntl`).
- **Queued follow-up retry tooltip** — "Retry queued message"→**"Try sending this queued message again. Edit or delete it if retry keeps failing."** (`composer.queuedMessage.retryTooltip` + `.retryTooltipRemedy`, matching the sibling warning's existing `{primary}. {remedy}.` concat at line 69).
- **Composer branch switcher** create button — "Create new branch…"→**"Create and checkout new branch…"** (`composer.footer.branchSwitch.createAndCheckout`; the handler already runs `git checkout -b`, so the label is accurate).

**Coordinated command-group taxonomy alignment** (audit-7 deferred this as a *piecemeal* 2-file edit — correctly, since that would create inconsistency — but flagged the right fix is to align all arrays together). HiCodex's `commandMenuGroupKey` taxonomy titles are shared across **4 arrays** (`command-panel.ts` GROUP_TITLE_ORDER, `settings-panel-workflow.ts` KEYBOARD_SHORTCUTS_GROUP_TITLE, `keyboard-shortcuts-dialog.tsx`, `keyboard-shortcuts-settings-panel.tsx`). Verified the full Codex label set (`codex.commandGroup.*` and `keyboardShortcutsDialog.section.*` **agree**): only **2** differ — `thread` "Thread"→**"Chat"** and `workspace` "Workspace"→**"Project"** (panels/navigation/skills/configure/app already matched). Changed the **title** in all 4 arrays (keys unchanged); updated the `command-panel.test.ts` group-title expectations. (The `/debug-config` config-layer label "Workspace" in `slash-request-workflow` is a *different* surface — left as-is.)

**Deferred (4, verified reasons):** copy-success toasts "{Thing} copied"→Codex "Copied {thing}" (**app-wide** word-order across 8 callers + a parallel convention; and Codex emits toasts only for working-dir/markdown, *not* session-id/deeplink — a naive swap would add toasts Codex lacks → refactor decision); user-message copy aria "Copy message" (**structural** — HiCodex unifies user+assistant+turn copy into one shared `MessageActionRow` whose aria already matches Codex's *shared* `copyButton.copyAriaLabel`="Copy"; splitting it to add the bespoke user-variant label is a shared-component change); sidebar "Organize"→"Organize sidebar" (**structural** — Codex's is a submenu-opener menuitem with a different option set; HiCodex's is an inline heading over a reworked flattened set); user-message goal chip "Goal: {objective}"→"Sent as goal" (**structural** — Codex splits goal display into a fixed chip + a `composer.threadGoal.*` pill family that HiCodex lacks entirely; a blind swap would drop the objective text from the UI).

## §M-51 — audit #8 (date/time/number · settings form-controls · file/attachment/image · markdown) + clean-swap exhaustion

Final clean-swap **workflow** over the last un-swept categories returned **5 candidates → 1 implement / 4 defer / 0 reject**. Yield trajectory **21 → 3 → 1** confirms the clean user-visible string-gap space is **essentially exhausted**. (markdown/code-block found 0; the date/time finder explicitly documented ~8 already-aligned formatting families — relative time `{n}m/h/d/w/mo/y` incl. 7d/30d/365d thresholds, file/edit ICU plurals, `# tasks completed`/`# comment`/`# CI test` ICU, image-preview aria, diff stats `+N -N`, per-message timestamp Intl, file-size formatter — so they are confirmed aligned, not silent gaps.)

**1 implemented + verified** (tsc 0 · 74 UI · build ✓): the **text-only-model image rejection** string `"Current model does not declare image input support"` (HiCodex's internal/technical phrasing, 7 sites). Codex has **two** user-facing strings, and I mapped them **precisely** (more aligned than the finding's single-string collapse) — confirmed by their bundle `description`s and the call-site semantics:
- 5 **add-image guards** in `composer.tsx` (`addImageFilesAsDataUrls` / `addTransferFiles` / local-image picker / `confirmAttachment`) → **"This model does not support image inputs. Try a different model."** (`composer.imageInputsUnsupported`, desc "Toast shown when a user tries to **add** images for a text-only model").
- 2 **submit guards** in `use-turn-submission.ts` (queued-follow-up send + draft submit, gated on `composerHasImageAttachments && !supportsImageInput`) → **"Remove images or switch models to send this message"** (`composer.submit.imageInputsUnsupported`, desc "Message shown when **submit is blocked** due to unsupported image inputs").

**Deferred (4, verified):** right-rail `% used` label (Codex's `{usage}% used ({remaining}% left)` lives on the *composer* surface, not the Desktop status-popover HiCodex's footer emulates; + bare `{n}% used` is an app-wide taxonomy shared with account quota labels; + structural two-branch logic) · right-rail token tooltip (Codex renders a styled multi-element tooltip *card* w/ `Context window:` header + `{k}k / {k}k tokens used`; HiCodex uses a flat native `title` — structural) · permissions mode descriptions (HiCodex-original status-list; the trailing period is HiCodex's *consistent* full-sentence convention across all 5 sibling modes) · appearance theme/reduce-motion descriptions (HiCodex uses mode-aware dynamic helpers across 3 surfaces — richer than Codex's single static line — an established design, not a missed string).

**Clean-swap phase complete (audits 1–8).** Remaining alignment opportunities are all **non-clean-swap** and documented with verified reasons: (a) **app-wide conventions** a user should decide once — apostrophe ASCII→curly (204 sites), copy-success toast word-order, `% used` format; (b) **structural** — table headers, goal chip↔composer-pill split, shared-copy-button variant split, status-tooltip card; (c) **HiCodex-specific Chinese business shell** (KB views ~700 literals, custom-gateway model config ~23, nav-rail tabs ~7) with no Codex equivalent. None are blind string swaps; each needs a deliberate decision or feature work.

## §M-52 — copy-success toast word-order convention (coordinated, like §M-50 taxonomy)

audit-8 deferred this as a "convention/refactor decision" (not a clean *piecemeal* swap). Done as a **coordinated** convention alignment — the same shape as the §M-50 GROUP_TITLE taxonomy. HiCodex's copy feedback read **"{Thing} copied"** (`copyTextToClipboard` helper in `HiCodexApp.tsx` → `${label} copied`, 6 call sites) + a parallel **"{label} copied to clipboard."** in `use-command-panel-actions.ts`. Codex's convention is **"Copied {thing}"** (verified: `threadHeader.copyWorkingDirectorySuccess`="Copied working directory", `copyConversationMarkdownSuccess`="Copied conversation as Markdown", aria "Copied session id").

Aligned: the helper now emits **`Copied ${label}`** with the noun passed **lowercase** (so the success toast is byte-exact: "Copied working directory" / "Copied conversation as Markdown" / "Copied session id" / "Copied conversation path"); the sentence-initial "unavailable" warning capitalizes it (`${sentenceLabel} is unavailable`) so that path stays grammatical. The parallel command-panel toast → **`Copied ${action.label}`** (dropped "to clipboard" + period, matching Codex's specific-thing form). **Kept** HiCodex's session-id/conversation-path toasts (Codex emits no toast there, but additive copy-confirmation feedback is a defensible HiCodex choice, now in Codex's word order — not a misalignment). No test impact. `tsc 0 · 74 UI · build ✓`.

This was the last app-wide *word-order/wording* convention. (Apostrophe convention handled separately in §M-53.)

## §M-53 — apostrophe convention (ASCII → Codex typographic U+2019), scoped to user-facing strings

§M-52 had flagged this as "not a safe mechanical swap." That was overcautious: a *blanket* sweep is unsafe, but **scoped to user-facing string content** it is safe + Codex-byte-exact. Codex's source strings uniformly use **U+2019** ('') for both contractions ("Couldn't") and possessives ("Codex's"); HiCodex used ASCII `'`. 

Scoping (grep): 487 contraction-apostrophes total — **433 in comments** (skipped, cosmetic), 2 in `replaceAll("\\'", …)` string-manipulation *code* (skipped), 3 in `thread-workflow.ts` model-prompt text (skipped — sent to the LLM, not UI), leaving **~12 genuine user-facing strings**. Converted those to U+2019:
- "Couldn't load this preview" (file-preview ×3 + artifact-preview ×9 — **resolves the §M-48 deferral**)
- approval labels: "Yes, and don't ask again…" / "Don't persist" / "Don't show again" / "…isn't on the current network allowlist"
- turn-rating feedback: "Didn't follow my instructions" / "…my template"
- "Manage Codex's browser." (nav subtitle), memories "…this chat's context", config-write error "app-server's…", edited-message error "This conversation's rollout file…", feedback-diagnostics "Matches Codex Desktop's…"

Several are **Codex-verbatim ICU defaults** (`composer.memoriesSlashCommand.useMemoriesDescription`, `execApprovalRequest.menu.*`) — now byte-exact to Codex; `replace_all` also corrected the code-comment copies that quote them. Why safe: the apostrophe-between-letters pattern is always string/text content (never a JS single-quote delimiter), it's a pure character swap (no layout/visual change), and tsc + build + 74 tests catch any breakage. Updated **8 test assertions** to curly (approval ×3, config-write, slash, i18n, and the pending-request HTML-render assertion `&#x27;`→literal U+2019). `tsc 0 · 74 UI · build ✓`.

**All app-wide conventions are now aligned** (chat/thread terminology, command-group taxonomy, copy-toast word order, no-trailing-period empties, typographic apostrophes). Combined with the 8 clean-swap audits, the **string/label/convention layer is comprehensively aligned**. The only remaining alignment tiers are (a) **structural/visual** (table layout, goal-pill family, tooltip card — need A/B verification against Codex.app, not headless-verifiable) and (b) **HiCodex-specific Chinese** (KB/model-gateway/nav-rail — no Codex source).

## §M-54 — keyboard-shortcut / command axis: verified aligned (headless-checkable, no string layer)

Opened a fresh **non-string axis** to confirm coverage: the keymap. HiCodex's `state/commands.ts` `COMMAND_DESCRIPTORS.defaultKeybindings` were extracted and diffed against Codex's `electron:{…defaultKeybindings:[{key:…}]}` registry in the bundle. **All sampled bindings match byte-exact**: openCommandMenu `⌘K`, findInThread `⌘F`, toggleSidebar `⌘B`, newThread `⌘N`, toggleFileTreePanel `⌘⇧E`, searchChats `⌘G`, searchFiles `⌘P`, archiveThread `⌘⇧A`, renameThread `⌘⌥R`, showKeyboardShortcuts `⌘⇧/`. The per-binding `// codex: electron-menu-shortcuts-*.js …` citations were accurate — a prior pass aligned this correctly; **no change needed**.

A command-set spot-check surfaced a few Codex commands not in HiCodex's `COMMAND_IDS` — all **feature-dependent, not clean keybinding adds**: `reloadBrowserPage` / `hardReloadBrowserPage` (Codex in-app browser page), `toggleTraceRecording` (debug/perf tracing), `newWindow` / `closeTabOrWindow` (window management — `⌘W`/new-window are largely Tauri/OS-native, and §M-44 already added thread-in-new-window). These need the underlying feature, so they fall in the structural/feature tier (and the Codex command extraction was a narrow grep, so this isn't an exhaustive command diff).

**Net:** keybindings/formatting/strings/conventions/aria/tooltips/dialogs/empties/subtitles are all aligned and verified headless. The remaining tiers are unchanged: structural/visual (need Codex.app A/B), HiCodex-specific Chinese (no Codex source), and feature-dependent commands (browser/trace/window — need the feature).

## §M-55 — `newWindow` command (⌘⇧N), extending §M-44

Implemented the Codex `newWindow` command (one of §M-54's feature-dependent command gaps) — chosen because it's **functional, not visual** (so it doesn't need the A/B verification the structural tier does) and it extends the already-built §M-44 multi-window infra. Codex: `electron:{menuTitle:"New Window", defaultKeybindings:[{key:"CmdOrCtrl+Shift+N"}]}`.
- **main.rs** — `host_open_new_window(app)`: `WebviewWindowBuilder` + `WebviewUrl::default()` + a `static AtomicU64` counter for unique window labels + `initialization_script` injecting `window.__HICODEX_INITIAL_NEW_CHAT__ = true` (mirrors §M-44's thread-id injection). Registered in `generate_handler!`.
- **tauri-host.ts** — `openNewWindow()` → `invoke("host_open_new_window")`.
- **commands.ts** — `COMMAND_IDS.newWindow` + descriptor (title "New window", `⌘⇧N`, `availableIn: ["electron"]` desktop-only, `commandMenuGroupKey: "navigation"`).
- **HiCodexApp.tsx** — registers the command (handler `openNewWindow().catch(…)`, caught so browser ⌘⇧N is a harmless no-op) + a startup effect mirroring §M-44's: on `connected`, if `__HICODEX_INITIAL_NEW_CHAT__`, call `createWorkbenchThread()` once (the fresh window starts a new chat).

Verified: **cargo check ✓ · 26 cargo tests ✓ · tsc 0 · 74 UI ✓ · build ✓**. Runtime caveat (same as §M-44, accepted precedent): the actual multi-window open + the new-chat-on-startup timing **can't be verified headless** — needs a real multi-window manual check. Additive: the main window has no injected global, so its behavior is unchanged.

**Native-menu discovery (corrects §M-54).** HiCodex has a native macOS menu (`install_native_menu`, main.rs:2394) where window/app commands live: New Chat (⌘N → emits `newChat`), Search (⌘K → `search`), Settings (⌘, → `settings`), **Close Window (⌘W)**, **Reload (⌘R)**, Quit (⌘Q → `app.exit`). So §M-54's "closeTabOrWindow / reloadBrowserPage absent" was wrong — they exist as **deliberate "unsupported" stubs** (`MENU_CLOSE` → "Close Window is unsupported because HiCodex has no safe tab target"; `MENU_RELOAD` → "Reload is unsupported because HiCodex has no separate browser panel"). The webview `newWindow` ⌘⇧N works on desktop precisely because ⌘⇧N is *not* a native-menu accelerator (so the webview keyboard layer captures it).

**Close Window un-stubbed (done).** `closeTabOrWindow` (⌘W) was a stub ("no safe tab target") — stale once §M-44/§M-55 added real secondary windows. `MENU_CLOSE` now closes the focused **secondary** window (`label != "main"`, via `app.webview_windows().into_values().find(is_focused).close()`); the **main window stays guarded** (⌘W keeps a no-tab-target message there — closing the app is ⌘Q's job), so the blast radius is bounded. Verified **cargo check ✓ · 26 cargo tests ✓**; the which-window-is-focused close is runtime-flagged (same multi-window caveat).

**Native-menu New Window (done).** Added `MENU_NEW_WINDOW` ("New Window", ⌘⇧N) to the native File menu (after New Chat). Refactored the window-open into a shared `open_new_window_impl(&AppHandle)` helper used by both the `host_open_new_window` command and the menu handler (`MENU_NEW_WINDOW => open_new_window_impl(app)`). This is the **same native+webview coexistence as New Chat** (`MENU_NEW_CHAT ⌘N` + webview `newThread ⌘N` both ship): on desktop the native menu accelerator swallows ⌘⇧N (so the webview command is the browser/palette path — no double-open); New Chat shipping without a double-fire bug is the evidence this is safe. So New Window now matches Codex across all three surfaces (menu bar + accelerator + command palette). Verified **cargo check ✓ (no warnings) · 26 cargo tests ✓**.

**Net (window commands):** the full window-command pair is now aligned — `newWindow`/⌘⇧N (menu + palette + key) and `closeTabOrWindow`/⌘W (close focused secondary window; main guarded). The only window item left is Reload (⌘R), still a legitimate stub (HiCodex has no in-app browser panel). Remaining feature-dependent commands (`reloadBrowserPage`/`hardReloadBrowserPage`/`toggleTraceRecording`) need features HiCodex doesn't have.

## §M-56 — keyboard-shortcuts table → Codex's one-flat-table structure (first structural-visual port, flagged)

Re-framed the "structural/visual" tier: the saved [[feedback_hicodex_visual_ab_verify]] rule is a *verification method* (verify structural/visual diffs against Codex.app, because string audits miss them), **not a ban on the change**. So — like §M-44/§M-55 ship-and-flag-for-runtime — I can port a structural change by **faithfully replicating Codex's verified structure** and flagging the pixel-check for the user's A/B.

Did the most bounded one: the keyboard-shortcuts settings table. HiCodex rendered **per-section tables** (an `<h3>` + `<table>` per group, **no column headers**); Codex (audit-6) renders **one flat table**: `<colgroup>` + a single `<thead>` (`settings.keyboardShortcuts.table.command`="Command" / `.keybinding`="Keybinding" / `.actions`="Actions" sr-only) + per-section group-header rows in the `<tbody>`. Restructured to match — one `<table>` with a colgroup (50/30/20 widths, so column sizing is explicit/reliable), the column-header thead, and `sections.flatMap` emitting a `hc-keyboard-settings-section-row` group header + the Row `<tr>`s per section. CSS: colgroup widths, thead styling (left/tertiary/border-b), section-row th (reusing the old uppercase-secondary section-title style), 16px edge-cell insets, sr-only. Verified **tsc 0 · 75 UI · build ✓** — added `keyboard-shortcuts-settings-panel.test.ts` (the panel was untested), an SSR render test asserting the new structure: **exactly one `<table>`**, a single `hc-keyboard-settings-thead` with `Command`/`Keybinding` headers, and per-section `hc-keyboard-settings-section-row` group rows. So the **DOM structure is now regression-locked**. **Visual flag (unchanged):** the rendered *pixels* (column alignment, header/section-row spacing) still need an A/B against Codex.app — I replicated Codex's known structure but can't verify appearance headless (same ship-and-flag as §M-44/55, visual instead of runtime).

The other two structural items stay deferred for **more than "can't A/B"**: the goal chip ("Goal: {objective}" → Codex "Sent as goal") would **lose the inline objective** (Codex moves it to a `composer.threadGoal.*` pill family HiCodex doesn't have — a functional regression, not just a restyle); the context-usage tooltip card has the audit-8 **wrong-surface** ambiguity (the `% used`/token strings are the composer's, but HiCodex's footer emulates the Desktop status-popover). Building the goal-pill family is the real unlock for the chip.

## Quick history

- 2026-05-29 (visual-contract pass, §J): multi-agent CSS/layout sweep — resolved real-bundle Tailwind to concrete px/rem/hex vs clean-room semantic CSS (the dimension string audits miss). 26 fixes applied + 3 orphan rules removed: assistant prose line-height 22px / headings 24·18·18px wt600 / list+paragraph spacing; inline-code, blockquote (italic), strong 600, hr; transcript padding 16px; hero weight 400; action-button 26px/r8 + instant reveal; composer chips 12px/28px + blur 16px; worked-for label 14px; exec card r10 / footer 14px / divider gap 8px; exec-card "Shell" title + cwd removed (embedded variant) + single-run `$ {command}`. User bubble audited already-aligned. Then all 3 deferred items landed: composer model/reasoning/permissions chips moved in-bubble (new `ComposerSettingsChips` `footerSettings` slot; needs an in-app eyeball), background `#faf9f5`→`#fff`, and the MCP text-result code-card (sticky `plaintext` header) — plus an exec copy-button overlap fix (`:has()` region scoping, mirroring Codex's per-region group-hover). typecheck + build green; **all 73 UI tests pass** (the long-red `buildsMcpDetails` went green with the MCP card).
- 2026-05-29: Codex Desktop updated to v26.519.81530 (May 27 ASAR); multi-agent re-audit of all 8 intermediate-output surfaces (§I). 18 source-backed fixes applied — `model/rerouted` timeline item; unified-diff failure-dialog 4-way title + body intro + success/danger toasts + "Git apply error" line + "Review" label + arrow icon; plan shared copy strings + filled fade button + 2000ms reset; exploration plain-comma counts + accordion heights; generated-image aria trim; restored hover-revealed per-message timestamp; filled selected thumb; removed invented "Feedback sent"/"Copied to clipboard"/gallery aria literals; markdown href + image scheme allowlists (codex:// clickable, ftp dropped). 6 deferred. Corrects the earlier exploration-count "conjunction" claim (B7 / reasoning §3): Desktop uses a plain ", " separator; the conjunction is only the cross-type web-search/MCP summary. typecheck + build + 73 UI tests green.
- 2026-05-24: Codex Desktop ASAR re-checked for assistant memory citations. HiCodex now renders citation source rows through the Desktop-style file-reference button path rather than fallback `href` anchors, while keeping the localized summary/line labels, full-path truncation, and note layout aligned.
- 2026-05-24: Codex Desktop ASAR re-checked for automation citation chips. HiCodex now keeps chips static until an automation id and open route are available, then routes the button through the Automations panel instead of logging a debug-only click.
- 2026-05-24: Codex Desktop ASAR re-checked for inline diff detail rows. HiCodex now renders the edited-file detail list, show-more/collapse behavior, large-file placeholder, and Desktop's single-file `Details` row without duplicate per-row stats.
- 2026-05-24: Codex Desktop ASAR re-checked for reasoning placeholder and generated-image surfaces. HiCodex drops completed reasoning remnants, renders the turn-level generated-image gallery, keeps the right-rail 6-item Artifacts cap, and opens generated images in the Desktop-style lightbox.
- 2026-05-20: Codex Desktop re-extracted; reasoning (`B1`–`B3`), collab agent (`D`), and several `B5`–`B6`/`B8`/`B10` entries retracted after evidence review showed Codex either does not render the surface (`Ux` dead code) or already matches HiCodex semantics.
- E2 (Failure Dialog + `host_apply_patch_action`) shipped in the same pass.
- A1 reducer convergence (4 channels retired) shipped in the same pass.
